# Phase 10 -- `spherical-indexing-emsphinx-regression`: plan

Branch `spherical-indexing-emsphinx-regression` off `develop` (after
the Phase 9 merge, jwestraadt/kikuchipy#10). Models: plan/spec on
Fable 5 (xhigh, ultracode); tests, implementation, adversarial
review and fixes by Opus 5 agents (xhigh, ultracode). Autonomous
mode (approval gate waived for spherical-indexing phases; decisions
flagged in section 6). Tests are written (failing) before the code
they exercise; the reference files themselves are generated by the
committed script during implementation and the failing tests assert
against their promised names/keys. No numba kernels this phase.
Every number in `requirements.md` was measured on 2026-09-03 with
the drafting probes (`p10_gen.py`, `p10_ours.py`, `p10_extra.py`)
or the adversarial-review re-measurements (`p10c_*.py`,
`p10r_*.py`; session scratchpad, not committed; recipes and full
outputs in `validation.md` "Recorded results") against the built
EMSphInx binaries @ 60f3517. **Revised 2026-09-03 after adversarial
review**; the spec-level deltas are marked in `requirements.md`.

## 0. Constitution amendments (applied 2026-09-03 in the spec commit)

1. `specs/roadmap.md`, Phase 10 box: rewrite into **five**
   deliverable boxes (the drafting rewrite had three and omitted the
   review and PR gates the other phases carry -- review finding)
   with the measured facts:
   - box 1: "`create_emsphinx_reference.py` in
     `src/kikuchipy/data/emsphinx/` (import-safe; pinned to 60f3517
     via a `git rev-parse` hard assert; canonical route:
     `Manufacturer` EMsoft unflipped, nml vendor Bruker +
     `pc_average`, `nthread=1 batchsize=1`, `bw` 68, uint8 repacks
     byte-identical to `signal.data.reshape(-1, h, w)` --
     guard-asserted; parses the datafile `Scan 1/EBSD/Data` (float32
     Phi1/Phi/Phi2/Metric/IQ, uint8 Phase -- the `.ang` is a
     5-decimal-Euler/3-decimal-ci/1-decimal-iq cross-check only,
     measured); generation-time acceptance guards (acid median <
     1.2 deg, coarse/refined IQ bitwise); takes the machine-wide
     binary lock; measured ~11 s end-to-end, bitwise deterministic
     run-to-run **given a fixed fftw.wisdom** and md5-identical
     across full sweeps)".
   - box 2: "eight `regression_*.npz` refs in
     `src/kikuchipy/data/emsphinx/` (scenario matrix: coarse/refined
     x nregions **{0, 7, 10}** -- 7 replaces the pre-measurement 4
     so the mosaic-AHE remainder path is binary-compared (measured
     0.335/0.367 deg, IQ 6.6e-9) -- + gausbckg T + the EMsoft
     vendor route at d500; **the delta axis is retired as
     measured-inert**: EMsoft-route d250 = d500 bitwise through the
     binary AND bit-identical round-tripped pc for delta
     {125, 250, 500}, so the delta invariance is pinned as a
     namelist round-trip unit test instead; + large `[::15, ::15]`
     20-pt and `[::5, ::5]` 165-pt subsets; float32 result arrays +
     provenance incl. the exact namelist text, the
     `.6g`-round-tripped `pc` the binary used, and
     patterns/master md5s; ~60 kB total, each < 100 kB, md5s in
     `_registry.py`, no URL; NO refine-only scenarios --
     `refineImage` discards its refinement, research item 40)".
   - box 3: "bidirectional tests: ours-vs-theirs CI bands measured
     then pinned (refined median 0.31-0.34 -> < 0.7, max 0.34-0.49
     -> < 0.75/0.8/1.0, coarse 0.510/0.622 -> < 1.0 median, >= 8/9
     < 1.25, all < 4.0 (one grid cell = 2.667 deg); scores r
     0.935-0.973 -> > 0.85/0.88/0.90 with mean|diff| < 0.03, max <
     0.07; IQ max|diff| <= 1.4e-8 -> < 1e-3 (one uint8 gray level
     moves one pattern's IQ by up to 5.2e-5 -- the fastmath
     platform-drift ladder) -- the preprocessing discriminator;
     route pins: from_kwargs == the stored namelist text exactly,
     pc == its `.6g` round trip; the stretch-emulated diagnostic
     pins the item-31 decomposition (0.34 -> 0.094 deg); reference
     integrity guards labelled regeneration-only); theirs-on-ours =
     Phase 9 acceptance + the local-gated regenerate-and-diff
     (md5-identical, wisdom-qualified, large weekly); the a-priori
     refined < 0.2 / coarse < 0.5 / r > 0.98 are re-anchored on the
     measured item-31 decomposition (mission criterion 2 amended:
     the < 0.2 gate is met under stretch emulation)".
   - box 4: "adversarial review (fidelity to the recorded
     constraints, conventions, bug-injection list) + fixes".
   - box 5: "signed commits pushed; PR #11 opened".
2. `specs/tech-stack.md`:
   - "Tests, docs, data": extend the EMSphInx references sentence
     with the measured contract -- "the Phase 10 `regression_*.npz`
     refs hold the datafile's float32 Phi1/Phi/Phi2/Metric/IQ +
     uint8 Phase per scan point plus provenance arrays
     (emsphinx_commit probed from the checkout, bw, normed, refine,
     nregions, gausbckg, delta, vendor, route, dataset, scan_shape,
     scan_steps, sample_tilt, subset_slice, emsphinx_compatible,
     manufacturer, flip, kikuchipy_version, master_sht/master_md5,
     patterns_md5, **the exact namelist text the binary consumed**,
     and the `.6g`-round-tripped `pc` it used -- CI sets
     `detector.pc = ref['pc']` and recomputes namelist and pc from
     the declared route); `np.savez` uncompressed, no timestamps,
     byte-stable (measured: full regeneration sweeps are
     md5-identical given a fixed fftw.wisdom); the `.ang` is
     text-rounded (Euler 5 decimals = 5e-6 rad, ci 3 decimals of
     the Metric = 5e-4, iq 1 decimal) and is a cross-check, not the
     payload; comparisons: orientations via `angle_with` bands
     (both sides `Orientation(..., Oh)`, `degrees=True`), scores
     via Pearson + |diff| bands (both metrics normalised, ~2 %
     apart -- never equality), IQ via near-equality < 1e-3
     (measured <= 1.4e-8 parity; one flipped gray level = up to
     5.2e-5 -- IQ is the preprocessing-parity probe and its
     failure message names `patterns_md5`); each ref < 100 kB,
     asserted per file".
   - "Numerics", the existing "EMSphInx' `X (w - 1)` stretch of up
     to 0.49 px is not reproduced" parenthesis gains the measured
     consequence: "(measured consequence, Phase 10: emulating the
     stretch in pc space collapses the cross-engine refined median
     from 0.34 to 0.094 deg -- ~75-85 % of the residual -- while
     *worsening* agreement with the stored xmap (0.365 -> 0.584
     deg), so the physical convention is kept and the regression
     suite pins the collapse)".
3. `specs/mission.md`:
   - success criterion 2 (**flagged for user review** -- 6.1):
     replace the a-priori numbers with the measured bands **and the
     decomposition** -- "Agrees with `IndexEBSD.exe` (EMSphInx
     `master` @ 60f3517, CPU, `nthread=1`) on kikuchipy's Ni
     datasets on identical inputs at `bw` 68 (measured 2026-09-03,
     shipped as the Phase 10 regression suite): refined median
     misorientation 0.31-0.34 deg (pinned < 0.7), coarse median
     0.51 (pinned < 1.0), scores Pearson r 0.94-0.97 (pinned >
     0.85-0.90) with mean absolute difference < 0.03, image quality
     equal to <= 1.4e-8 (CI band 1e-3 for cross-platform uint8
     drift); the residual is EMSphInx's deliberately un-ported
     `bilinearCoeff` sampling stretch (research item 31): emulating
     it collapses the refined median to 0.07-0.09 deg -- under the
     original < 0.2 gate -- while worsening agreement with the
     stored xmap, so kikuchipy keeps the physical convention and
     the suite pins the collapse; a kikuchipy-written `.sht` and a
     kikuchipy-repacked pattern file are accepted by the EMSphInx
     binaries (Phase 2/9 gated tests + the Phase 10 bitwise
     regenerate-and-diff)". Rationale recorded in requirements D7
     (the guesses predate the measured decomposition; the drafting
     draft's "peak-interpolation and Newton-path differences"
     attribution was refuted by the review's stretch emulation).
   - success criterion 5: append the roadmap's exemption so the
     no-CHANGELOG call does not contradict the mission -- "... and
     a CHANGELOG entry (phases with no user-facing change may skip
     it, per the roadmap gate rule)" (review finding).
4. Research addenda:
   - `specs/_research/explore-emsphinx-programs-and-formats.md`
     section 7: the datafile `Scan 1/EBSD/Data/Phase` is **uint8**
     (Phase 9 D7 recorded float32 -- corrected by measurement); the
     `.ang` columns are written under `std::fixed`
     (`tsl.hpp:783-794`): Euler at `setprecision(5)` (max 5.0e-6
     rad vs the datafile), ci = the Metric at `setprecision(3)`
     (max 5.0e-4), iq = the IQ at `setprecision(1)` ({0.1, 0.2,
     0.3} observed -- Phase 9's "constant 0.2" explained).
   - `specs/_research/explore-emsphinx-core-algorithm.md` section 8
     new items:
     (48) `IndexEBSD.exe` at `nthread=1 batchsize=1` is bitwise
     deterministic run-to-run **given a fixed fftw.wisdom**: the
     programs plan with `FFTW_PATIENT` (`square_sht.hpp:384`,
     `sht_xcorr.hpp:366`) and import/export the machine-wide wisdom
     at start/exit (`util/fft.hpp:71-108, 281-330`) -- PATIENT
     picks algorithms by timing and different algorithms round
     differently, so the bit pattern is a function of the wisdom
     state (md5 recorded beside the reference md5s); the shared
     file is also a race -- serialize runs.
     (49) the delta axis is **inert on every vendor route**: the
     EMsoft-vendor pair {250, 500} through `.6g` namelists is
     bitwise identical in output (`sDst` scales exactly in decimal:
     15021.2/7510.6/3755.3) and the round-tripped pc is
     bit-identical for delta {125, 250, 500} -- delta invariance is
     a property of the kikuchipy conversion, pinned as a namelist
     round-trip unit test, not a reference scenario; the
     EMsoft-vs-Bruker route rounding differs at 1e-6 in pc = 5.8e-5
     deg.
     (50) EMSphInx's DCT IQ equals kikuchipy's ported IQ to float32
     quantisation (<= 1.4e-8 over 165 points and every
     preprocessing variant, incl. the non-dividing `nregions` 7
     remainder path at 6.6e-9) -- IQ is the sharpest cross-engine
     preprocessing probe; but it is chained to the exact uint8
     pattern bytes: one gray level in one pixel moves that
     pattern's IQ by up to 5.2e-5 (AHE histogram cascade, 24-trial
     scan), so cross-engine CI bands must sit above the
     platform-drift budget (~2e-4 under numba fastmath) and below
     the smallest real signature (1.5e-2).
     (51) **item 31 measured end-to-end** (Phase 10): emulating the
     `bilinearCoeff` stretch in pc space
     (`pc' = (0.5/w + pc_x (w-1)/w, 0.5/h + pc_y (h-1)/h,
     pc_z (w-1)/w)`) collapses the kikuchipy-vs-IndexEBSD refined
     median from 0.3404 to 0.0940 deg (coarse 0.5096 -> 0.0745,
     large165 0.3390 -> 0.0717; scale-scan minimum exactly at
     `(w-1)/w`), i.e. ~75-85 % of the cross-engine residual is
     this documented sampling-convention difference and the true
     interpolation/Newton floor is ~0.07-0.09 deg; the emulation
     worsens stored-xmap agreement (0.365 -> 0.584 deg), so the
     port keeps kikuchipy's physical convention.
5. Tick the roadmap Phase 10 boxes only as gates complete (spec
   commit, failing tests, refs + implementation, review, PR #11).

## 1. `create_emsphinx_reference.py` + the eight references

1. Script per requirements D4 (import-safe module; `main(output_dir=
   None, program=None)` resolving `KIKUCHIPY_EMSPHINX_DIR` only when
   `program is None`; the machine-wide lock file
   (`<tempdir>/kikuchipy-emsphinx-program.lock`, the conftest
   protocol) taken inside `main`; commit hard-assert; run dirs;
   canonical-route guards with the `reshape(-1, h, w)` byte
   comparison; D3 parsing; `.ang` cross-check; generation-time
   acceptance guards; D2 `.npz` writer incl. the verbatim
   `namelist` text, `patterns_md5`, `master_sht`/`master_md5`,
   `preprocessing`, `subset_slice`, `emsphinx_compatible`,
   `manufacturer`, `flip`, `kikuchipy_version` keys; prints the
   fftw.wisdom md5 before and after the sweep; `__main__` guard;
   sequential runs; kikuchipy GPL header; docstring embedding the
   recipe and the EMSphInx commit inline -- no `specs/` path).
2. Scenario table = requirements D1 verbatim (eight scenarios,
   `nregions` {0, 7, 10}, no d250), expressed once as a
   module-level tuple that both the regenerate-and-diff test and
   the CI completeness test import (name, dataset, vendor, delta,
   refine, nregions, gausbckg).
3. Large subsets: full-map background removal, `inav[::step,
   ::step]`, full-map `pc_average`, `scan_steps` = axis scale x
   step; repack as their own (rows, cols) scans; `subset_slice`
   recorded.
4. Run on this machine; commit the eight `.npz` (regenerated with
   the final key set -- the drafting candidates lack the review
   keys) plus the script; add the eight md5 lines to
   `src/kikuchipy/data/_registry.py` (no URLs). Record final md5s,
   sizes and the wisdom md5 in `validation.md`.

## 2. CI tests -- `tests/test_indexing/test_spherical_emsphinx_regression.py`

1. Module docstring mapping tests to this spec; kikuchipy header;
   frozen constants: the pinned commit (**maintained independently
   of the script's literal** -- the CI commit check has power only
   through this duplication, stated in a comment), the band values
   of D6, the scenario/kwargs table.
2. Shared helpers: `load_reference(name)`
   (`Dataset(...).fetch_file_path()` + `np.load(allow_pickle=
   False)`), `scenario_kwargs(ref)` (provenance -> indexing kwargs,
   D5), `their_orientations(ref)` (both sides `Orientation(...,
   Oh)`; `from_euler` on the float64 Bunge-radian stack),
   `misorientation(...)` (the Phase 7 test-module pattern), a
   **session-scoped** harmonics fixture (`from_file` + resize is
   the dominant fixed cost and is rebuilt once per xdist worker),
   one indexing-run cache per scenario (module-scoped fixture).
3. `TestReferenceFiles` (CI): registry lists exactly the eight
   files and their md5s verify -- glob anchored at
   `Path(kikuchipy.data.__file__).parent / "emsphinx"` (the
   installed package; the `-build-install-wheel` job has no
   `src/`); **completeness quad-equality** (script scenario table
   == registry keys == directory glob == the test module's frozen
   table; the script import is what D4's import-safety buys);
   every file loads with `allow_pickle=False`; frozen key set and
   dtypes (float32 result arrays, uint8 phase, float64 `(3,)` pc,
   unicode provenance incl. `namelist`); provenance pins (commit
   vs the module's own literal, bw 68, normed True, sample_tilt
   70.0, phase all zero, dataset/scan_shape consistency);
   **per-file size < 100 kB** (the constitution's rule) with the
   total recorded via `record_property`.
4. `TestReferenceIntegrity` (CI, refs only, D6 -- class docstring:
   frozen bytes vs frozen bytes, fires only at regeneration; NOT
   parity surface): emsoft-vs-Bruker gap < 1e-3 deg and metric not
   identical; coarse/refined IQ bitwise + refined metric strictly
   above coarse per point (no upper bound); nregions/gausbckg IQ
   arrays pairwise distinct.
5. `TestRoutePins` (CI, real conversion code, D6):
   `test_namelist_matches_the_declared_route` (`from_kwargs(**
   frozen) -> to_string()` == `ref["namelist"]` exactly, per small
   scenario) and `test_pc_matches_the_stored_namelist`
   (`from_string(ref["namelist"]).to_detector(70).pc` ==
   `ref["pc"]`, exact float64 -- safe only because `.6g` quantised
   first; comment says so). The large-scenario recomputes live in
   the pooch-gated class.
6. `TestOursVsTheirsSmall` (CI): parametrised over the six small
   scenarios; per scenario assert the D6 misorientation bands
   (coarse with the 8/9 + single-cell-jump clause), score r +
   |diff| bands, the IQ < 1e-3 band (assertion message names
   `ref["patterns_md5"]`); the sample-tilt binding test (detector
   == harmonics == ref); the kwargs-table pin (`scenario_kwargs`
   output vs the frozen table -- the refine-flag mutant's killer,
   D5); `test_stretch_emulation_collapses_the_residual` (the D7
   evidence pin: item-31 pc-space emulation on the refined anchor,
   median < 0.2 deg and < the unmodified median, both recorded);
   measured values recorded via `record_property`.
7. `TestOursVsTheirsLarge` (default suite, `importorskip("pooch")`):
   the 20-pt scenario with its D6 bands + the large namelist/pc
   recomputes; `@pytest.mark.weekly` `test_the_165_point_scenario`
   with the weekly bands (median < 0.7, p95 < 0.9, max < 1.0,
   r > 0.88).
8. `tests/test_indexing/test_spherical_namelist.py` gains
   `test_emsoft_delta_invariance_of_the_conversion` (CI, D1): the
   EMsoft-vendor `from_kwargs -> to_string -> from_string ->
   to_detector` pc is bit-identical across delta {125, 250, 500}
   and the `.6g` `sDst` values are the decimal-exact
   3755.3/7510.6/15021.2 -- the retired delta axis's whole content,
   pinned where the per-module precedent puts conversion tests.

## 3. Local-gated tests (KIKUCHIPY_EMSPHINX_DIR)

1. `TestRegenerateReferences` (same module, gated via the conftest
   `emsphinx_program` fixture -- which holds the binary lock; the
   test **calls `emsphinx_program("IndexEBSD")`** (the "not built"
   skip lives inside that callable, not in the fixture request) and
   passes the resolved path into
   `create_emsphinx_reference.main(tmp_path, program=...)`,
   restricted to the small scenarios; assert per-file md5 equality
   with the shipped refs; on mismatch, emit (1) the fftw.wisdom
   caveat (suspect #1) and (2) the per-array diff as the assertion
   message (D8). Import the script module lazily inside the test
   (no collection-time import).
2. Weekly + gated: the same over all eight scenarios (adds pooch,
   the full-map background removal and ~2 s of indexing).
3. No further gated tests: Phase 9's acceptance suite is the
   theirs-on-ours surface (requirements D8).

## 4. Registry, coverage, housekeeping

1. `src/kikuchipy/data/_registry.py`: eight md5 lines **appended
   after `emsphinx/ni_small_20kv_bw384.sht`** in the in-package
   block, values aligned to the existing hand-maintained column
   (the block is grouped by directory, not alphabetical, and sits
   inside `# fmt: off` -- ruff-format will not fix alignment; the
   longest new key, 49 chars, fits the column set by the 62-char
   `emsoft_ebsd_master_pattern/...` key). No `_registry_urls`
   entries.
2. `pyproject.toml`: add `omit = ["src/kikuchipy/data/*/create_*.
   py"]` to `[tool.coverage.run]` **in the same commit as the
   script** (verified: no `omit` exists today; without it the
   never-imported scripts report 0 % and move the codecov project
   status -- there is no `codecov.yml` to absorb it).
3. No CHANGELOG entry (D9, flagged 6.1); no `__init__.pyi` or docs
   pages; the two docstring `Notes` sentences of D9 in
   `signals/ebsd.py` and `_spherical/_indexer.py`; the existing
   `--ignore-glob=src/kikuchipy/data/emsphinx/*.py` already covers
   the new script in the doctest job (verified present in
   `pyproject.toml` -- re-check at implementation).
4. `pre-commit run --files` over the script, the test modules,
   `_registry.py`, `pyproject.toml` and the two docstring files
   (never `specs/`). NB the GPL licenseheaders hook has **no
   `files:` regex, only an `exclude:`** (verified in
   `.pre-commit-config.yaml`) -- what protects the `.npz` binaries
   is licenseheaders' own extension whitelist, not a repo regex;
   confirm on the tests-first run that the hook leaves them
   untouched.

## 5. Adversarial review and fixes

1. Fidelity reviewer refutes against the recorded constraints: the
   canonical route (Phase 9 D1/D7), uint8-only (D2/item 45), the
   retired delta axis and its namelist pin (D1/items 44-47/49), the
   refine-only ban (item 40), resize-from-stored-bw parity (item
   39), the item-31 decomposition (D7/item 51), the datafile layout
   and `.ang` 5/3/1-decimal precision (D3), the point-order
   contract, and the D6 band arithmetic (each band vs its measured
   value and margin, incl. the IQ ladder and the coarse
   single-cell clause). Conventions reviewer: GPL headers, no
   public surface beyond the D9 Notes, registry style (4.1's
   wording), weekly/pooch gating, **no `specs/` paths in `src/`**
   (the codebase's actual rule -- phase numbers do appear in
   shipped `_spherical` docstrings and are fine), the
   no-CHANGELOG + criterion-5 reconcile (6.1).
2. Test-quality reviewer runs the **bug-injection list** -- every
   mutant dies by a named test (or is recorded as reviewed-only
   with its killer stated):
   - **script: flip route inverted** (`manufacturer="Bruker"` or
     `flip=True`): dies at generation by the `/patterns ==
     signal.data.reshape(-1, h, w)` guard, and belt-and-braces by
     the acid median-< 1.2 guard (wrong pairing measured ~39.6
     deg).
   - **script: non-uint8 repack** (float32 signal cast dropped):
     dies by the generation dtype guard; end-to-end it would also
     die by the acid guard (uint16 corruption measured 38.9 deg).
   - **script: commit check dropped / wrong checkout**: dies at
     generation by the `git rev-parse` hard assert; a stale pin
     dies because the stored `emsphinx_commit` is probed and CI
     compares it against the **test module's independent literal**
     (the two pins are equal by construction only while both are
     maintained -- the comment states the duplication is the
     mechanism).
   - **script: Euler stored in degrees**: dies by every
     misorientation band (CI, garbage-scale angles).
   - **script: `phi1`/`phi2` swapped**: dies by the refined
     misorientation bands (a Bunge swap is degree-scale on these
     orientations).
   - **script: `metric` written from IQ** (column mixup): dies by
     the score |diff| band (|0.2 - 0.6| >> 0.07) and the r band.
   - **script: `iq` taken from the `.ang` column** (1-decimal):
     dies by the IQ near-equality band (rounding is ~5e-2 >> 1e-3).
   - **script: Euler parsed from the `.ang`** (text-rounded): NOT
     killable by bands (5e-6 rad ~ 3e-4 deg); dies by the
     regenerate-and-diff md5 (the shipped refs came from the `.h5`)
     -- recorded as the gated killer.
   - **script: `emsoft_d500` generated on the Bruker vendor** (the
     one route mixup left after the delta-axis retirement): dies by
     `test_namelist_matches_the_declared_route` (the stored
     namelist would read `vendor 'Bruker'` against the frozen
     table's EMsoft -- exact string mismatch) and by
     `test_pc_matches_the_stored_namelist` + the vendor provenance
     pin.
   - **script: `from_kwargs` mishandles `delta`** (the retired
     axis's residual risk): dies at generation -- a dropped `delta`
     scaling puts `sDst` outside sanityCheck's [5, 90] mm window
     (exit 1) or moves the geometry through the acid guard -- and
     in CI by the namelist module's delta-invariance pin (2.8).
   - **script: pc stored as the unrounded `pc_average`**: dies by
     the exact `test_pc_matches_the_stored_namelist`.
   - **script: namelist text not stored / stored from a different
     run**: dies by the exact-equality route pin (and the scenario
     distinctness of the integrity guards).
   - **script: two scenarios written from one run** (mixup): dies
     by the integrity distinctness asserts (IQ pairwise distinct;
     emsoft-vs-Bruker metric must NOT be identical) and the
     namelist route pins.
   - **script: np.savez_compressed or pickled objects**: dies by
     `allow_pickle=False` loading + the md5 regenerate-and-diff
     (zlib is version-dependent) + the registry md5s.
   - **script/preprocessing: a background-removal default changes
     upstream**: dies at generation by `patterns_md5` +
     the acid guard; on CI it surfaces as an IQ-band failure whose
     message names `patterns_md5` as the changed input (D5) -- the
     recorded diagnosis channel for platform drift too.
   - **refs: registry entry missing/stale, or a scenario added
     without regenerating**: dies by
     `test_registry_lists_every_reference` + the completeness
     quad-equality (CI, no binaries needed -- review addition).
   - **test: kwargs hardcoded, `n_regions`/`gausbckg` ignored**:
     dies by the IQ near-equality band -- the nr0/nr7/gb refs'
     IQ differs from the nr10 pipeline's by 1.5e-2-1.3e-1
     (measured ranges 0.289-0.327 / 0.18565-0.22098 / 0.187-0.216
     vs 0.173-0.204; smallest gap 1.5e-2 >> 1e-3) -- and
     structurally by the kwargs-table pin.
   - **test: `refine` flag ignored**: NOT killable by the bands --
     measured cross-runs sit inside them (ours-coarse vs refined
     ref median 0.431/max 0.668 < 0.7/0.75; ours-refined vs coarse
     ref median 0.575 < 1.0; score |diff| also inside) and
     coarse/refined IQ is identical by construction. Dies by the
     frozen kwargs-table test (`scenario_kwargs(ref)` vs the
     literal per-scenario table, D5). Recorded honestly as a
     structural kill, not a band kill.
   - **test: detector pc left at `pc_average`**: sub-band
     (~3e-4 px); dies by review + the pc route pin recording what
     `ref["pc"]` is *for* -- recorded as reviewed-only.
   - **test: symmetry dropped from the misorientation** (bare
     `Rotation` on either side): the theirs-side bare `Rotation`
     dies loudly (`AttributeError: no attribute 'symmetry'`, orix
     0.14.2 -- review-measured); a both-sides-`Rotation`
     comparison dies by the refined bands (the m-3m reduction is
     degree-scale -- the Phase 4-7 precedent).
   - **test: large subset off by transpose** (`[::5, ::15]` or
     `inav` order confused): dies by the 20-pt scenario's bands
     (wrong points = wrong orientations, degree-scale) and the
     `scan_shape` provenance consistency test.
   - **conftest lock not held**: the gated test holds it via
     `emsphinx_program`; the manual path now holds it via `main()`
     itself (D4) -- the measured FFTW-wisdom race
     (`STATUS_STACK_BUFFER_OVERRUN`) is the rationale.
3. Fix, re-run (`-n 4` after a `-n 0` warm run is NOT needed -- no
   numba this phase), `pre-commit run --files <changed>`; coverage:
   the **new test module's helpers 100 %**; the generation scripts
   are excluded via the `omit` added in 4.2 (the drafting claim
   that an exclusion already existed was wrong -- verified, and
   fixed in the same commit).

## 6. Open questions -- decided 2026-09-03 (autonomous mode), flagged for review

1. **Mission criterion 2 is amended and no CHANGELOG entry is
   written** -- the two calls most worth a human look. The
   amendment replaces guessed gates with the measured bands *and
   the item-31 decomposition that explains them* (D7 -- the
   original < 0.2 gate is recorded as met under stretch emulation,
   not discarded); criterion 5 gains the roadmap's exemption clause
   in the same commit so the no-CHANGELOG call is not a documented
   contradiction. Fallbacks: keep the old criterion numbers as an
   explicit "superseded" note; resp. one `Added` line for the
   suite.
2. **`create_emsphinx_reference.py` carries the kikuchipy GPL
   header only**: it transcribes no EMSphInx expression -- it runs
   the binaries through Phase 9's own API. Precedent: `get_scan_info`
   (Phase 9 6.3) and `create_emsphinx_sht_fixtures.py`. The `.npz`
   payloads are program outputs with provenance, not stamped
   source. Fallback: the delimited notice.
3. **The datafile `.h5`, not the `.ang`, is the reference payload**
   (D3): float32 vs 5-decimal text, real IQ vs 1-decimal, and the
   Metric identified. The Phase 9 `read_ang` freeze is honoured
   where it applies (gated acceptance + the generation-time
   cross-check); it was never a promise that Phase 10's *payload*
   would be the `.ang`.
4. **A `gausbckg = .TRUE.` scenario is added** beyond the roadmap's
   literal matrix: without it the ported Gaussian background is
   never exercised end-to-end. Cost 5.2 kB; the roadmap box rewrite
   (0.1) records the widened matrix.
5. **`normed = .FALSE.` is not swept** (the roadmap fixes normed T;
   nothing consumes the un-normalised cross-engine comparison, and
   the plain-correlator path is pinned by Phases 4-7 unit tests).
6. **Regenerate-and-diff asserts file md5s, not tolerances**:
   measured foundation -- the binary is bitwise deterministic at
   `nthread=1 batchsize=1` **given a fixed fftw.wisdom** and
   `np.savez` is byte-stable (also across numpy 1.23.5/1.26.4/
   2.0.2), so two full sweeps were md5-identical. If another
   machine, FFTW build, wisdom state or a numpy zip-writing change
   ever breaks this, the recorded fallback is a measured tolerance
   amendment, not a silent loosening; the failure message names
   the wisdom first.
7. **`detector.pc = ref["pc"]`** (the `.6g`-rounded triple) rather
   than the unrounded `pc_average`: both engines then see identical
   geometry; the choice is pinned by the exact-recompute test, and
   the ~3e-4 px difference is sub-band either way.
8. **Scenario runs are cached module-scoped, the harmonics
   session-scoped** in the CI tests (one `spherical_indexing` call
   per scenario, ~0.15-0.3 s each measured warm). Budget restated
   for `-n 4` (review finding): xdist `--dist load` spreads the
   module across workers, each rebuilding the harmonics fixture, so
   the honest budget is **<= 8 s wall under `-n 4`**, not the
   serial ~2 s.
9. **The large refs keep only the canonical refined scenario** per
   subset: the matrix axes are small-map concerns; large adds
   statistics (165 points) and a real-map default-suite guard (20
   points). A coarse large scenario would add nothing the small
   coarse + large refined pair does not.
10. **File naming `regression_<scenario>.npz`** groups the eight
    refs behind one prefix in the data directory and the registry,
    away from the two `.sht` oracles.
11. **The delta axis is retired** (review finding, D1): measured
    inert on the EMsoft route too (bitwise output, bit-identical
    round-tripped pc); its content ships as the namelist
    delta-invariance unit test; the roadmap constraint is amended
    on the record with the null measurement.
12. **`nregions` 4 -> 7** (review finding, D1): every dividing
    nregions leaves the mosaic-AHE remainder path un-compared;
    nr7 measured 0.3346/0.3667 deg, IQ 6.6e-9 -- costs nothing,
    closes the last preprocessing path.
13. **Per-scenario `.npz` files are kept** over one combined file
    (review question, D2): per-file md5 granularity localises a
    drifted scenario in regenerate-and-diff and registry failures;
    the ~5 kB/file zip overhead and eight registry lines are the
    recorded price; the completeness test pins set == table either
    way.
14. **The IQ band is 1e-3** (review finding, D6): the critic's
    1e-4 proposal was itself re-measured -- the one-gray-level
    sensitivity is up to 5.2e-5, not 4.04e-6 -- so the band sits
    at 1e-3: ~5x above the fastmath drift budget, >= 15x below the
    smallest real signature; `patterns_md5` in the failure message
    is the diagnosis channel.
15. **The stretch-emulated diagnostic ships as a CI test** (review
    finding, D5/D7): one extra ~0.2 s run pins the decomposition
    the mission amendment rests on (median < 0.2 under emulation,
    and < the baseline median).

## 7. Commit and PR

1. Signed commits in gate order: spec + section-0 amendments;
   failing tests; script + regenerated refs + registry +
   `pyproject.toml` coverage omit + the D9 docstring Notes
   (implementation); review fixes. Tick the Phase 10 boxes in
   `specs/roadmap.md` with the measured numbers as each gate
   completes; push; PR **#11** into fork `develop` with the
   template, the licence statement (new code is kikuchipy-GPL only;
   no BSD opt-out question arises) and the no-CHANGELOG rationale
   (6.1).
