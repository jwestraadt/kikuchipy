# Phase 2 -- `sht-master-spectra-and-file`: plan

Branch `sht-master-spectra-and-file` off `develop` (after the Phase 1 PR
jwestraadt/kikuchipy#2 is merged; if not yet merged, branch off the Phase 1
branch and rebase-by-merge later). Models: this plan and the spec on Fable 5
(xhigh, ultracode); tests, implementation, adversarial review and fixes by
Opus 5 agents (xhigh, ultracode). Every task group is independently
reviewable; the order below is the implementation order. Tests are written
(failing) before the code they exercise. Numba caches: none of the new code
adds `@njit` kernels (vectorised NumPy suffices, `requirements.md` D6/D10);
Phase 1 kernels are reused, so run the new tests once with `-n 0` before
`-n 4`.

## 0. Constitution amendments (applied 2026-08-16 in the specs PR)
1. `specs/roadmap.md`, Phase 2 boxes: add `to_master_pattern` (direct Lambert synthesis, needed by `kp.load`) and the `_sht.py` lazy-weights amendment to the first box; symmetry LUT wording "38 groups" -> "the 38 `_groups` names + the `'2'`/`'m'` aliases returned by `get_point_group`"; data box: "`ni_20kv_bw384.sht`, `ni_small_20kv_bw384.sht` (mp2sht.exe; the latter from an uncompressed repack because `mp2sht.exe` lacks HDF5 deflate); synthetic per-(zRot, cmpFlg) fixtures generated at test time (`_dummy_files/emsphinx_sht.py`, md5s pinned after `sht2png.exe` acceptance)"; Phase 9 box: `to_master_pattern` and `describe` leave it (both ship in Phase 2; `describe` was listed in both boxes) -> "stereographic option, `plot_power_spectrum`, `.plot()` conveniences"; Phase 4 tests box gains the gate of D7: "normalised correlator with the Ni master in both `emsphinx_compatible` settings against a known rotation: argmax misorientation within the grid/refinement tolerance, score difference recorded"; Phase 6/7 boxes note "IndexEBSD.exe parity runs use the default `emsphinx_compatible=True`".
2. `specs/tech-stack.md`: symmetry line -> "38 `_groups` names plus `'2'` and `'m'` (orix z-unique aliases of `112`/`11m`); flags are validated against the coefficients (relative power of the systematic zeros <= `SYMMETRY_POWER_TOLERANCE = 1e-8`, `m > 0` doubled) and downgraded to the largest satisfied divisor with a warning on mismatch, because orix maps space groups 3-15 to z-unique groups while EMSphInx and SHTfile assume unique axis b; the `.sht` writer applies the same tolerance to the packing loss (`strict=True` raises, `False` warns and drops as EMSphInx does)"; add "`quadrature_weights` of `SphericalHarmonicTransform` is lazy; the Sneeuw guard applies to `analyze` only, Lambert *synthesis* works at any odd `dim`"; quirk paragraph: add the sentence "the default `emsphinx_compatible=True` reproduces EMSphInx/mp2sht (parity rel-L2 < 1e-6, measured 2e-9/9e-10); `False` gives the corrected normalisation with `a_00 ~ 0`; the un-normalised correlator is unaffected by the choice, the normalised one (EMSphInx's `normed = true` default; `IndexEBSD` never removes the master DC) can be -- Phase 4 gates it, Phase 6/7 parity runs use the default `True`"; add the lattice-unit caveat (kikuchipy's EMsoft readers keep nm, `.sht` `lat` is nm, the ebsdsim reader gives A -- `save()` writes the stored value); note `mp2sht.exe`'s HDF5 lacks deflate; licence-header line: "`_sht_file.py` is **BSD-3-Clause** (`.license_bsd.tmpl` header via the BSD hook, whose `files:` regex gains `indexing/_spherical/_sht_file\.py`, plus the verbatim SHTfile notice); it imports nothing GPL-derived and the GPL modules import it, never the reverse"; code-layout line: "the `.sht` reader plugin ... GPL header" unchanged; add "`from_master_pattern` refuses integer multi-site EMsoft masters (kikuchipy's reader sums sites in the file dtype, EMSphInx in float32)".
3. `specs/mission.md`: legal-status bullet for the `.sht` codec -> "SHTfile ... BSD-3-Clause -- `_sht_file.py` is shipped BSD-3-Clause with the notice verbatim (kikuchipy's BSD hook)"; success criterion 4 wording "BSD-3 SHTfile notice preserved for the `.sht` codec" stays true; optionally add "`.sht` written by kikuchipy carries `softwareVersion kp<version>` and a provenance `notes` string".
4. Repo config (**applied on the Phase 2 code branch**, not in the specs PR; fork and upstream): `.pre-commit-config.yaml` BSD-3 hook `files:`/GPL hook `exclude:` regexes gain `indexing/_spherical/_sht_file\.py`; `pyproject.toml` `addopts` gains `--ignore-glob=src/kikuchipy/data/emsphinx/*.py`.

## 1. Phase 1 amendment: lazy quadrature weights (`_sht.py`)
1. `SphericalHarmonicTransform.__init__` stores `dim`, `layout`, ..., but computes `self._quadrature_weights = None`; a `quadrature_weights` property (documented as before) computes and caches `_grid.quadrature_weights(dim, layout)` on first access; `analyze` accesses it (so the Lambert guard still raises there, now as `ValueError` from `analyze` or from the property); `synthesize` never does. `__repr__` unchanged.
2. Tests (`tests/test_indexing/test_spherical_sht.py`, added): `SphericalHarmonicTransform(384, "lambert", 769)` constructs and `synthesize` of a single `Y_l^m` (`(l, m) = (6, 4)`) reproduces `sph_harm_y` on `normals(769, "lambert")` to 1e-10 (gated `importorskip("scipy", minversion="1.15")` as Phase 1); `analyze` on the same transformer raises `ValueError` (Sneeuw guard); `quadrature_weights` is computed once (identity of the cached array). Grep Phase 1 tests for constructor-raises-at-large-Lambert-dim assertions and move them to `analyze` (none expected: `test_lambert_weights_raise_at_dim_401` targets `_grid.quadrature_weights`).

## 2. `.sht` codec (`_sht_file.py`) and the fixtures
1. Module, **BSD-3-Clause** (`.license_bsd.tmpl` header + the SHTfile BSD-3 block, D10/D17; imports only `numpy`, `dataclasses`, `struct`, `math` and stdlib): constants (`MAGIC_LE = b"*sht"`, `MAGIC_BE = b"*SHT"`, `VERSION = (1, 1)`, `MODALITY_*`, `VENDOR_*`, `FLAG_INVERSION = 0x01`, `FLAG_MIRROR_Z = 0x02`, `FLAG_MIRROR_Y = 0x04`, `FLAG_MIRROR_X = 0x08`, `LAT_GRID_*`), `_CRC_TABLE` (a `tuple` of 256 ints) generated from `0x1EDC6F41`, `crc32c(data: bytes | bytearray | memoryview, crc: int = 0) -> int` (plain-Python loop over the bytes, D10), `_pad8(n)`, dataclasses `ShtAtom`, `ShtCrystal` (five strings + their `*_bytes: bytes | None` raw padded twins), `ShtEMsoftSimulation`, `ShtHeader` (`doi`/`notes` + `doi_bytes`/`notes_bytes`), `ShtHarmonics`, `ShtFile` (+ lossless `to_dict()`/`from_dict()` and the payload-free `metadata_dict()` with numbered `crystal_0`/`simulation_0` nodes), `num_harmonics(bw, z_rot, flags)`, `pack_harmonics(alm, bw, z_rot, flags)`, `unpack_harmonics(packed, bw, z_rot, flags)`, `space_group_z_rotation(sg)`, `space_group_compression_flags(sg)` (verbatim 230-entry tables), `read_sht(source, check_crc=True) -> ShtFile` (bytes or path), `write_sht(filename, sht_file) -> None` (+ `sht_file_to_bytes(sht_file) -> bytes` used by both the writer and the byte-identity test; raw string bytes re-emitted when present, encoded + NUL-padded otherwise), `_sanity_check(sht_file)` mirroring the **full** `File::sanityCheck` (D10 list: header incl. reserved bytes, modality enum, padded string lengths, energy/angle ranges; mpData incl. `numXtal == len(crystals) == len(simulations)`, vendor enum, record size/modality/vendor; harmonics count; the File-level modality cross-check; plus `numXtal >= 1` and `bw <= 32767`).
2. Tests `tests/test_indexing/test_spherical_sht_file.py` (exact assertions in `validation.md`): CRC check values, table spot values by true index (`LUT[192]`, `LUT[200]`) and `sum(LUT) == 68719476608`; timing of `crc32c` on the 75 kB file recorded (`record_property`, expected ~4 ms); LUT checksums/spot values/orix cross-check (16..230 agree, 3..15 disagree as recorded); `num_harmonics` closed forms and `doub_cnt == num_harmonics(bw, z_rot, flags)` + `len(payload) * 8 == file_size - payload_offset - 4` over every shipped and generated `.sht` (27 files, 25 distinct flag pairs); pack/unpack identity for all 25 `(zRot, cmpFlg)` pairs on rule-respecting synthetic `alm` and `pack(unpack(payload)) == payload` bitwise on the three Ni files; header parse of the two in-package Ni files (all D16 facts) and of the shipped EMSphInx Ni file behind `KIKUCHIPY_EMSPHINX_DIR`; read -> `sht_file_to_bytes` byte identity for the two in-package files (and the shipped one); `write -> read -> write` idempotence + pinned md5 for the 25 generated ones; `from_dict(to_dict(f))` byte identity and `metadata_dict()` shape; CRC corruption -> `ValueError`, `check_crc=False` passes; big-endian magic and version `(1, 0)` -> `NotImplementedError`; wrong magic -> `ValueError`; `0x04|0x08` -> `ValueError`; opaque simulation bytes survive a round trip (synthetic Laue/EMsoft file with `simMetaSize 32`); `numXtal = 2` parses; string padding (`doiLen` 46 -> 48 B, 19 -> 24 B, 0 -> 0 B, 8 -> 8 B); a non-UTF-8 note byte + non-zero pad byte round-trips byte-identically; every writer sanity-check message; the module's import set contains nothing GPL-derived.
3. Synthetic fixtures at test time: `src/kikuchipy/data/_dummy_files/emsphinx_sht.py` (`SYNTHETIC_SPACE_GROUPS`, `create_synthetic_sht_files(directory) -> dict[int, Path]`, D16 recipe, RNG-free fill, `softwareVersion b"kp-fixt\x00"`), session fixture `emsphinx_synthetic_sht_files` in `conftest.py` (`tmp_path_factory`, precedent `conftest.py:620-630`); the 25 md5s are pinned in the test after the one-off `sht2png.exe` acceptance and recorded in `validation.md`. Shipped-fixture generation script `src/kikuchipy/data/emsphinx/create_emsphinx_sht_fixtures.py` (run once, output committed; every path a CLI argument with the values below as defaults; ignored by the doctest job via `pyproject.toml`):
   ```
   # (a) repack the in-package master without compression (mp2sht.exe has no HDF5 deflate)
   uv run python -c "import h5py,sys; s,d=sys.argv[1:3]
   fi=h5py.File(s,'r'); fo=h5py.File(d,'w')
   def cp(n,o):
       if isinstance(o,h5py.Group): fo.require_group(n)
       else:
           ds=fo.create_dataset(n,data=o[()],dtype=o.dtype)
           [ds.attrs.__setitem__(k,v) for k,v in o.attrs.items()]
   fi.visititems(cp); fo.close(); fi.close()" \
     src/kikuchipy/data/emsoft_ebsd_master_pattern/ni_mc_mp_20kv_uint8_gzip_opts9.h5 <tmp>/ni_small_uncompressed.h5
   # (b) the two mp2sht fixtures (run in <tmp>; neither exe writes template files to the CWD)
   "$KIKUCHIPY_EMSPHINX_DIR/build/Release/mp2sht.exe" <tmp>/ni_small_uncompressed.h5 src/kikuchipy/data/emsphinx/ni_small_20kv_bw384.sht
   "$KIKUCHIPY_EMSPHINX_DIR/build/Release/mp2sht.exe" "$KIKUCHIPY_DATA_CACHE/develop/data/ebsd_master_pattern/ni_mc_mp_20kv.h5" src/kikuchipy/data/emsphinx/ni_20kv_bw384.sht
   # (c) the 25 synthetic files with our writer into <tmp>/synthetic (create_synthetic_sht_files), then verify each with
   "$KIKUCHIPY_EMSPHINX_DIR/build/Release/sht2png.exe" <tmp>/synthetic/sg001_bw16.sht <tmp>/sg001.png   # exit 0, stdout contains "effective sg# 1"; md5 of each accepted file -> validation.md + the test's pinned dict
   # (d) md5sum src/kikuchipy/data/emsphinx/*.sht  -> _registry.py (two entries)
   ```
   `KIKUCHIPY_EMSPHINX_DIR = C:/Users/westraadt.1/Repos/EMSphInx` and the kikuchipy cache `C:/Users/westraadt.1/AppData/Local/kikuchipy/kikuchipy/Cache` on this machine. Register the **2** shipped files in `src/kikuchipy/data/_registry.py` (`emsphinx/...`, `md5:`), no URL. Data functions in `kp.data` for them are *not* added in this phase (tests use `kikuchipy.data._data.Dataset("emsphinx/...").fetch_file_path()`; open question 3 below for Phase 11).

## 3. Symmetry flags (`_symmetry.py`)
1. `Z_ROTATION_ORDER_AND_MIRROR` (40 keys, D9 table), `SYMMETRY_POWER_TOLERANCE = 1e-8` (the one tolerance shared with `save()`), `point_group_flags(name_or_symmetry) -> (n_fold, mirror)` (`ValueError` listing known names; `None` -> `(1, False)`), `space_group_for_point_group(name) -> int` (lowest sg with `get_point_group(sg).name == name`, computed once at import from orix, plus the axis-specific aliases of D11), `candidate_space_groups(name) -> tuple[int, ...]` (lowest sg of each distinct `(zRot, cmpFlg)` pair for that name; length 2 for `3m`, `-3m`, `-42m`, `-6m2`, else 1), `systematic_zero_power(alm, n_fold, mirror) -> (float, float)` (relative powers in rows `m % n != 0` and in odd `(l+m)` entries, `m > 0` doubled) and `validate_flags(alm, n_fold, mirror) -> (n_fold', mirror', warnings)` with the largest-satisfied-divisor downgrade (`6 -> 3 -> 2 -> 1`, `4 -> 2 -> 1`) and `<=` comparison.
2. Tests `tests/test_indexing/test_spherical_symmetry.py`: key set == `_groups` names | {"2", "m"}; every value equals the operator oracle (proper z-order, improper z 2-fold; D9); every `get_point_group(sg).name`, `sg in 1..230`, is a key; `space_group_for_point_group` spot values (D11) and `get_point_group(result).name == name` for the 32 names it returns; `candidate_space_groups` for the four ambiguous names and one unambiguous; `validate_flags` keeps `(4, True)` for the Ni coefficients and downgrades on synthetic violations (row `m = 2` filled on a 4-fold -> `n_fold 2`; row `m = 3` filled on a 6-fold -> `2`, row `m = 2` on a 6-fold -> `3`, rows `m = 2` and `3` -> `1`; `(l+m)` odd filled -> `mirror False`) with the warning text; boundary `0.9e-8` passes / `1.1e-8` fails; unknown name raises.

## 4. `MasterPatternHarmonics` (`_master_pattern_harmonics.py`)
1. Private helpers, each with the EMSphInx line refs in its docstring: `_resize_lambert(image, new_dim)` (D5), `_to_legendre(north, south, dim_leg)` (D5+D6, returns `(north_leg, south_leg)`), `_normalize_hemispheres(north, south, emsphinx_compatible) -> (north, south, mean, std)` (D7), `_energy_weights(master_pattern, energy_weights) -> np.ndarray` (D4, incl. the `accum_e` reader `_accum_e_weights(path, energies, ehistmin, ebinsize)`), `_check_atom_sum(master_pattern)` (D3 integer multi-site guard), `_metadata_from_master_pattern(mp, beam_energy, sample_tilt, bandwidth, normalize, emsphinx_compatible)` (D11 dict incl. the provenance `notes`), `_phase_from_sht(sht_file)`, `_sht_from_harmonics(h, doi, notes, preserve_header)` (D11), `_packing_loss(alm, bw, z_rot, flags) -> float` (D11 guard quantity), `_ELEMENT_SYMBOLS` (118), `_master_pattern_dict(h, dim, hemisphere)` (the signal dict shared with the io plugin; `hemisphere` validated against `{"upper", "lower", "both"}`).
2. `class MasterPatternHarmonics` per D1/D2/D15 (`original_metadata` attribute, `save(..., strict=True)`, `describe()` with `a_00`/DC power fraction) with the docstring `Notes` table of quirks (incl. the D7 normalised-correlator statement) and `:cite:` `lenthe2019spherical` (mp2sht/format) -- add `:cite:` keys already in `doc/user/bibliography.bib` only; `Raises` sections list every path incl. the flag-ambiguous point-group trap of D11.
3. `EBSDMasterPattern.get_spherical_harmonics` in `signals/ebsd_master_pattern.py` (next to `get_patterns`), lazy import of `MasterPatternHarmonics` inside the method (keeps `kikuchipy.signals` import cost unchanged), numpydoc `Examples` at `bandwidth=32` on `nickel_ebsd_master_pattern_small(projection="lambert", hemisphere="both")`; the `.save` example writes into `tempfile.mkdtemp()`.
4. `indexing/__init__.pyi` export; `indexing/__init__.py` docstring widened (D18).
5. Tests `tests/test_indexing/test_spherical_master_pattern_harmonics.py` and a `TestGetSphericalHarmonics` class in `tests/test_signals/test_ebsd_master_pattern.py` -- exact assertions in `validation.md`: DCT rescale oracles (constant, single mode, pad and crop); bilinear sampling probes (constant image -> constant; a plane `a*X + b*Y` is reproduced exactly at every Legendre normal since bilinear interpolation is exact for bilinear functions); normalisation (weighted mean 0 / std 1 to 1e-12 with the *same* weights; `emsphinx_compatible=True` reproduces `mean_true*2` and the corner weights `omega/4`; `False` gives corner `omega/2`); energy weights (single energy `[1]`; synthetic `accum_e` written to a temp HDF5 with `Ehistmin`/`Ebinsize` -> weights `== counts/sum`; subset renormalisation; missing file/dataset -> `ValueError`; explicit weights normalised; negative -> `ValueError`; `master_patterns.h5` multi-energy without `accum_e` -> `ValueError`, with explicit weights -> runs at bw 4); the mp2sht parity test (in-package Ni master, the **default** `emsphinx_compatible=True`, `bandwidth=384` under `pytest.warns(UserWarning)`, vs `ni_small_20kv_bw384.sht`: rel-L2 < 1e-6 all and after `remove_dc`, `a_00` abs 1e-8, structural-zero relative power < 1e-20) and its weekly twin on the cached full master; the opt-in `emsphinx_compatible=False` branch tests (`|a_00| < 1e-3`, `power_spectrum().sum()` within 1 % of `4 pi`, global factor recorded); **`normalize=False`** through the public keyword (equals an in-test `analyze` of the un-normalised regridded hemispheres; the `dim_scaled == dim` early return leaves the input unchanged, not doubled); the integer multi-site guard (synthetic 2-site uint8 master with `Natomtypes 2`/`combinesites 0` -> `ValueError` naming `combinesites`; float32 `master_patterns.h5` passes); hemisphere handling (`"upper"` + centrosymmetric == `"both"` with `south = north[::-1, ::-1]` bit-exact; `"lower"` raises; non-centrosymmetric `"upper"` raises); `TypeError` for an `ECPMasterPattern`, `NotImplementedError` for a stereographic master; `NotImplementedError` from `rotate`; `resize` up/down identities; `remove_dc`; `power_spectrum` single harmonic + Parseval on a fine Legendre grid (validation); `describe` substrings incl. `a_00`; `__repr__`; `from_file`/`save` round trips (field equality, CRC equality, `softwareVersion` NUL-padded byte-for-byte, provenance `notes`, `preserve_header=True` byte identity for the two Ni fixtures + one generated + one non-UTF-8 synthetic, `preserve_header=True` on a non-file instance raises, lossy packing raises with `strict=True` / warns and drops with `strict=False`, the guard-agreement test at relative amplitude `1e-6`/`1e-3`, the flag-ambiguous `Phase(point_group="3m")` trap naming 156/157, `overwrite`); `to_master_pattern` (shape/dtype/axes incl. the centred offsets vs the source master's `-201`, `hemisphere` in all three values + invalid, phase, `r > 0.99` vs the upsampled source at `dim = 769`, `dim = 401` crop warning and `r > 0.95`, `get_patterns` NCC > 0.99 at 769); the D9 guard warnings incl. the divisor downgrade; `beam_energy`/`sample_tilt` from metadata vs keyword vs `ValueError`.

## 5. io plugin `emsphinx_master_pattern`
1. Files per D14 (`specification.yaml` with `description:`; `file_reader` docstring with the "Not meant to be used directly; use :func:`~kikuchipy.load`." line and a `Returns` section; `HEMISPHERE = Literal["upper", "lower", "both"]`); `file_reader` builds `MasterPatternHarmonics.from_file(filename)` then `_master_pattern_dict(h, dim, hemisphere)`; docstring states EBSD-only scope and the `NotImplementedError` cases; add the module to the `io/plugins/__init__.py` docstring list.
2. Tests `tests/test_io/test_emsphinx_master_pattern.py`: `kp.load` returns `EBSDMasterPattern`, `projection == "lambert"`, `hemisphere == "both"`, shape `(2, 769, 769)` float64, axes names/offsets (`-384`; and `dim=401` gives `-200` vs the source master's `-201`), `phase.space_group.number == 225`, `phase.point_group.name == "m-3m"`, lattice `0.35236`, one atom `Z 28`, `original_metadata` nodes (`header.beam_energy == pytest.approx(20.1)`, `header.primary_angle == 70`, `harmonics.bandwidth == 384`, `crystals.crystal_0.formula == "Ni"`, `simulations.simulation_0.num_px == 200`, `as_dictionary() == h.original_metadata`), `dim=401` warning + shape, `hemisphere="upper"`/`"lower"` shapes and content, `hemisphere="north"` -> `ValueError` listing the options, `lazy=True` accepted, `_plugin` appears in `kikuchipy.io._io.PLUGINS` with `sht`; a synthetic ECP file (modality 2, written with the codec) -> `NotImplementedError` naming "ECP"; a two-crystal synthetic file -> `NotImplementedError`; `.plot()` smoke test (matplotlib Agg) and `get_patterns` smoke test.

## 6. Data registration, docs, CHANGELOG
1. `_registry.py` entries for the 2 shipped files (task 2.3); `doc/user/related_projects.rst` sentence; CHANGELOG entries (D18, three one-sentence bullets); `pyproject.toml` `--ignore-glob`; `.pre-commit-config.yaml` regexes (task 0.4); `doc/user/bibliography.bib` unchanged (Lenthe 2019 exists).
2. `uv run sphinx-build -b html doc doc/_build/html` renders `kikuchipy.indexing.MasterPatternHarmonics` and `kikuchipy.io.plugins.emsphinx_master_pattern`.

## 7. Adversarial review and fixes
1. Ultracode workflow (Opus 5): (a) fidelity reviewer refutes `_master_pattern_harmonics.py` line by line against `master.hpp:242-640`, `image.hpp:513-618`, `mp2sht.cpp:53-130` (DCT normalisation and pad/crop corner, `0.5/nDm^2`, `dim_scaled = round(sqrt 2 * dim_leg)`, `sphere_to_square` on the *north* normals for both hemispheres, `floor` + `min` clamps, row/column order, `omega[ring]`, four border halvings incl. corners, `totW` vs `2 totW`, `2 totW` in the std, `analyze(bw)`, no DC removal, `accum_e` last axis, `:319` bin-count check) and `_sht_file.py` against `sht_file.in.hpp` (offsets, `int8` vs `uint8` fields, string padding, CRC seed/inversion, pack row types incl. `m % (2n)`, LUT transcription, `sanityCheck` subset), by re-running the parity and byte-identity tests plus a compiled-C++ cross-check where cheap; (b) kikuchipy-conventions reviewer (headers incl. the BSD block, numpydoc, lazy imports, keyword-only args, `workers=1`, plugin files, registry md5s, CHANGELOG wording, no `pooch` import at module scope, `-n 4` cleanliness); (c) test-quality reviewer with bug injection (at least: dropped `0.5/nDm^2`, `idctn` instead of `dctn(type=3)`, `dim_scaled` from `dim` instead of `dim_leg`, `ceil` instead of `floor`, transposed X/Y in the bilinear step, corner halved once under `emsphinx_compatible=True`, `2 totW` mean under `True`, missing energy renormalisation, `accum_e` first axis, `pack` real/imag swapped for `mirX`, `m % n` vs `m % 2n`, CRC not inverted at one end, `LUT[192]`/`LUT[200]` swapped, string padding off by 8, `softwareVersion` space-padded, `zRot` from the point-group table, missing losslessness guard, guard tolerance applied to amplitude instead of power, divisor downgrade skipped (`6 -> 1`), `to_master_pattern` swapping hemispheres, `hemisphere="lower"` returning north, offsets `-sy // 2`, `Legendre` instead of `Lambert` synthesis, `numbered` metadata nodes replaced by lists, raw string bytes re-encoded on `preserve_header=True`) -- each must be killed by the suite.
2. Fix, re-run, `uv run pre-commit run --files <changed files>` (incl. `pyproject.toml`, `.pre-commit-config.yaml`).

## 8. Commit and PR
1. Signed commits (spec + constitution amendments + repo config; failing tests; shipped fixtures + generation script + `_dummy_files` generator; codec; symmetry; harmonics; signal method + export; io plugin; docs + CHANGELOG; review fixes); tick the Phase 2 boxes in `specs/roadmap.md`; push; PR into fork `develop` with the template, stating GPL-only licensing for the EMSphInx-derived modules and that `_sht_file.py` is SHTfile-derived and shipped BSD-3-Clause.

## 9. Open questions for the user (decisions frozen above; say so if you want them changed)
1. `_sht_file.py` ships BSD-3-Clause (D17; needs the pre-commit regex amendment and the mission/tech-stack wording). Alternative: keep it GPL like the rest of `_spherical/` and drop the "no relicensing claim" sentence.
2. **Decided by the user (2026-08-16): default `emsphinx_compatible=True`** (parity-first, every existing `.sht` and `IndexEBSD` use it) with `False` as the opt-in corrected normalisation (D7, D18, D19.3 updated); the Phase 4 normalised-correlator gate and the `notes` provenance are kept, no `save()` warning. (The draft proposed default `False`.)
3. `save(strict=True)` raises on lossy packing; `strict=False` warns and drops as EMSphInx does. Alternative (critic 2): warn-and-drop by default.
4. Whether Phase 11 adds `kp.data` accessors for the two shipped `.sht` files (tests use the private `Dataset(...)` API meanwhile) and the `.sht` row of the supported-formats table in `load_save_data.ipynb` (that notebook currently carries your uncommitted edits, so it stays untouched here).
