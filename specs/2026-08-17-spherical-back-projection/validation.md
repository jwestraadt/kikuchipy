# Phase 5 -- `spherical-back-projection`: validation

## Automated (default suite; run from Git Bash)

```
uv run pytest tests/test_indexing/test_spherical_back_projection.py tests/test_indexing/test_spherical_preprocessing.py -n 0   # first run: warm the numba cache in one process
uv run pytest tests/test_indexing/test_spherical_back_projection.py tests/test_indexing/test_spherical_preprocessing.py -n 4
uv run pytest --doctest-modules src/kikuchipy/indexing/_spherical
uv run pytest --cov=kikuchipy.indexing._spherical --cov-report=term-missing tests/test_indexing/test_spherical_back_projection.py tests/test_indexing/test_spherical_preprocessing.py
uv run pre-commit run --files src/kikuchipy/indexing/_spherical/_back_projection.py src/kikuchipy/indexing/_spherical/_preprocessing.py src/kikuchipy/indexing/_spherical/_euler.py src/kikuchipy/indexing/_spherical/_xcorr.py src/kikuchipy/indexing/_spherical/__init__.py tests/test_indexing/test_spherical_back_projection.py tests/test_indexing/test_spherical_preprocessing.py
```

`eps` below is `numpy.finfo(float).eps = 2.220446049250313e-16`; "the Ni detector" is `kp.data.nickel_ebsd_small().detector.deepcopy()` with `pc = pc_average` (`(60, 60)`, `pc = (0.42513885, 0.21336699, 0.50070692)`, `sample_tilt` 70, `tilt` 0, `binning` 8, `px_size` 1); "the Ni master" is `MasterPatternHarmonics.from_master_pattern(nickel_ebsd_master_pattern_small(projection="lambert", hemisphere="both"), bandwidth=bw)` (default `emsphinx_compatible=True`, flags `(4, True)`); "misorientation" is `Orientation(a.data, Oh).angle_with(Orientation(b.data, Oh), degrees=True)` with `Oh = orix.quaternion.symmetry.Oh`; `dim = _grid.default_dim(bw, "legendre")` (55 / 65 / 71 / 91 / 115 at `bw` 53 / 63 / 68 / 88 / 113); "the resampled shape" is `(h_out, w_out)` of `requirements.md` D3. Every "measured" value comes from the drafting probe on top of the merged Phase 1-4 modules, recorded at the end of this file; assertion tolerances are measured-then-pinned with the stated margins. Random data uses `np.random.default_rng(seed)`; nothing depends on test order. Bitwise assertions appear only where both sides consume byte-identical inputs through the same code path (the constant-pattern mask, `out=` reuse, the `.py_func` of the pure-arithmetic gather kernel is recorded bitwise and asserted at `4 eps`); everything crossing a library call or the compiled/interpreted boundary is pinned at a few `eps` or at the measured tolerance.

Required assertions (each is a named test):

Construction and guards (D1)
- `SphericalBackProjector(det, 68)` on the Ni detector: `bandwidth == 68`, `dim == 71`, `sht.dim == 71 and sht.bandwidth == 68 and sht.layout == "legendre"`, `rescaled_shape == (53, 53)` (`circular_mask=False`) and `(49, 49)` with `circular_mask=True`, `n_points == 1317` / `1117`, `sphere_index.dtype == np.int64`, `pixel_index.shape == (n_points, 4)`, `weights.shape == (n_points, 4)` with every row summing to `1` to `1e-12` and all entries in `[0, 1]`, `solid_angles.shape == (n_points,)`, `window_fraction == pytest.approx(0.145851, rel=1e-4)` / `0.123997`, `solid_angle_fraction == pytest.approx(0.145711, rel=1e-4)` / `0.124350`, `scale_factor == pytest.approx(0.629873, rel=1e-4)` / `0.581873`, `sphere_solid_angle == 2 dim^2 - 4 (dim - 1)` to `1e-9` (`9802`), `window_solid_angle == solid_angles.sum()`; **eager, shareable**: `"window_harmonics" in vars(projector)` (or the equivalent for the chosen attribute storage) and `projector.sht._quadrature_weights is not None` immediately after construction, and a second access returns the same object (`is`); `repr` contains `"bw = 68"`, `"dim = 71"`, `"(60, 60) -> (53, 53)"`, `"1317 points"`.
- `bandwidth < 1` -> `ValueError`; `oversampling <= 0` -> `ValueError`; a non-`EBSDDetector` -> `TypeError`; the raw `nickel_ebsd_small().detector` (navigation shape `(3, 3)`, size 9) -> `ValueError` whose message contains `"pc_average"`, `"9"` and `"deepcopy"` and does **not** contain `"index"` (`EBSDDetector` is not subscriptable); **`EBSDDetector((60, 60), pc=np.full((1, 1, 3), 0.5), sample_tilt=70)`** (navigation shape `(1, 1)`, size 1) is accepted, `projector.detector.navigation_shape == (1,)` (reshaped on the internal copy) and its LUT equals that of the same detector built from `pc=[0.5, 0.5, 0.5]` bitwise; **empty-window guards**: `EBSDDetector((60, 60), pc=Ni pc, sample_tilt=-40, tilt=0)` -> `ValueError` whose message contains `"solid_angle_fraction"` (measured `rescaled_shape (0, 0)`), a `signal_mask` that is `True` everywhere -> the same `ValueError`, and a `signal_mask` that is `True` everywhere except `[0, 0]` -> `ValueError` whose message contains `"empty window"` (measured `solid_angle_fraction 3.2e-5`, `rescaled_shape (1, 1)`, `n_points 0` -- the second guard, distinct from the first); `det.azimuthal = 5` -> `ValueError` containing `"azimuthal"`, `det.twist = 3` -> `ValueError` containing `"twist"` (each alone; measured: `azimuthal` 5 changes `sample_to_detector`, so the guard is not vacuous); `signal_mask` of shape `(60, 61)` or dtype `int` -> `ValueError`; `dim` even or `< bandwidth + 2` -> `ValueError` (from `SphericalHarmonicTransform`); the constructor's `detector` attribute is a distinct object (`is not det`) and mutating `det.pc` afterwards leaves `projector.detector.pc` and the LUT unchanged.
- **Direction oracle (D2)**: on the Ni detector and on three synthetic detectors (`EBSDDetector((48, 60), pc=(0.55, 0.65, 0.6), sample_tilt=70, tilt=10)`, `EBSDDetector((60, 60), pc=(0.5, 0.5, 0.5), sample_tilt=70, tilt=-30)`, `EBSDDetector((41, 41), pc=(0.3, 0.8, 0.45), sample_tilt=65, tilt=0)`), the module's direction -> pixel chain applied to `_get_direction_cosines_from_detector(det)` returns `(col, row) == (c, r)` of every pixel to `abs 1e-10` (measured `1.6e-14 / 1.4e-14` on the Ni detector and `1.4e-14 / 1.3e-14`, `1.4e-14 / 1.2e-14`, `1.4e-14 / 1.6e-14` on the three synthetic ones) and `in_front` everywhere; the projector's LUT points, mapped *back* through `_get_direction_cosines_for_fixed_pc`'s formula at the continuous `(col, row)` (a test-local reimplementation of the kernel's arithmetic for fractional pixels), reproduce the Legendre normal to `1e-10` (this oracle covers direction -> detector pixel only; the resample map is pinned in the LUT block below); **frame statement**: `det.sample_to_detector.to_matrix()[2] == (sin 20 deg, 0, cos 20 deg)` to `1e-12` at `sample_tilt` 70 / `tilt` 0 (the EMSphInx `alpha` identity), and `Rotation.from_matrix(M).to_matrix() == M` to `1e-14`.
- **Binning-neutrality (D2)**: `EBSDDetector(shape=det.shape, pc=det.pc, sample_tilt=70, tilt=0, binning=1, px_size=8.0)` gives a projector whose `sphere_index`, `pixel_index` are equal and whose `weights` differ by `0.0` from the `binning` 8 / `px_size` 1 one (same arithmetic on the same numbers: bitwise is legitimate); the EMsoft round trip `EBSDDetector(shape, pc=det.pc_emsoft(), convention="emsoft", binning=8, px_size=1.0, sample_tilt=70)` reproduces `pc` to `1e-12` and the same LUT to `1e-12` in the weights.
- **The south hemisphere is never gathered (D4)**: for `sample_tilt`/`tilt` in `{(70, 0), (70, 10), (70, -30), (0, 0), (20, 0)}` (`circular_mask=False`) the north point counts are `1317 / 1375 / 1235 / 524 / 901` (`pytest.approx(., abs=2)` -- the count is an integer property of the geometry, so exact; `abs=2` guards a future change of the equator ring's inclusive test) and `unproject` of a random pattern returns `south` all zeros; the direction-cosine `z` ranges `[0.188, 1.000] / [0.041, 0.991] / [0.579, 1.000] / [-0.841, 0.379] / [-0.605, 0.673]` are recorded (`record_property`) and, for `(0, 0)`, the fraction of pixels with `z < 0` (**measured `0.783`**, re-measured 2026-08-17; the drafting note's `0.55` was wrong) is asserted `> 0.5` while the LUT count is `< 0.6` of the `(70, 0)` count (measured `524 / 1317 = 0.398`, i.e. 60 % of the window lost) -- the clip is real, not a wrap.

Sizes, solid angle, LUT (D3-D4)
- **Size table**: `bw -> (circle, no circle)`: `53 -> (38, 41)`, `63 -> (45, 49)`, `68 -> (49, 53)`, `88 -> (63, 69)`, `113 -> (80, 87)` on the Ni detector (LUT points `667 / 788`, `934 / 1093`, `1117 / 1317`, `1844 / 2157`, `2955 / 3474`) (each `rescaled_shape` square since the detector is); `scale_factor` at `bw` 53 / 68 / 88 with the circle `0.448905 / 0.581873 / 0.748092` to `rel 1e-4`; **rounding**: a detector for which `scale_factor oversampling ncols` lands within `1e-9` of `k + 0.5` (constructed by choosing `oversampling` from the measured `scale_factor` and `ncols`) gives `w_out == k + 1` (`floor(x + 0.5)`; Python `round` would give `k` for even `k`).
- `solid_angle_fraction` on the Ni detector: `0.124350` (circle) / `0.145711` (no circle) to `rel 1e-4`; equals the fraction of the `502 x 502` Lambert grid points that pass a test-local `_inside_detector` (a second implementation using `_grid.square_to_sphere` and the D2 chain) exactly (integer counts **`62175 / 72856` of the literal divisor `500002`**, confirmed 2026-08-17; the consistent two-hemisphere count `502004` would give `0.123854 / 0.145130` -- recorded, so that the quirk stays visible); **window fraction vs analytic**: `|window_fraction / A - 1| < 0.02` with `A = sum_{pixels in window} x_scale y_scale (1 + x_g^2 + y_g^2)^(-3/2) / (4 pi)` (measured `0.123997 / 0.123848 = 1.0012` circle, `0.145851 / 0.145136 = 1.0049` no circle) and `|solid_angle_fraction / window_fraction - 1| < 0.01` (measured `1.0028 / 0.9990` -- a plausibility bound: the two agree because the divisor quirk and the rim inclusion bias them in opposite directions, D3).
- `sphere_solid_angle == 2 dim^2 - 4 (dim - 1)` to `1e-9` at `bw` 53 / 68 / 88 (measured `5834 / 9802 / 16202` exactly); `solid_angles[p] == _grid.ring_solid_angles(dim, "legendre")[_grid.ring_number(dim).ravel()[sphere_index[p]]]` bitwise (same lookup).
- **LUT membership**: every `sphere_index` is a north-grid pixel whose normal maps to `col in [-0.5, 59.5]`, `row in [-0.5, 59.5]`, and (circle) `(col - 29.5)^2 + (row - 29.5)^2 <= 900`; every excluded north pixel with `in_front` fails one of those; `pixel_index` in `[0, h_out w_out)`; `pixel_index[:, 1] - pixel_index[:, 0] in {0, 1}` and `pixel_index[:, 2] - pixel_index[:, 0] in {0, w_out}` (the clamp structure); **rectangular circle pin**: `EBSDDetector((48, 60), pc=(0.55, 0.65, 0.6), sample_tilt=70, tilt=10)` with `circular_mask=True` at `bw` 68 -> `n_points == 953` and `rescaled_shape == (42, 52)` (the physical circle; the C++ rescaled-geometry circle would give 958 -- recorded).
- **Rim structure** (in resampled coordinates, where the clamps act; `x = (col + 0.5) w_out/ncols - 0.5`, `y` likewise, from the reconstructed `(col, row)`): a rim point has `x < 0`, `x > w_out - 1`, `y < 0` or `y > h_out - 1`; (a) every weight row sums to `1` to `1e-12` (measured max deviation `2.2e-16`) and every entry is in `[0, 1]`; (b) for `x < 0`: `weights[:, 1] == 0.0` and `weights[:, 3] == 0.0` exactly and `pixel_index[:, 1] == pixel_index[:, 0] + 1`; for `y < 0`: `weights[:, 2] == weights[:, 3] == 0.0` exactly and `pixel_index[:, 2] == pixel_index[:, 0] + w_out`; (c) for `x > w_out - 1`: `pixel_index[:, 0] == pixel_index[:, 1]` (and `[:, 2] == [:, 3]`); for `y > h_out - 1`: `pixel_index[:, 0] == pixel_index[:, 2]`; for `x < w_out - 1`: `pixel_index[:, 1] == pixel_index[:, 0] + 1`, for `y < h_out - 1`: `[:, 2] == [:, 0] + w_out`; **no weight equals `1.0` anywhere** in the LUT (measured max `0.969` circle / `0.992` no circle at `bw` 68); the rim count is asserted `<= 0.05 n_points` for both `circular_mask` settings at `bw` 68 and recorded: **3 (circle) / 31 (no circle)** in resampled coordinates (left 2 / 10, top 0 / 10, right 1 / 11, bottom 0 / 0), **2 / 24** in detector coordinates (`col` or `row` outside `[0, 59]`); at `bw` 53 / 88 / 113: 6 / 32, 7 / 42, 7 / 52 resampled and 4 / 30, 7 / 44, 22 / 97 detector (recorded).
- **Resample map (structural pin of the DCT sampling convention)**: with `(col, row)` reconstructed for every LUT point (test-local D2 chain on `legendre_normals(dim).reshape(-1, 3)[sphere_index]`), `i0 = clip(floor(x), 0, w_out - 1)`, `j0 = clip(floor(y), 0, h_out - 1)`, `i1 = min(i0 + 1, w_out - 1)`, `j1 = min(j0 + 1, h_out - 1)`, `wx1 = clip(x - i0, 0, 1)`, `wy1 = clip(y - j0, 0, 1)`: `pixel_index == [j0 w_out + i0, j0 w_out + i1, j1 w_out + i0, j1 w_out + i1]` bitwise and `weights == [wy0 wx0, wy0 wx1, wy1 wx0, wy1 wx1]` to `1e-12` (measured `0.0`); **negative control**: with EMSphInx' `x_s = (col + 0.5)/ncols (w_out - 1)` instead, `i0` differs for more than `100` points (measured **295 of 1317**, `j0` 230, either 460; 209 of 1117 with the circle) and more than `0.9 n_points` weight rows differ by more than `1e-6` (measured all 1317; `max |x_s - x| = 0.4999` px) -- so the test discriminates the stretch, which the direction oracle, the D8 lock and a single-pixel argmax test cannot (D2).
- **`signal_mask` exclusion (nearest pixel)**: with `signal_mask[20:32, 25:40] = True`, `n_points` drops `1117 -> 1020` (circle) and every removed point has `floor(row + 0.5) in [20, 32)` and `floor(col + 0.5) in [25, 40)`; every kept point has not; the guard cases (all-`True` mask -> the `solid_angle_fraction` guard; `[0, 0]`-only mask -> "empty window") are in the construction block above; `rescaled_shape` `(49, 49) -> (47, 47)` (the mask enters `solidAngle`).

Resample, IQ, `unproject` (D5-D6)
- **`dctn` convention regression (first, the roadmap's test box)**: for a seeded standard-normal `x` of shape `(60, 60)`, `dctn(dctn(x, type=2, workers=1), type=3, workers=1) / (4 h w)` equals `x` to `abs 1e-12` (measured `max |.| = 1.6e-15`; the ratio itself spans `14399.999999996-14400.000000018` because it divides by near-zero pixels, so the difference, not the ratio, is asserted); the constant `37.0` padded to the `53 x 53` corner with the DC *kept* gives the constant `532800 = 37 x 4 x 60 x 60` to `rel 1e-12` through `dctn(., type=3)`; **negative control**: `scipy.fft.idctn(padded, type=3, workers=1)` on the same input has `np.ptp > 1` (measured `189.5`) and `wrong.max() / correct.max() == pytest.approx(1 / 53**2, rel=1e-6)` (measured exactly `1/53^2`, the Phase 2 finding), so a swap to `idctn` or a `norm=` drift is caught numerically.
- **Constant pattern -> mask** (the pocketfft finding): `unproject(np.full((60, 60), 7.0))` and `unproject(np.full((60, 60), 7, np.uint8))` return `north` bitwise equal to `window_mask()[0]` (ones on the LUT points, zeros elsewhere), `south` all zeros, and `return_image_quality=True` gives `iq == 1.0`; **`unproject(np.zeros((60, 60)), return_image_quality=True)`** returns the same mask with **`iq == 0.0`** (the `sumP == 0` value of `imageQuality`; the literal chain gives the mask too: all-zero DCT, all-zero resampled image, `stdev == 0`); a call-recording spy on `_back_projection.dctn` records **no call** for these inputs (the `ptp` short-cut) and exactly two calls (`type=2` then `type=3`, `workers == 1` on both, no `norm` kwarg) for a random pattern; **the literal `stdev == 0.0` branch of the kernel** (`unproject` never returns the stdev, so these drive `_unproject_kernel` directly with the projector's LUT arrays): the all-zero resampled image `np.zeros(h_out * w_out)` writes exactly `1.0` on `sphere_index`, leaves the rest of `north` untouched and returns exactly `0.0` -- **the only constant that reaches the branch through the real LUT** (measured `stdev` for `np.full(., v)`: `3.5 -> 7.5e-15`, `1.0 -> 1.1e-15`, `7.0 -> 1.5e-14`, `100.0 -> 6.3e-13`, `2.0 -> 2.2e-15`, `0.0 -> 0.0`; 18 of 1117 weight rows sum to `1 +- 1 ulp`; an earlier draft asserted the branch for `3.5`, which no faithful kernel satisfies, and a `stdev < tiny` "fix" would break fidelity to `if(Real(0) == stdev)`, `:607`), recorded as such; **and the tiny-amplitude control**: a seeded standard-normal image scaled by `1e-14` must be *normalised*, not masked -- returned `stdev > 0` (measured `6.7e-15`), weighted mean `<= 1e-10` (measured `3.8e-17`) and weighted second moment `1` to `1e-10` (measured `1 - 2e-16`), so any `<=`/`< tiny` relaxation of the branch dies; the pocketfft measurement itself is pinned as a *guard*: `max |dctn(np.full((60, 60), 37.0), type=2)[AC]| > 1e-13` (measured `1.1e-11`) so that a future exact DCT does not silently make the short-cut redundant without notice (recorded value; if it ever becomes `0` the test still passes because the short-cut is asserted separately -- the guard's failure would only tell the reviewer the note is stale, so it is `record_property`, not an assertion).
- **Normalisation identities**: for the nine Ni patterns and five seeded random patterns, `sum(north[sphere_index] solid_angles) / window_solid_angle` is `<= 1e-10` in magnitude and `sum(north[sphere_index]^2 solid_angles) / window_solid_angle == 1` to `1e-10` (measured `-9.6e-16`, `1 - 9e-16`); the `stdev` returned by `_unproject_kernel` on the `_dct_rescale` output is `> 0` (measured `4.9e5`; the kernel is called directly since `unproject` returns `(north, south[, iq])`); `north` is exactly `0` off the LUT and `south` exactly `0`; **weights matter**: recomputing the identity with unit weights instead of `solid_angles` violates it by more than `1e-3` (the pattern is not constant, so a wrong-weight port is caught).
- `out=`: passing `(north, south)` buffers writes into them (`is` identity), only the LUT points of `north` change (a pre-filled `north = 5.0` keeps `5.0` off the window -- the documented C++ contract), `south` is untouched (its stale values are the caller's problem, documented); wrong shape/dtype/non-contiguous `out` -> `ValueError`; a `pattern` of the wrong shape -> `ValueError`; a `pattern` with NaN is not guarded (documented; a test asserts NaN propagates and no exception).
- `image_quality(pattern) == unproject(pattern, return_image_quality=True)[2]` bitwise for the nine Ni patterns (same float64 input, same spectrum) **and for `np.full((60, 60), 7.0)`, the `uint8` constant 7 and `np.zeros((60, 60))`** (the shared `ptp` short-cut: `1.0`, `1.0`, `0.0`) **and** equals a test-local literal transcription of `imageQuality` (`:489-507`) on `dctn(pattern, type=2)` of the *input* size to `rel 1e-12` on the Ni patterns and to `abs 1e-12` on the constants (measured: the literal value is exactly `1.0` for the constants `1.0 / 7.0 / 37.0 / 200.0` and `0.0` for zeros); the nine values are in `[0.7, 0.85]` (measured `0.7663-0.7788`, recorded); `iq(3 p) - iq(p)` `<= 1e-12` (measured `1.1e-16`), `iq(p + 100) != iq(p)` (measured `+0.056`, recorded), uniform noise `< 0.2` (measured `0.051`, recorded); `_dct_image_quality` is `_back_projection._dct_image_quality` and `not hasattr(_preprocessing, "dctn")`.
- **Harmonic recovery** (`pytest.importorskip("scipy", minversion="1.15")` for `sph_harm_y`): for `(l, m) in {(2, 1), (3, 2), (4, 0), (5, 3), (6, 4), (8, 2)}` the pattern `Re Y_l^m(dc)` on the Ni detector (circle), unprojected at `bw` 68: the windowed correlation between `north[sphere_index]` and the weighted zero-mean/unit-std `Re Y_l^m(normals[sphere_index])` is `> 0.999` (measured `0.999999 / 0.999999 / 0.999999 / 0.999999 / 0.999998 / 0.999991`), the weighted rms difference `< 0.01` (measured `0.0011-0.0042`), and the correlation between `sht.analyze(north, south)` and `sht.analyze(truth on the window)` (real and imaginary parts concatenated) `> 0.999` (measured `>= 0.999993`); `(12, 5)` recorded (`0.999964`, rms `0.0085`). **Mean fill (D5)**: with `signal_mask[20:32, 25:40] = True` and the masked block of `100 Re Y_4^2 + 50` set to `255`, the windowed correlation is `> 0.99` (measured `0.9965` with the fill; without it `0.9323` -- a test-local no-fill variant is asserted `< 0.95` so that the fill's effect is visible), and with the block set to `0` `> 0.99` (measured `0.9965`, `0.9812` without).
- **`window_harmonics`/`squared_harmonics` (D7)**: `mlm = window_harmonics` is `(68, 68)` complex128 with `mlm[0, 0].real sqrt(4 pi) / (4 pi) == window_fraction` to `rel 1e-3` (measured `0.123965 / 0.123997` circle, `0.145820 / 0.145851` no circle), computed at construction (`is` identity on repeated access; present before any call, D1), equal to `sht.analyze(*window_mask())` bitwise; `squared_harmonics(alm)` equals `sht.analyze(n**2, s**2)` of `sht.synthesize(alm)` bitwise, `ValueError` for `alm` of another bandwidth; **`signal_mask` changes `rDen`**: `NormalizedSphericalCrossCorrelator(68, flm, flm2, 4, True, mlm)` vs the same with the masked projector's `mlm` (and its own `flm2`, identical since `flm2` does not depend on the mask): `max |rDen_m - rDen| / rDen > 0.01` (measured `0.108`), both finite and positive everywhere, and **pinned**: `r_den.min() == pytest.approx(1.785, rel=0.05)`, `r_den.max() == pytest.approx(2.150, rel=0.05)` for the unmasked circle projector at `bw` 68, and `1.660 / 1.951` for the default (`circular_mask=False`) projector (re-measured 2026-08-17: `1.7852 / 2.1503`, `1.6599 / 1.9508`; the constitution's measured-then-pinned rule).

Forward-projection convention lock (D8)
- **27 rotations at `bw` 68** (`circular_mask=False`): with `Rotation.from_euler` of the Bunge grid `{20, 140, 260} x {30, 80, 130} x {10, 130, 250}` deg, `sim = mp.get_patterns(R, det, energy=20, compute=True)`, for every pattern `unproject -> sht.analyze -> correlate` with `NormalizedSphericalCrossCorrelator(68, flm, projector.squared_harmonics(flm), 4, True, projector.window_harmonics)` and with `SphericalCrossCorrelator(68).correlate(flm, gln, 4, True)`: **the misorientation of `_euler.rotation_from_zyz(zyz)` (= `~Rotation(zyz_to_quaternion(zyz))`) to `R` is `< 2` deg for all 27 and both correlators** (measured normalised median 0.344 / max 0.717, un-normalised 0.328 / 0.536; circle: 0.424 / 0.727 and 0.371 / 0.574), the median `< 1` deg, **and the other sign `Rotation(zyz_to_quaternion(zyz))` has median `> 10` deg** (measured 35.0 / 35.5); the medians and maxima are `record_property`, and **the scores are pinned**: over the 27 rotations `min(score) == pytest.approx(1.087, rel=0.05)` and `max == approx(1.143, rel=0.05)` for the normalised correlator, `0.594 / 0.652` for the un-normalised one (measured; the constitution's measured-then-pinned rule -- a `flm2`/`mlm` scale defect that leaves the argmax intact is otherwise invisible); weekly the same at `bw` 53 (measured `~R` 0.512 / 1.040 normalised, 0.591 / 1.040 un-normalised; `R` 35.3 / 60.2) with the same bounds.
- **Asymmetric blob** (row/column/flip): a zero `(60, 60)` pattern with `100` on the `3 x 3` block about `(r0, c0) in {(10, 45), (50, 5)}` (both inside the circle; the test uses `circular_mask=False` anyway), unprojected with the Ni detector at `bw` 68 with `circular_mask=False`: the angle between the Legendre normal of `argmax(north)` and `dc[r0, c0]` is `< 2 x 180/dim` deg (`5.07`; measured **`1.05 / 1.13`** at the default -- the drafting values `1.05 / 1.51` were the circle case), and `> 20` deg to each of `dc[59 - r0, c0]` (vertical flip; measured `49.4 / 50.4`), `dc[r0, 59 - c0]` (horizontal; `52.6 / 53.9`) and `dc[c0, r0]` (transpose; `71.4 / 85.9`); repeated with `EBSDDetector((60, 60), pc=(0.55, 0.65, 0.6), sample_tilt=70)` (PC below centre) at `(15, 40)` and `(45, 12)`: measured `1.14 / 0.34` true, `41.7 / 39.4` vertical, `28.2 / 50.6` horizontal, `50.9 / 63.2` transposed (same bounds).

Real data (`nickel_ebsd_small`, D6/D12)
- All nine patterns (raw `uint8`, no preprocessing) at `bw` 68 with the circle: `south` all zero, `north` non-zero on exactly `n_points == 1117` pixels (every LUT point receives a finite value; measured 1117 of 1117), the identities above, `iq` in `[0.7, 0.85]` (measured `0.7663 / 0.7734 / 0.7726 / 0.7667 / 0.7769 / 0.7709 / 0.7680 / 0.7788 / 0.7732`), timing per pattern recorded.
- **Mean-PC error floor** (default suite; `remove_static_background()` + `remove_dynamic_background()` first, `mp = nickel_ebsd_master_pattern_small(..., energy=20)`, `refine_orientation(xmap=s.xmap, detector=., master_pattern=mp, energy=20, method="LN_NELDERMEAD", trust_region=[2, 2, 2], rtol=1e-6)`): (a) per-point detector -> misorientation to the stored `xmap` median `< 0.2` and max `< 0.5` deg (control; measured 0.052 / 0.188); (b) `pc_average` -> vs stored median `< 0.6`, max `< 1.0` (measured 0.296 / 0.557); (b) vs (a): median and max recorded (measured 0.333 / 0.542) and asserted `< 1.0`; the PC spread `max |pc - pc_average|` recorded (`(0.0057, 0.0032, 0.0013)`). The method is `"LN_NELDERMEAD"` when `kikuchipy._constants.dependency_version["nlopt"] is not None`, else `"minimize"` (`pytest.importorskip` cannot express a fallback); which one ran is `record_property` and the bounds hold for both (measured 0.296 / 0.557 vs 0.296 / 0.558). **Default suite, `nickel_ebsd_large(allow_download=True)`** (skips without the `tests` extra; the constitution's "subset in the default suite, full map weekly"): `navigation_mask` keeping every 15th row and column (**20 points**): (b) vs (a) median `< 0.6`, max `< 1.5` deg (measured 2026-08-17: median 0.283 / p90 0.753 / p95 0.762 / max 0.823), (a) vs stored median `< 0.2` (measured 0.038 / max 0.174), (b) vs stored recorded (0.295 / p95 0.815 / max 0.843), the correlation with `|pc - pc_average|` recorded (0.958; `scipy` Nelder-Mead 0.282 / 0.761 / 0.810); measured cost load 1.8 s + backgrounds 0.7 s + 2 x 0.3 s. **Weekly**: every 5th row and column (165 points): (b) vs (a) median `< 0.6`, p95 `< 1.2`, max `< 1.5` deg (measured 0.291 / 0.735 / 0.964), (b) vs stored recorded (0.302 / 0.521 p90 / 1.075), the Pearson correlation between the misorientation and `|pc - pc_average|` `> 0.8` (measured 0.97), all values `record_property`.

Preprocessing (D9)
- `_circular_mask((60, 60))` has `2819` `True` pixels and equals `kp.filters.Window("circular", (60, 60)).astype(bool)`; likewise `(61, 61)` (`2821`) and `(7, 7)` (`29`); `_circular_mask((48, 60))` has `1792` and differs from `Window` (`2531`; recorded); `radius=10` on `(60, 60)`: centre `(30, 30)`, count `317` (`sum((i-30)^2 + (j-30)^2 <= 100)`).
- **1-D Gaussian fit** on `x = 0..57`: `(a, b, c) = (25.3, 288, 150)` and `(40.7, 60.5, 90)` recovered to `1e-9` with `r2 == 1` to `1e-12` and `converged` (measured exact, 7 iterations); with `+ 0.5 sin x`: `|a - a0| < 1e-3`, `|b - b0| / b0 < 1e-3` (measured `3e-5`, `2e-4`); with `+ 3 sin(x/2) + 5`: `|a - a0| < 0.05` (measured `0.012`) and `r2 > 0.97` (measured `0.978-0.997`); **the integer-mean quirk**: `(30, 128, 200)` returns `converged == False` (the `ss = 0` NaN path; asserted so that nobody "fixes" the stopping rule) and `_gaussian_background` of the corresponding separable image therefore returns the flat fallback on that axis (`b == inf`); **flat input** `np.full(58, 5.0)` (and `np.full(58, 0.0)`): `converged == False` **without raising**, kernel status `!= 0`, and `_gaussian_background` of the corresponding flat image takes the fallback (`b == inf`, background `== mean`) -- the `0/0` in `estimate` (`error_model="numpy"`); the `.py_func` under `np.errstate(divide="ignore", invalid="ignore", over="ignore")` also returns without raising with a non-zero status (NumPy-scalar accumulators, D9); **the status values are `record_property`, not asserted** (measured with the scratch kernel: compiled **3** -- all-NaN `jTj` with equal sign bits passes the C++-direction `cholesky` and all 50 iterations run -- and `.py_func` **2**, because NumPy's negation in `dfda = -fxb * 2` flips the sign bit of the NaN and the sign test fires; the C++ itself lands on either "failed to converged" or "cholesky ..." by the same platform-dependent NaN sign propagation, and `BckgSub2D::fit` catches both); **`_cholesky_solve_3x3` NaN semantics and direction** (deterministic, compiled and `.py_func`): `np.full((3, 3), np.nan)` -> status `0` with a NaN solution (the C++ comparisons are false for NaN; the defensive `not (pivot >= eps)` would give 2), the same with `a[1, 1] = -np.nan` -> status `1` (sign), `np.diag([1e-17, 1, 1])` -> status `2` (`1e-17 < eps`; a `< 0` mutant proceeds), `[[1, 1, 0], [1, 1, 0], [0, 0, 1]]` -> `2` (exact zero pivot), and the negative-definite `-[[4, 2, 0], [2, 3, 1], [0, 1, 2]]` with `b = [1, 2, 3]` -> `0` and `A x == b` to `1e-12` (the `neg` path; measured `x = (-0.25, 1.6e-16, -1.5)`, residual `4.4e-16`); a ramp `1..58`: converged, `a > 57` (measured `62.7`, recorded); `n < 3` -> status 1 (`ValueError` from the Python wrapper).
- **2-D background**: the separable Gaussian `180 exp(-(i - 32.4)^2/800) exp(-(j - 27.9)^2/648)` on the `(60, 60)` circle: `emsphinx_compatible=True` -> `gx.a == 31.4` and `gy.a == 26.9` to `1e-3` (the off-by-one) and `max |background - truth|` over the good pixels `> 5` (measured `8.16`); `False` -> `< 0.01` (measured `0.0028`); with `+ 10 cos(i/3) cos(j/4) + 20` the two errors are recorded (`33.4 / 25.7`); a flat pattern -> `background == mean` everywhere and `_remove_gaussian_background == 0`; masked pixels -> `0` after subtraction; **`[0, 0]` initialisation**: a pattern whose maximum `255` sits at `[0, 0]` under a mask excluding `[0, 0]` still has `row_max[0] == 255` and `col_max[0] == 255` (faithful `:259-260`); the real Ni pattern 0 parameters `gx = (23.87, 1101.8, 232.8)`, `gy = (25.81, 1485.0, 233.8)` to `rel 1e-3` (recorded, `pytest.approx(rel=0.05)` asserted).
- **Mosaic AHE**: `n_regions` 10 on `(60, 60)`: tiles `(0, 6), (6, 12), ..., (54, 60)` on both axes and midpoints `3, 9, ..., 57`; `n_regions` 7: tiles `[0, 9), [9, 17), [17, 26), [26, 34), [34, 43), [43, 51), [51, 60)` and midpoints `4, 13, 21, 30, 39, 47, 56`; the interpolation pair of pixel `3` is `(l, u, c, f) = (0, 1, 1.0, 0.0)` and of pixel `0` `(0, 0, 0.5, 0.5)`, of pixel `59` `(9, 9, 0.5, 0.5)`; **uniform** `100` -> every output `255.0`; the ramp `np.tile(np.arange(60, dtype=np.uint8) * 4, (60, 1))` -> `min 42.5`, `max 255` to `1e-9`; **all-masked tile identity**: with `good = zeros` except `[:6, :6]`, the block `[30:36, 30:36]` equals `(v + 1) 255/256` to `1e-10` (measured `2.8e-14`); **masked-out pixels are equalised**: with the circle mask the corner pixels are non-zero and differ from the unmasked result (measured up to `164.6` levels; asserted `max diff > 50`); **`skimage` equivalence**: `max |_mosaic_ahe(p, 10) - 255 equalize_adapthist(p, kernel_size=(6, 6), clip_limit=0, nbins=256)| < 8` on Ni patterns 0, 4, 8 (measured `5.90 / 4.72 / 5.90`, mean `2.4-3.0`) and the correlation `> 0.99999`; `n_regions` 4 vs `kernel_size=(15, 15)`: `< 2` (measured `1.13`); `n_regions` 7 vs `kernel_size=(60/7, 60/7)`: recorded (`70.2`, correlation `0.924`); **kikuchipy**: `EBSD.adaptive_histogram_equalization(kernel_size=(6, 6), clip_limit=0, nbins=256)` on `nickel_ebsd_small` vs `_mosaic_ahe(p, 10)` min-max rescaled to `[0, 255]`: `< 2` gray levels (measured `1.02`); `n_regions > min(shape)` or `< 0` -> `ValueError`.
- `_to_uint8`: `[0.0, 1.0, 6.0]` -> `[0, 43, 255]` (`factor = 255/6 = 42.5`, `1 -> 42.5 + 0.5 = 43.0`, truncated to `43`; banker's `round(42.5)` would give `42`, so this input separates the two rules), `[0.0, 3.0, 6.0]` -> `[0, 128, 255]`, `[10, 10.5, 12]` (float32 input) -> `[0, 64, 255]`; the output dtype is `uint8`; a flat buffer -> zeros without a warning (the C++ UB case, documented); `kp.pattern.rescale_intensity(np.array([0.0, 1.0, 6.0]), dtype_out=np.uint8)` gives `[0, 42, 255]` (`astype` truncation), i.e. `!= _to_uint8` -- recorded so the mapping-table row stays true.
- **Process order**: spies (`monkeypatch` on `_preprocessing._gaussian_background` and `_mosaic_ahe`) record the call order `background` then `ahe` for `gaussian_background=True, n_regions=10`, only `ahe` for `False, 10`, only `background` for `True, 0`, none for `False, 0` (which returns `pattern.astype(np.float64)`); a `uint8` input with `n_regions=10` reaches `_mosaic_ahe` **unrescaled** (the spy sees the input array's values) while the same values as `float32` are passed through `_to_uint8` first (the spy sees a min-max rescaled image, which differs unless min/max are `0`/`255`); the defaults of `_preprocess_pattern` are `good_pixels=None, gaussian_background=False, n_regions=10, emsphinx_compatible=True` (asserted via `inspect.signature`); output dtype `float64`, shape preserved; the input is never modified (a copy is compared).

Kernels and conventions (D10)
- Every `@njit` kernel of both modules (`KERNEL_NAMES` asserted equal to the modules' `CPUDispatcher` members) has a `py_func` (`hasattr` guard) whose result equals the compiled one: `_unproject_kernel` to `abs 4 eps` on the Ni pattern (measured `0.0`), the fit kernel's `(a, b, c)` to `rel 1e-9` and the same status on the synthetic cases (under `np.errstate` for the flat case), the AHE kernels and `_to_uint8_kernel` bitwise (integer/lookup arithmetic), `_row_col_max_kernel` bitwise; flags: `cache=True`, `nogil=True`, no `parallel`, no `fastmath` on all, `error_model == "numpy"` on `_fit_gaussian_1d_kernel` **only** (`targetoptions.get("error_model")` is `None` on every other kernel of both modules incl. `_cholesky_solve_3x3` -- and, regression, on Phase 4's kernels other than `_interpolate_maxima`); a DCT-call recording test asserts `workers == 1` and `type` 2/3 on `_back_projection.dctn` (patched in the module namespace and in `scipy.fft`), and **`_preprocessing` has no FFT**: `not hasattr(_preprocessing, "dctn")` and `"scipy.fft" not in inspect.getsource(_preprocessing)`, so the one recording test covers every `scipy.fft` call of the phase.
- Timing/memory baselines via `record_property` (informational): `unproject` per pattern (best of 100 after warm-up) at `bw` 53 / 68 / 88 (113 weekly), `sht.analyze` at the same, construction time, `tracemalloc` peak and resident of the construction, `_preprocess_pattern` per pattern for the four branches; loose bounds: `unproject < 50 ms`, construction `< 5 s`, resident LUT `< 5 MB` at `bw <= 113`.

## Weekly
- `uv run pytest --weekly tests/test_indexing/test_spherical_back_projection.py -n 4`: the D8 lock at `bw` 53 (27 rotations, both correlators, both signs), the 165-point `nickel_ebsd_large` error-floor subset (165 refinements x 2, `~2 s`; the 20-point subset is in the default suite), the memory row at `bw` 113, and the harmonic recovery up to `(12, 5)`.

## Manual
- Headers: `_back_projection.py` and `_preprocessing.py` carry the kikuchipy GPL header and the delimited EMSphInx notice (CMU/Lenthe, GPL-2.0-or-later conveyed under GPL-3.0-or-later, "changed by Johan Westraadt, 2026-08") listing the ported functions with line ranges (`detector.hpp` incl. the `:424` size guard, `util/image.hpp`; `util/gaussian.hpp`, `util/ahe.hpp`, `modality/ebsd/imprc.hpp`, `util/linalg.hpp` `solve::cholesky` `:354-358` / `decompose::cholesky` `:411-431` / `backsolve::cholesky` `:487-493`), the replaced `interpolatePixel` and the not-ported list (incl. `BackProjector::clone`); the two test modules carry the kikuchipy header only.
- `src/kikuchipy/indexing/_spherical/__init__.py` lists ``_back_projection`` and ``_preprocessing`` in its `Submodules` block and still imports nothing; no new public names in `indexing/__init__.pyi`; no CHANGELOG entry; `_euler.rotation_from_zyz`/`rotation_to_zyz` docstrings **and the `_xcorr.py` module docstring (line 150, "provisional until Phase 5")** no longer say "provisional" and cite Phase 5 D8; `_preprocessing.py` imports nothing from `scipy.fft`.
- Docstrings freeze: the D2 chain and the frame statement (and that the oracle does not cover the resample map, which is pinned structurally), the pixel-centre convention (EMSphInx' stretch not reproduced, measured 0.49 px), the physical guard and the always-zero south, the D3 arithmetic with the Ni size table and the `solidAngle` divisor quirk, the rim clamp with the 1.8-2.8 % of the default window in the physical rim, the physical circle vs the C++'s rescaled circle (5 of 958 points on `(48, 60)`), the empty-window guards, the pocketfft constant-DCT finding and the `ptp` short-cut with its IQ values (`1.0` / `0.0`), the `signal_mask` polarity/nearest-pixel/mean-fill semantics, `circular_mask=False` default and the `circmask` mapping, the D6 normalisation and the `stdev == 0` branch (reachable only from an all-zero window), `rDen` ownership and the `s2m` binary-mask remark, immutability and thread sharing (no `clone()`), the D8 lock numbers, the D11 numbers; `_preprocessing`: the process order and defaults, the off-by-one behind `emsphinx_compatible`, the stopping-rule quirk, the `cholesky` NaN semantics (C++ direction; the flat-input status is platform-dependent), the fallback triple, the AHE facts (uniform -> 255, masked pixels equalised, all-masked tile identity, CLAHE equivalence), the two circles, the kikuchipy mapping table incl. `_to_uint8` vs `rescale_intensity`; numpydoc, types in signatures only, comment lines `<= 72` chars, three import blocks.
- Coverage of `_back_projection.py` and `_preprocessing.py` `>= 95 %` (kernels via `.py_func`; no sanctioned exclusion).
- Adversarial review findings addressed or explicitly deferred with reason; the bug-injection list of `plan.md` 4.1 fully killed.
- Known limitations stated: one PC per call; `azimuthal`/`twist` raise; footprints below the sample plane are clipped (and an empty footprint or an all-masked detector raises); the projector's circle is the physical one (EMSphInx' LUT circle differs by rounding on rectangular detectors); the resample is a global DCT (masked pixels are mean-filled, corners outside a circle are not); the coarse accuracy on simulated patterns is 0.3-0.5 deg median at `bw` 68 (the correlator's tri-quadratic, Phase 4 D10); the mean-PC floor of ~0.3 deg median / ~0.5-1.0 deg max on the Ni data bounds every later comparison with kikuchipy's per-point-PC maps; EMSphInx' pixel stretch is not reproduced (0.49 px); the Gaussian background estimator over-estimates a background under a modulated signal (as EMSphInx'); the DCT IQ is offset-dependent and not kikuchipy's IQ.

## Definition of done
All Phase 5 boxes in `specs/roadmap.md` ticked, default suite green on Windows (this machine) with `-n 4` after a `-n 0` warm-up, weekly run once locally and green, PR opened into fork `develop`; determination results (the direction-oracle deltas, the size table, window fractions and solid angles, the D8 medians/maxima/scores for both signs and both correlators, the blob angles, the harmonic-recovery correlations, the `rDen` change, the error-floor statistics, the Gaussian/AHE/IQ numbers, timings and `tracemalloc` peaks) recorded below. "PR merged" is tracked in the roadmap.

## Recorded results

### 2026-08-17 -- pre-implementation reference measurements (spec drafting)

Environment: this machine (Windows 11), Python 3.13.12, numpy 2.4.6, scipy 1.17.1, numba 0.65.1, orix 0.14.2, scikit-image 0.26.0, kikuchipy 0.14.dev0 (`develop` after the Phase 4 merge). Drafting probe (scratchpad, not committed): `bp_probe.py` -- `detector_pixel_map` (D2 chain), `in_detector` (physical extent / pixel-centre extent, circle, nearest-pixel mask), `solid_angle_fraction` (`solidAngle(501)` on `_grid.square_to_sphere`), `build_lut` (north and interior-south loops with the physical guard, `scaleFactor`, `floor(x + 0.5)` sizes, DCT-convention or EMSphInx-stretch resample positions, clamped bilinear weights, `omeg`/`omgW`/`omgS`), `dct_rescale` (`scipy.fft.dctn` type 2 -> corner copy -> DC zero -> type 3, IQ on the input spectrum), `_unproject_kernel` (`@njit(cache=True, nogil=True)`, literal `unproject`), `window_mask`; `prep_probe.py` -- `cholesky_solve` (3x3 `solve::cholesky` with the C++ throws as exceptions), `gauss_estimate`, `gauss_fit` (verbatim stopping rule), `circ_mask_int`, `bckg_fit` (with the `emsphinx_compatible` abscissa offset), `bckg_subtract`, `MosaicAHE` (`setSize`/`computeHist`/`equalize` out of place), `to_uint8_minmax`, `process` (`PatternProcessor::process` order); drivers `probe1.py` (geometry, sizes, constant DCT, timings), `probe2.py` (D8 lock: `bw` argument, `stretch`/`nocirc` switches), `probe3.py`/`probe3b.py` (error floor small/large), `p5_prep.py`, `p5_ahe.py`, `p5_extra.py` (harmonic recovery, `rDen`, tilt table, `.py_func`, nine patterns), `p5_mem.py`, `p5_maskfill.py`, `p5_win.py`.

- **Geometry (D2)**: chain applied to `_get_direction_cosines_from_detector(Ni det)`: `max |col - c| = 1.554e-14`, `max |row - r| = 1.421e-14`, all in front; `dc[:, 2]` in `[0.18830, 0.99994]`; `_sample_to_detector_matrix(70 deg, 0, 0, 0)` vs `Rotation.from_matrix(.).to_matrix()`: `2.2e-16`; orthonormality `1.1e-16`; `Z_d = (0.34202014, 0, 0.93969262)`. Binning-neutral LUT (`binning` 1 / `px_size` 8 vs 8 / 1): `sphere_index` equal, weights `0.0`; EMsoft round trip `pc_emsoft = (35.93335, 137.58384, 240.33932)` -> `pc` to `2.8e-17`.
- **Sizes and window (D3/D4)**, Ni detector, `bw` (`dim`): circle -> `solid_angle_fraction`, `scale_factor`, `(h_out, w_out)`, `n_north`, `n_south`, `omgW`, `omgS`, `omgW/omgS`, sphere pixels; then no circle: `sa`, `scale`, out, `n`, fraction. 53 (55): `0.124350, 0.448905, 38x38, 667, 0, 723.93, 5834, 0.124089, 5834`; `0.145711, 0.485936, 41, 788, 0.146445`. 68 (71): `0.124350, 0.581873, 49x49, 1117, 0, 1215.42, 9802, 0.123997, 9802`; `0.145711, 0.629873, 53, 1317, 0.145851`. 88 (91): `0.124350, 0.748092, 63x63, 1844, 0, 2009.96, 16202, 0.124056, 16202`; `0.145711, 0.809804, 69, 2157, 0.144831`. 63 (65): circle out 45, `n` 934, scale `0.532009`; no circle out 49, `n` 1093, scale `0.575895`, fraction `0.144993`; 113 (115): circle out 80, `n` 2955, scale `0.947561`; no circle out 87, `n` 3474, scale `1.025728`, fraction `0.145514`. Direction oracle on the three synthetic detectors of the test: `(48, 60)` `1.42e-14 / 1.29e-14`, `(60, 60)` tilt -30 `1.42e-14 / 1.15e-14`, `(41, 41)` `1.42e-14 / 1.64e-14`, all in front. Pixel-centre-extent variant ("centres mode", rejected): `n` 663 / 1115 / 1837, fraction `0.123328 / 0.123791 / 0.123586` -- i.e. 2-7 rim points per bandwidth. Analytic pixel-grid solid-angle fraction: rectangle `0.145136`, circle `0.123848`. Build time 15-26 ms. `signal_mask[20:32, 25:40]`: `n` 1117 -> 1020, fraction `0.123997 -> 0.113003`, `sa` `0.124350 -> 0.113146`, `w_out` 49 -> 47. Rim points at `bw` 68: 2. EMSphInx stretch (`X (w_out - 1)`) vs DCT-convention positions: `max |dx| = 0.4973`, `max |dy| = 0.4891` resampled px (`w_out` 49).
- **Tilt table (D4)**, no circle, `bw` 68: `(sample_tilt, tilt) -> n_north, n_south, dc z range, window fraction, sa`: `(70, 0): 1317, 0, [0.188, 1.000], 0.1459, 0.1457`; `(70, 10): 1375, 0, [0.041, 0.991], 0.1462, 0.1456`; `(70, -30): 1235, 0, [0.579, 1.000], 0.1451, 0.1457`; `(0, 0): 524, 0, [-0.841, 0.379], 0.0464, 0.0450`; `(20, 0): 901, 0, [-0.605, 0.673], 0.0856, 0.0836`; `(-20, 0): 116, 0, [-0.975, 0.040], 0.0095, 0.0062`. (The `(0, 0)` pixel fraction below the equator is `0.783`, re-measured 2026-08-17; the drafting text's 55 % was wrong.)
- **DCT / constant pattern (D5)**: `dctn(dctn(x, 2), 3) / x = 14399.999999999998` (`4 h w = 14400`), rel `1.6e-15`; `dctn(np.full((60, 60), v), type=2)` AC max: `v = 1.0: 0.0`; `37.0: 1.104e-11` (DC `532800`); `200.0: 4.709e-11` (DC `2880000`); rescaled `60 -> 100` of the constant 37: `max |.| = 3.77e-11`; `unproject(np.full((60, 60), 7.0))` through the literal chain: `stdev = 5.403e-12`, `iq = 1.0`, **0 ones, 1117 non-zero noise values** (the mask branch is not taken) -- identical for the `uint8` constant; `window_mask` equals the constant-unproject `south` (both zero) but not `north`. Hence the `ptp` short-cut.
- **`unproject` identities and timing (D6)**, Ni pattern 0, `bw` 68: weighted mean `-9.588e-16`, weighted second moment `0.9999999999999991`, resampled stdev `487106.9`, `iq 0.766259`, south non-zero 0; per pattern (100 reps, warm): DCT + IQ `0.058-0.060` ms, DCT alone `0.038` ms, gather kernel `0.004` ms, `unproject` incl. DCT+IQ `0.062-0.069` ms; `analyze` `0.397-0.402` ms; the first call in a fresh process (cache load) `~15` ms. `.py_func` vs compiled: `0.0` (values and return).
- **`mlm`/`rDen` (D7)**: `bw` 68 circle: `mlm[0,0].real = 0.4394432`, `s2m/(4 pi) = 0.1239646`, `window_fraction 0.1239973`; no circle: `0.5169199`, `0.1458204`, `0.1458512`; `bw` 53: `0.4396649` -> `0.1240272` vs `0.1240889`; `bw` 88: `0.4396979` -> `0.1240365` vs `0.1240561`. `rDen` (finite, positive) min/max: `bw` 68 circle `1.7852 / 2.1503`, no circle `1.6599 / 1.9508`; 53 circle `2.0758 / 2.3969`, no circle `1.9350 / 2.2144`; 88 circle `1.5950 / 1.9294`. Normalised-correlator build 0.04-0.10 s. `signal_mask` block: `mlm[0,0]` `0.43944 -> 0.40047`, `max |rDen_m - rDen|/rDen = 0.1083`.
- **Forward-projection lock (D8)**, 27 rotations, per `bw`, `circle`: sign -> median / max / min deg, `n < 2`, `n < 1`; scores. `bw` 68 circle: `~R` normalised `0.424 / 0.727 / 0.175, 27, 27`; `R` normalised `34.999 / 58.174 / 0.417, 1, 1`; `~R` un-normalised `0.371 / 0.574 / 0.055, 27, 27`; `R` un-normalised `37.335 / 58.168 / 0.307, 2, 2`; scores normalised `1.025-1.078`, un-normalised `0.517-0.554`; per-rotation `~R` normalised: `0.390 0.363 0.649 0.675 0.208 0.727 0.471 0.424 0.248 0.480 0.365 0.424 0.612 0.220 0.425 0.300 0.274 0.191 0.417 0.455 0.474 0.459 0.485 0.541 0.411 0.330 0.175`. **`bw` 68 no circle: `~R` normalised `0.344 / 0.717 / 0.121, 27, 27`; `R` `35.022 / 58.177 / 0.360, 1, 1`; `~R` un-normalised `0.328 / 0.536 / 0.064, 27, 27`; `R` `35.541 / 58.175 / 0.259, 2, 2`; scores `1.087-1.143` / `0.594-0.652`.** `bw` 53 circle: `~R` `0.569 / 1.104 / 0.101, 27, 26`; `R` `37.415 / 60.230 / 2.085, 0, 0`; un-normalised `0.581 / 1.106`, `R` `30.789 / 57.246`; scores `0.917-0.997` / `0.398-0.440`; no circle: `~R` `0.512 / 1.040 / 0.139, 27, 26`; `R` `35.270 / 60.242`; un-normalised `0.591 / 1.040`; scores `0.978-1.060` / `0.458-0.514`. `bw` 88 circle: `~R` `0.256 / 0.417 / 0.085, 27, 27`; `R` `41.691 / 60.378`; un-normalised `0.253 / 0.423`; scores `1.102-1.140` / `0.607-0.655`. `bw` 68 circle **with the EMSphInx stretch**: `~R` `0.488 / 0.727 / 0.151`; un-normalised `0.605 / 0.710 / 0.178`; scores `1.002-1.076` / `0.485-0.553`. Time per pattern in the lock loop (incl. first-call cache loads averaged in): unproject 0.56-0.59 ms, analyze 0.24 / 0.47 / 0.97 ms, two correlations 13 / 26-33 / 60 ms at `bw` 53 / 68 / 88 (Phase 4's baseline).
- **Asymmetric blob (D8)**, Ni detector, circle, `bw` 68, `(r0, c0)` -> angle to `dc[r0, c0]` / vertical flip / transpose / horizontal flip (deg): `(10, 45): 1.05 / 49.43 / 71.44 / 52.62`; `(50, 5): 1.51 / 51.20 / 84.99 / 52.36`; `(5, 5)` (outside the circle): `9.13 / 60.36 / 9.13 / 67.12`; `(30, 30)`: `0.60 / 1.60 / 0.60 / 2.21` (near the centre every variant is close -- not a test pixel). Grid pitch `180/71 = 2.54` deg.
- **Harmonic recovery (D5)**, Ni detector, circle, `bw` 68, `(l, m)` -> windowed correlation, weighted rms, spectral correlation: `(2,1) 0.999999, 0.0014, 1.000000`; `(3,2) 0.999999, 0.0011, 1.000000`; `(4,0) 0.999999, 0.0011, 1.000000`; `(5,3) 0.999999, 0.0015, 0.999999`; `(6,4) 0.999998, 0.0020, 0.999998`; `(8,2) 0.999991, 0.0042, 0.999993`; `(12,5) 0.999964, 0.0085, 0.999971`. Mean fill with the `[20:32, 25:40]` block on `100 Re Y + 50`: `Y_4^2` block `= 0`: no fill `0.98122`, fill `0.99652`, unmasked `0.999999`; block `= 255`: `0.93232`, `0.99652`; `Y_8^2`: `0.99699 / 0.99892 / 0.999986` and `0.91218 / 0.99892`.
- **Nine Ni patterns**: `iq = 0.7663, 0.7734, 0.7726, 0.7667, 0.7769, 0.7709, 0.7680, 0.7788, 0.7732`; kikuchipy `get_image_quality`: `0.5238, 0.5239, 0.5285, 0.5202, 0.5288, 0.5248, 0.5232, 0.5252, 0.5299` (correlation `0.616`); IQ of a constant `1.0`, zeros `0.0`, uniform noise `0.0509`, `iq(3p) - iq(p) = 1.1e-16`, `iq(p + 100) - iq(p) = +0.0556`; south zero, 1117 of 1117 window values non-zero for every pattern.
- **Mean-PC error floor (D12)**: small: PC spread `(0.00572, 0.00323, 0.00131)`; stored scores `0.5255 0.4220 0.4748 0.5467 0.4253 0.4904 0.5112 0.4274 0.4849`; per-point refined vs stored: median `0.052`, max `0.188`, min `0.010`, scores `0.3903-0.5093` (44 pat/s); `pc_average` refined vs stored: `0.296 / 0.557 / 0.207`, scores `0.3895-0.5036`; `pc_average` vs per-point: median `0.333`, max `0.542`, per point `0.379 0.227 0.189 0.481 0.228 0.525 0.333 0.542 0.238`; `scipy` Nelder-Mead `pc_average` vs stored `0.296 / 0.558`. Large (`nickel_ebsd_large`, every 5th row/column, 165 points; PC spread `(0.0169, 0.0179, 0.0268)`, std `(0.00284, 0.00275, 0.00202)`; 0.8 s per run): `pc_average` vs per-point: median `0.291`, p90 `0.512`, p95 `0.735`, max `0.964`; per-point vs stored `0.051 / 0.119 (p90) / 0.300`; `pc_average` vs stored `0.302 / 0.521 / 1.075`; stored scores subset min/mean `0.2858 / 0.4870`, refined means `0.4466 / 0.4459`; `corr(misorientation, |pc - pc_average|) = 0.9687`; five worst `0.803 0.823 0.829 0.864 0.964` deg at `|dpc| = 0.0092 0.0093 0.0088 0.0093 0.0101`.
- **Gaussian fit (D9)**: exact `(25.3, 288, 150)` -> `(25.300000, 288.000000, 150.000000)`, `R^2 = 1`, 7 iterations; `(40.7, 60.5, 90)` -> exact, 7; `(30, 128, 200)` -> non-convergence (`ss = 0.0`, `metric = NaN` on every iteration, `a` stays `30.0`); `+ 0.5 sin x`: `(29.99997, 127.9986, 200.0005), R^2 0.999975, 7`; `(25.29898, 288.0522, 149.9951), 0.999949, 8`; `(40.69985, 60.4905, 90.0031), 0.999864, 7`; `+ 3 sin(x/2) + 5`: `(29.9994, 137.449, 203.477), 0.99743, 11`; `(25.3120, 312.55, 153.90), 0.99718, 9`; `(40.6985, 69.52, 93.94), 0.97840, 14`; flat `5.0`: the *Python* drafting probe raised in `cholesky` ("requires all positive or negative diagonal") -- the interpreted path's NaN sign bits differ, see the second block below: the compiled kernel runs 50 NaN iterations (status 3), the `.py_func` hits the sign test (status 2); both are "not converged" for the wrapper and the fallback; ramp `1..58`: `(62.667, 1700.15, 56.766), 0.9921, 12`. 2-D: circle mask `(60, 60)` centre `(30, 30)`, `r` 30, 2819 good; separable Gaussian + bands + 20: `gx = (31.421, 974.97, 200.12)`, `gy = (26.923, 796.59, 202.37)`, `c = 202.37`, error max/rms `33.35 / 24.39` (`True`), `25.71 / 23.98` (`False`); pure Gaussian: `gx.a = 31.4000`, `gy.a = 26.9000`, `c = 179.997`, error max `8.1565` (`True`) / `0.0028` (`False`); Ni pattern 0: `gx = (23.873, 1101.768, 232.838)`, `gy = (25.810, 1484.992, 233.768)`, `c = 233.768`; flat image: `gx = gy = (30, inf, 5.0)`, background `5.0`.
- **Mosaic AHE (D9)**: `n_regions` 10: tiles `(0,6,0,6), (6,12,0,6), (12,18,0,6) ... (54,60,54,60)`, `i_mids = 3 9 15 21 27 33 39 45 51 57`; `n_regions` 7: tiles `(0,9), (9,17), (17,26), (26,34), (34,43), (43,51), (51,60)`, mids `4 13 21 30 39 47 56`; uniform 100 -> `{255}`; ramp `0..236` step 4 -> `min 42.5`, `max 255.0`, correlation with the input `0.365`; circle mask changes pattern 0 by up to `164.61`, masked-out pixels in `[0.0, 136.97]`; all-masked-tile identity `2.84e-14`; vs `skimage` `equalize_adapthist(kernel_size=(6, 6), clip_limit=0, nbins=256) * 255`: max `5.9028 / 4.7222 / 5.9028`, mean `2.989 / 2.381 / 2.993`, correlation `1.000000` (patterns 0 / 4 / 8); vs kikuchipy `adaptive_histogram_equalization((6, 6), 0, 256)` (uint8): `6.625 / 5.250 / 6.444` raw, `1.019 / 1.014 / 1.019` after min-max rescale; kikuchipy default (`shape // 4`, `nbins` 128) correlation `0.669 / 0.674 / 0.672`; vs raw pattern `0.486 / 0.500 / 0.492`; `n_regions` 4 vs `kernel_size` 15: max `1.133`, correlation `0.9999999935`; `n_regions` 7 vs `60/7`: max `70.20`, correlation `0.9238`. `_circular_mask` vs `Window("circular")`: `(60,60)` 2819 = 2819, `(61,61)` 2821 = 2821, `(7,7)` 29 = 29, `(48,60)` 2531 (kp) vs 1792, `(60,48)` likewise (739 differing pixels).
- **Process chain (Python probe)**: AHE only `3.3` ms -> `[0, 255]` float64; background + AHE `4.3` ms -> `[0, 255]`; background only `1.2` ms -> `[-35.53, 18.65]`; none -> `[26, 245]` float64.
- **Construction / memory (D11)** (`tracemalloc`, SHT + weights + LUT + `mlm`): `bw` 53: 55 ms, resident 2.69 MB, peak 32.70 MB; 63: 48 ms, 4.29, 34.29; 68: 50 ms, 5.50, 35.45; 88: 69 ms, 11.46, 41.23; 113: 95 ms, 23.14, 52.61; per-pattern `unproject` (DCT+IQ+gather) `0.055 / 0.058 / 0.069 / 0.077 / 0.080` ms, `analyze` `0.186 / 0.300 / 0.402 / 0.826 / 1.827` ms; all on the Numba ring-DFT path (`dim <= 131`); the LUT is `98296` bytes at `bw` 68 (`n = 1117`); SHT constructor 7.3 ms, weights 0.9 ms, LUT 26.0 ms, mask + `mlm` 15.7 ms.

### 2026-08-17 -- re-measurements for the spec revision (adversarial review of the spec)

Same environment. Scratch port `bp2.py` (D1-D6: `BP(det, bw, circ, stretch, signal_mask, circle_rule="physical"|"resampled", guard)`, `kernel` = the literal `_unproject_kernel`, `iq_literal`), drivers `m1.py` (rim, circle rule, divisor, tilt 0, empty window), `m2.py` (blob at the default, single-pixel argmax, `stdev == 0` branch, tiny image, IQ constants, DCT round trip), `m3.py` (stretch negative control, DCT controls, one-pixel masks, `rDen`), `m4.py` (Numba fit kernel with the C++-direction `cholesky`: statuses compiled vs `.py_func`, constructed matrices), `m5.py` (`nickel_ebsd_large` 20-point subset), `m6.py` (detector navigation-shape edge cases), `m7.py` (NaN sign propagation). Every number quoted in the revised assertions above comes from these; the drafting numbers that changed are listed with the old value.

- **Rim** (Ni detector; `rim_res` = `x < 0 | x > w_out - 1 | y < 0 | y > h_out - 1` in resampled px, `rim_det` = `col`/`row` outside `[0, N - 1]`), `bw` -> circle: `n`, `w_out`, `rim_det`, `rim_res` (L/T/R/B), max weight, rows not exactly 1; then no circle: 53 -> `667, 38, 4, 6 (1/5/0/0), 0.996, 7`; `788, 41, 30, 32 (5/21/6/0), 0.986, 12`. **68 -> `1117, 49, 2, 3 (2/0/1/0), 0.969, 18`; `1317, 53, 24, 31 (10/10/11/0), 0.992, 23`.** 88 -> `1844, 63, 7, 7 (1/4/2/0), 0.984, 41`; `2157, 69, 44, 42 (11/12/11/8), 0.996, 39`. 113 -> `2955, 80, 22, 7 (2/2/3/0), 0.990, 57`; `3474, 87, 97, 52 (11/30/9/2), 0.991, 43`. Row-sum max deviation `1.1e-16`-`2.2e-16`; no weight equals 1 at any `bw`; facts (b)/(c) hold at every `bw` and setting; every non-rim point has four distinct indices; x-degenerate pairs 0/6, 1/11, 2/11, 3/9 and y-degenerate 0/0, 0/0, 0/8, 0/2 (circle/no circle) at 53/68/88/113. Old: "2 of 1117, asserted `<= 5`", "two equal indices and a weight of exactly 1".
- **Circle rule** (`bw` 68, `circular_mask=True`), physical vs C++ rescaled: `(48, 60)` `953 vs 958`, 5 differing, `rescaled_shape (42, 52)`; `(60, 60)` tilt -30 `1249 = 1249`; `(41, 41)` `1084 = 1084`; Ni `1117 = 1117`.
- **`solidAngle` divisor**: good points `62175` (circle) / `72856` (no circle); `/500002 = 0.124350 / 0.145711` (C++), `/502004 = 0.123854 / 0.145130` (consistent), `window_fraction 0.123997 / 0.145851`, analytic `0.123848 / 0.145136`.
- **Tilt 0**: fraction of pixels with `z < 0` `0.7833`, `z` range `[-0.841, 0.379]`, `n 524` vs `1317` (ratio `0.398`). Old: 55 %.
- **Empty window**: `sample_tilt` -40 / -60 / -80 with the Ni PC -> `sa 0.0`, `scale 0.0`, `out (0, 0)`, `n 0`; all-`True` mask -> `sa 0.0`, `(0, 0)`, `n 0`; mask leaving only `[30, 30]` -> `sa 5.4e-5`, `(1, 1)`, `n 1`; leaving `[28:33, 28:33]` -> `sa 1.37e-3`, `(5, 5)`, `n 13`; leaving only `[0, 0]` -> `sa 3.2e-5`, `(1, 1)`, **`n 0`**; of the 400 single-pixel masks on the 3-px lattice `[::3, ::3]`, 236 give `n 0` with `out (1, 1)`.
- **Blob at the default (`circular_mask=False`)**, `bw` 68, `(r0, c0)` -> true / vflip / hflip / transposed (deg): Ni `(10, 45): 1.05 / 49.43 / 52.62 / 71.44`; `(50, 5): 1.13 / 50.36 / 53.88 / 85.93`; `pc = (0.55, 0.65, 0.6)` `(15, 40): 1.14 / 41.68 / 28.24 / 50.88`; `(45, 12): 0.34 / 39.42 / 50.57 / 63.24`. Old (circle): `1.05 / 1.51`.
- **Single bright pixel** (rejected as a test), `bw` 68 no circle, `(r0, c0)` -> DCT-convention argmax to `dc[r0, c0]` / with the stretch / between the two argmaxes (deg): `(0, 0) 3.69 / 2.06 / 4.54`; `(59, 59) 3.14 / 22.91 / 19.78`; `(59, 0) 1.51 / 18.84 / 17.36`; `(0, 59) 0.46 / 0.46 / 0`; `(30, 59) 9.91 / 9.91 / 0`; `(59, 30) 0.76 / 0.79 / 1.46`; `(30, 0) 0.12 / 0.12 / 0`; `(0, 30) 0.86 / 0.86 / 0`; **`(58, 58) 13.58 / 6.73 / 18.82`**; `(1, 1) 1.49 / 1.49 / 0`; `(45, 55) 0.89 / 0.85 / 1.53`; `(55, 10) 2.20 / 6.99 / 9.12`; `(10, 45) 1.05 / 1.05 / 0`; **`(50, 5) 10.33 / 10.33 / 0`** (a `3 x 3` block at the same pixels: `1.89, 10.67, 6.76, 0.46, 1.63, 0.76, 0.12, 0.86, 3.07, 1.49, 0.89, 1.11, 1.05, 1.13`).
- **Resample-map pin**: `max |x_LUT - ((col + 0.5) w_out/ncols - 0.5)| = 0.0`; stretch `x_s = (col + 0.5)/ncols (w_out - 1)`: `max |x_s - x| = 0.4999` px, `i0` differs for `295 / 1317` (`j0` 230, either 460; circle 209 / 1117), all 1317 weight rows differ by `> 1e-6`, `max |wx1 - wx1_s|` on rows with equal `i0` `0.489`.
- **`stdev == 0` branch** (kernel on `np.full(h_out w_out, v)`, circle LUT): `v = 3.5 -> 7.548e-15`, `1.0 -> 1.109e-15`, `7.0 -> 1.510e-14`, `100.0 -> 6.254e-13`, `2.0 -> 2.217e-15`, **`0.0 -> 0.0`** (ones written); with test-local unit weights `[1, 0, 0, 0]` and `v = 2.0`: `2.2e-15` with `omgW = np.sum(omeg)`, exactly `0.0` only with `omgW` summed sequentially in the kernel's order (hence not used as a test). Tiny image (`standard_normal x 1e-14`, seed 0): `stdev 6.726e-15`, weighted mean `3.8e-17`, second moment `0.9999999999999998`.
- **IQ of constants** (literal transcription on `dctn(type=2)`): `1.0 / 7.0 / 37.0 / 200.0 -> 1.0` exactly (float equality `True`), `0.0 -> 0.0`, `uint8` 7 -> `1.0`.
- **DCT convention**: seeded standard-normal `60 x 60`: `dctn(dctn(x, 2), 3)/x` in `[14399.999999996313, 14400.00000001768]`, `max |rt/(4 h w) - x| = 1.554e-15` (`3.99e-16` relative to `max |x|`); constant 37 padded to `53 x 53` with DC kept: `dctn(., 3)` = `532800.0` everywhere (`= 37 x 14400`), `idctn(., type=3)`: `ptp 189.51`, `max ratio 3.55999e-4 = 1/53^2`; `idctn(., type=2)` (scipy's default inverse of a type-2 forward): `max |diff| / max |good| = 0.99991`.
- **`mlm` / `rDen`** (`bw` 68): no circle `mlm[0, 0] = 0.5169198974805473`, `s2m/(4 pi) 0.1458204`, `window_fraction 0.1458512`, `rDen` min/max `1.6599092756150797 / 1.9507568303551592`, shape `(68, 135, 135)`, finite; circle `0.4394432336951659`, `0.1239646`, `0.1239973`, `1.7851595843803882 / 2.150268103414923`.
- **Gaussian fit / cholesky (Numba scratch kernel, C++ direction, `error_model="numpy"`, NumPy-scalar accumulators)**: exact `(25.3, 288, 150)` -> status 0, exact, compiled and `.py_func`; integer mean `(30, 128, 200)` -> status 3 with params `(30, 128, 200, R^2 1)` both; **flat 5.0 -> compiled status 3, params all NaN; `.py_func` status 2, params `(0, NaN, 5, NaN)`, no exception either way**; flat 0.0 -> 3 / 2 likewise; ramp -> 0, `(62.667, 1700.15, 56.766, 0.9921)` both; `n < 3` -> 1. `_cholesky_solve_3x3`: all-`np.nan` -> 0 (compiled and `.py_func`); `a[1, 1] = -np.nan` -> 1 (sign bits `F, T, F`); all `-np.nan` -> 0; `diag(1e-17, 1, 1)` -> 2 (`< eps`); `[[1, 1, 0], [1, 1, 0], [0, 0, 1]]` -> 2; `-[[4, 2, 0], [2, 3, 1], [0, 1, 2]]`, `b = (1, 2, 3)` -> 0, `x = (-0.25, 1.57e-16, -1.5)`, `A x - b` max `4.4e-16`. NaN sign propagation: `0/0` has the sign bit set in NumPy and in Numba; through `dxb = dx/b`, `exp(-dx dxb)`, `c e`, `fxb dxb` the sign is kept in both; **`-(fxb) * 2`: NumPy clears it (negation flips the sign bit of a NaN), Numba keeps it** -- the reason the two paths part at the `cholesky` sign test. Flat estimate: `xy = +0.0`, `y2 = +0.0`, `b = NaN` (sign bit set).
- **`nickel_ebsd_large` 20-point subset** (`navigation_mask[::15, ::15] = False`; nlopt 2.10.0): load 1.8 s (`(75, 55)` navigation), backgrounds 0.7 s, per-point refinement 0.35 s, `pc_average` 0.22 s; (b) vs (a) median `0.283`, p90 `0.753`, p95 `0.762`, max `0.823`; (a) vs stored median `0.038`, max `0.174`; (b) vs stored `0.295`, p95 `0.815`, max `0.843`; `corr(misorientation, |pc - pc_average|) = 0.9575`; PC spread max `(0.0169, 0.0179, 0.0268)`; `scipy` `minimize`: `0.282 / 0.761 / 0.810`.
- **Detector edge cases**: `EBSDDetector((60, 60), pc=np.full((1, 1, 3), 0.5))`: `pc.shape (1, 1, 3)`, `navigation_shape (1, 1)`, `navigation_size 1`; `det[0, 0]` -> `TypeError: 'EBSDDetector' object is not subscriptable`; after `pc = pc.reshape(1, 3)`: `navigation_shape (1,)`, `gnomonic_bounds (1, 4)`, `sample_to_detector.shape (1,)`; `_get_direction_cosines_from_detector`: `(1, 3600, 3)` for the `(1, 1)` detector vs `(3600, 3)` for `(1,)`; `pc=[0.5, 0.5, 0.5]` and `pc=np.full((1, 3), 0.5)` -> `(1,)`; the raw Ni detector `(3, 3)`, size 9.
- **kikuchipy rounding**: `kp.pattern.rescale_intensity` casts with `astype(dtype_out)` (truncation; `_pattern.py:96`), so `[0, 1, 6] -> [0, 42, 255]` vs `_to_uint8`'s `[0, 43, 255]`; `_geometrical_simulation.py:206-207` uses the `x_scale`/`y_scale` properties (`/(ncols - 1)`).
