# Phase 5 -- `spherical-back-projection`: plan

Branch `spherical-back-projection` off `develop` (after the Phase 4 merge,
jwestraadt/kikuchipy#6). Models: this plan and the spec on Fable 5 (xhigh,
ultracode); tests, implementation, adversarial review and fixes by Opus 5
agents (xhigh, ultracode). Every task group is independently reviewable; the
order below is the implementation order. Tests are written (failing) before
the code they exercise. Numba caches: the new kernels are `cache=True`, so run
the new tests once with `-n 0` before `-n 4`. The drafting probe (a scratch
NumPy/Numba port of `detector.hpp`/`image.hpp`/`gaussian.hpp`/`ahe.hpp`/
`imprc.hpp` on top of Phases 1-4, driven by kikuchipy's detector, master
pattern and `nickel_ebsd_small`) that produced every number in
`requirements.md`/`validation.md` is **not** committed; its recipe is in
`validation.md` "Recorded results" so a reviewer can rebuild it. No compiled
C++ driver exists for this phase (`detector.hpp` needs HDF5 and the geometry
chain is deliberately kikuchipy's, `requirements.md` D2); the forward
projection `EBSDMasterPattern.get_patterns` is the oracle instead.
Revised 2026-08-17 after the adversarial review of the spec (fidelity and
conventions reviewers); the re-measurements behind the revision are in
`validation.md` "Recorded results", second dated block, and the decisions
taken in autonomous mode are in section 6 (items 9-19).

## 0. Constitution amendments (applied 2026-08-17 in the spec commit; `roadmap.md`, `tech-stack.md`, `mission.md` and the research doc are otherwise untouched by this phase)
1. `specs/roadmap.md`, Phase 5 boxes: rename the first box to "`_back_projection.py` (`SphericalBackProjector`: gather LUT on the north Legendre grid through kikuchipy's detector geometry (exact inverse of `_get_direction_cosines_for_fixed_pc`, pixel-centre convention, physical guard `z_s >= 0` -- the south hemisphere is never gathered), `solidAngle(501)`/`scaleFactor` port with `oversampling = sqrt(2)`, DCT rescaler with mean removal, DCT IQ, window mask built directly (pocketfft's constant DCT is inexact), `mlm`, `squared_harmonics` (`flm2`; the correlator owns `rDen`), single-PC (`navigation_size != 1`) and `azimuthal`/`twist` guards, two empty-window guards (`rescaled_shape < 1 px`, `n_points == 0`), `signal_mask` in kikuchipy polarity with mean fill, `circular_mask=False` default (physical circle), `window_harmonics` eager -- one immutable projector shared across threads)"; the second box to "`_preprocessing.py` (Gaussian background with the off-by-one behind `emsphinx_compatible`, `cholesky` with the C++ NaN comparison direction, mosaic AHE == `skimage` CLAHE for dividing tiles, `_preprocess_pattern` in EMSphInx order with `IndexEBSD` defaults `n_regions=10`, `gaussian_background=False`, no mask; no `scipy.fft` here -- the DCT IQ lives in `_back_projection.py`)"; the test box gains "`dctn` `4 h w` round trip + `idctn` negative control; structural pin of the resample map (the direction oracle cannot see a stretch there); rim structure; forward-projection lock measured: `~R` 0.34/0.72 deg median/max at bw 68 vs 35 deg for `R` -- `rotation_from_zyz` frozen; asymmetric-blob row/column check; scores and `rDen` measured-then-pinned; mosaic AHE vs kikuchipy AHE; Gaussian fit quirks; `nickel_ebsd_large` 20-point subset in the default suite"; the error-floor box becomes "Measured mean-PC error floor: `nickel_ebsd_small` refined with `pc_average` vs per-point PC median 0.33 / max 0.54 deg (vs stored xmap 0.30 / 0.56); `nickel_ebsd_large` 165-point subset median 0.29 / p95 0.74 / max 0.96 deg (corr 0.97 with `|pc - pc_average|`; 20-point default-suite subset 0.28 / 0.76 / 0.82); Phase 6 coarse tolerances (small: median < 1.5, >= 8/9 < 3 deg) and Phase 7 refined (small: all < 1.0, median < 0.5; large weekly: median < 0.6, p95 < 1.2, max < 2.0 deg) derive from it". Phase 6 boxes: append "Public `kp.indexing.SphericalBackProjector` exported with the indexer (one CHANGELOG entry)"; the Phase 6/7 tolerance sentences cite the measured floor.
2. `specs/tech-stack.md`, Numerics, back-projection bullet: append "the chain is `v = sample_to_detector.to_matrix() @ n`, `x_g = v0/v2`, `y_g = v1/v2` (`v2 > 0`), `col = (x_g - x_min)/((x_max - x_min)/ncols) - 0.5`, `row = (y_max - y_g)/((y_max - y_min)/nrows) - 0.5` (the exact inverse of `_get_direction_cosines_for_fixed_pc`, measured `1.6e-14` px), the sample frame is the sphere frame and `z_s >= 0` is the physical guard (EMSphInx `detector.hpp:336`; the C++ south loop is unreachable and not ported -- the south hemisphere is always zero), the resampled-image position is `(col + 0.5) w_out/ncols - 0.5` (the DCT sampling convention; EMSphInx' `X (w - 1)` stretch of up to 0.49 px is not reproduced), `circular_mask=False` by default (`IndexEBSD` `circmask = -1`), `signal_mask` (kikuchipy polarity) excludes sphere points by nearest pixel and mean-fills the pixel before the DCT, a constant pattern is detected with `ptp == 0` before the DCT because pocketfft's `dctn` of a constant has AC terms of `~1e-11` (found in Phase 5); `oversampling = sqrt(2)` on `scaleFactor(dim)` with `round = floor(x + 0.5)` (`solidAngle`'s divisor `500002` is a C++ quirk -- the loop evaluates `502^2` points, the consistent count is `502004` -- kept for size parity and labelled); the circle is the physical one of the unrescaled detector (EMSphInx' LUT circle in rescaled pixels differs by rounding on rectangular detectors: 5 of 958 points on `(48, 60)`, documented); the single-PC guard is `navigation_size != 1`; `rescaled_shape < 1 px` and `n_points == 0` raise `ValueError` (empty-window guards); `window_harmonics` is computed at construction and the projector is immutable -- Phase 6 shares one instance across dask threads (no `clone()`); kikuchipy `pc` (Bruker) enters directly -- binning/`px_size` never do".
3. `specs/tech-stack.md`, Numerics, Euler bullet: replace "the result orientation is sample->crystal (`~Rotation`), pinned by a forward-projection test through `EBSDMasterPattern.get_patterns` at known rotations *before* any EMSphInx reference is generated" by "the result orientation is sample->crystal, `_euler.rotation_from_zyz(zyz) = ~Rotation(zyz_to_quaternion(zyz))`, **pinned in Phase 5** by the forward-projection test (27 `get_patterns` rotations, `bw` 68: 0.34 deg median / 0.72 max for `~R`, 35 deg median for `R`); the `_euler` docstring drops 'provisional'".
4. `specs/tech-stack.md`, Numerics, quirks bullet: extend the list with "the `Model::fit` stopping rule (`|(ssPrev - ss)/ss|` non-decreasing and `< 1e-4`; an exact Gaussian with an integer mean gives `ss = 0`, NaN, non-convergence and the flat fallback -- reproduced), the row/column maxima initialised to `pattern[0, 0]` even when masked, masked-out pixels equalised by the AHE, the AHE uniform image -> 255, `_to_uint8` of a flat buffer -> zeros (C++ UB), the two half-pixel-different circles (`CircMask` integer centre `w//2` vs the projector's physical centre), the `ptp == 0` short-cut IQ (`1.0` for a non-zero constant, `0.0` for all-zero -- the exact `imageQuality` values), the `cholesky` NaN semantics (comparisons in the C++ direction, so a NaN never fails it and a flat input reaches the 50-iteration non-convergence; the status is platform-dependent and recorded, not asserted)"; and after "(mosaic AHE `n_regions`, 2-D Gaussian background, DCT image quality, circular mask)" add "measured in Phase 5: mosaic AHE equals `skimage`/kikuchipy CLAHE with `kernel_size = shape // n_regions`, `clip_limit=0`, `nbins=256` to 6 gray levels; the DCT IQ is offset-dependent and correlates 0.62 with kikuchipy's `get_image_quality`".
5. `specs/tech-stack.md`, Performance bullet: append "Phase 5 baseline: `unproject` (DCT + IQ + gather) 0.06-0.08 ms per pattern at `bw` 53-113 for a 60x60 detector, `sht.analyze` 0.19 / 0.40 / 0.83 / 1.83 ms at `bw` 53 / 68 / 88 / 113, projector construction 50-95 ms (a `~30 MB` transient for the `502 x 502` solid-angle grid), resident SHT tables 2.7-23 MB, LUT `< 0.3 MB`; back-projection + SHT is `< 4 %` of a coarse `correlate` at `bw` 68".
6. `specs/tech-stack.md`, Code layout, `@njit` bullet: extend the `error_model="numpy"` sentence with "(Phase 5: `_fit_gaussian_1d_kernel`, whose `estimate` divides `0/0` for a flat input and whose stopping metric divides `0/0` for an exact integer-mean Gaussian, both of which must reach the C++'s non-convergence fallback through NaN; its `_cholesky_solve_3x3` helper stays under the default model and writes its tests in the C++ direction (`if pivot < eps`, never `if not (pivot >= eps)`); scalar accumulators that may divide `0/0` are seeded from an array element so the `.py_func` yields NaN under `np.errstate` instead of raising)".
7. `specs/tech-stack.md`, Numerics, misorientation-tolerance bullet: replace "derived from a measured mean-PC error floor (re-run ...)" by the measured numbers of `requirements.md` D12 (0.33 / 0.54 deg small, 0.29 / 0.74 / 0.96 large 165 points weekly, 0.28 / 0.76 / 0.82 for the 20-point default-suite subset).
8. `specs/mission.md`, out-of-scope list: append "non-zero `azimuthal`/`twist` raises even though `EBSDDetector` implements them (v1); a detector footprint below the sample plane (`z_s < 0`) is clipped, as in EMSphInx"; deliverables table unchanged (the projector becomes public in Phase 6 next to `SphericalIndexer`).
9. `specs/_research/explore-emsphinx-core-algorithm.md` sections 4.3, 4.4, 4.5, 5.2, 5.3, 5.4, 6.4 and section 8: addenda pointing to this spec -- 4.3: the `X (w-1)` pixel stretch (D2) and the EMsoft/Bruker `cX` sign vs kikuchipy's emsoft5; 4.4: the south loop is unreachable (item 9 stays latent, now stated as unreachable), `omgS == 2 dim^2 - 4 (dim - 1)`, the `solidAngle` divisor quirk (`500002` vs the consistent `502004`), the two circles of the C++ (unrescaled in `solidAngle`, rescaled in the LUT loop) and the missing empty-geometry guard of the two-argument `rescale`; 4.5: pocketfft's constant DCT (a porting note); 5.2: the exact-Gaussian non-convergence, the masked `[0, 0]` initialisation and the NaN semantics of `cholesky` (a flat input never fails the decomposition, it runs 50 NaN iterations); 5.3: masked-out pixels are equalised, uniform -> 255, equivalence with CLAHE; 5.4: offset dependence; 6.4: `circmask` default `-1` (no detector circle by default); 8: new items 31-36 with those findings.

## 1. `_back_projection.py` -- tests `tests/test_indexing/test_spherical_back_projection.py`
1. Module header: kikuchipy GPL header + the delimited EMSphInx notice (source `include/modality/ebsd/detector.hpp` and `include/util/image.hpp` @ 60f3517, "changed by Johan Westraadt, 2026-08"), the list of ported functions with line ranges (`requirements.md` Scope) and the statement that `Geometry::interpolatePixel` is replaced by kikuchipy's detector geometry and that the south-hemisphere loop, `northPoleQuat`, `ecp`, `readEMsoft`, `BackProjector::clone` and the `flt` high-pass are not ported (the one-argument `rescale` guard `:424` *is*, extended); module docstring: the D2 chain and frame statement, the pixel-centre convention vs EMSphInx' stretch, the physical guard and the always-zero south hemisphere, the D3 size arithmetic with the Ni table, the constant-pattern finding, the `signal_mask` semantics (polarity, nearest pixel, mean fill), the D6 normalisation, `rDen` ownership, the D8 lock summary, the D11 numbers. Three import blocks; `Examples` on the class (doctest gate).
2. Helpers (private, vectorised): `_pixel_map(detector) -> (matrix, gnomonic_bounds, x_scale, y_scale)` and `_directions_to_pixels(n, geometry) -> (col, row, in_front)` (D2); `_inside_detector(col, row, shape, circular_mask, signal_mask) -> bool` (physical extent, the *physical* circle about `(N-1)/2` in detector pixels -- one predicate shared by the solid angle and the LUT, D1/D4 -- and the nearest-pixel mask); `_solid_angle_fraction(detector, circular_mask, signal_mask, grid_res=501)` (D3, `_grid.square_to_sphere` on the `(grid_res + 1)^2` grid, the literal divisor `grid_res^2 + (grid_res - 2)^2` with a comment naming it a quirk); `_rescaled_shape(shape, scale) -> (h_out, w_out)` with `floor(x + 0.5)`; `_build_lut(detector, dim, circular_mask, signal_mask, rescaled_shape) -> (sphere_index, pixel_index, weights, solid_angles, omg_w, omg_s)` (D4, incl. the DCT-convention position map `x = (col + 0.5) w_out / ncols - 0.5` and the rim clamps); `_dct_rescale(pattern, h_out, w_out, *, zero_mean=True, want_iq=False) -> (rescaled, iq)`, `_image_quality_from_spectrum(spectrum) -> float` and `_dct_image_quality(pattern) -> float` (D5; **the DCT image quality lives here**, with the `ptp == 0` short-cut `1.0`/`0.0`; `_preprocessing` has neither an IQ function nor a `dctn` binding); `_mean_fill(pattern, signal_mask) -> pattern` (D5).
3. Kernel `_unproject_kernel(rescaled_flat, pixel_index, weights, solid_angles, window_solid_angle, sphere_index, north_flat) -> float` (`@njit(cache=True, nogil=True)`, D6; the literal `stdev == 0.0` branch writing ones; returns the stdev or `0.0`).
4. `class SphericalBackProjector` (D1): constructor validation in this order -- `bandwidth`, `dim`/`sht`, detector type, **`navigation_size != 1`** (the `pc_average` message with the count; a size-1 detector whose `pc` is not `(1, 3)` is reshaped on the internal deepcopy), `azimuthal`/`twist`, `signal_mask` shape/dtype, `oversampling`; then `solid_angle_fraction`, `scale_factor`, `rescaled_shape`, **guard 1: `h_out < 1 or w_out < 1` -> `ValueError` naming `solid_angle_fraction`** (`:424` extended), the LUT, **guard 2: `n_points == 0` -> `ValueError` ("empty window")**, `window_solid_angle`/`sphere_solid_angle`/`window_fraction`, and finally **`window_harmonics = sht.analyze(*window_mask())` computed here** (eager; the instance is immutable afterwards and shared across threads -- no lazy attribute, no `clone()`); attributes and properties of D1; methods `unproject` (D6: shape check, float64 copy, mean fill, `ptp == 0` short-cut to the mask branch with **`iq = 1.0 if pattern.flat[0] != 0 else 0.0`**, DCT, kernel, `out=` handling, return tuple; per-call temporaries only), `window_mask`, `squared_harmonics` (D7, validated shape), `image_quality` (D5, the same short-cut), `__repr__`. Docstring `Notes` list every deviation from EMSphInx (pixel stretch not reproduced, south not gathered, `flip` absent, mask built directly, `ptp` pre-check with its IQ values, mean fill, rim clamp with the measured 1.8-2.8 % of the default window in the physical rim, the physical circle vs the C++'s rescaled circle (5 of 958 points on `(48, 60)`), the empty-window guards, `var` not returned, `clone` not ported -- share the instance) and every quirk kept (`stdev == 0` branch, `s2m` binary-mask assumption, `omgS` counting, the `solidAngle` divisor `500002`).
5. `src/kikuchipy/indexing/_spherical/__init__.py`: add ``_back_projection`` ("back-projection of detector patterns onto the square Legendre grid through kikuchipy's detector geometry") and ``_preprocessing`` ("EMSphInx pattern preprocessing: Gaussian background, mosaic AHE, DCT image quality") to the `Submodules` block, alphabetically; nothing imported.
6. `_euler.rotation_from_zyz`/`rotation_to_zyz` docstrings: drop the "provisional" paragraph, cite this spec's D8 (the only Phase 3 file touched; no behaviour change); **and `_xcorr.py:150`** (the module docstring's "provisional until Phase 5" sentence about the boundary orientation): replace by the D8 result (`~R` 0.34 / 0.72 deg median / max at `bw` 68 vs 35 deg for `R`) -- the only Phase 4 file touched, docstring only; both files are in the pre-commit list of `validation.md`.

## 2. `_preprocessing.py` -- tests `tests/test_indexing/test_spherical_preprocessing.py`
1. Module header: kikuchipy GPL header + the EMSphInx notice for `util/gaussian.hpp`, `util/ahe.hpp`, `modality/ebsd/imprc.hpp`, `util/linalg.hpp` (`solve::cholesky` `:354-358`, `decompose::cholesky` `:411-431`, `backsolve::cholesky` `:487-493`, 3x3), listing the ported functions and the not-ported in-place variants; module docstring with the D9 order, defaults, `circmask` mapping table and the kikuchipy-method mapping table (incl. the `_to_uint8` vs `rescale_intensity` row). **No `scipy.fft` import** in this module.
2. Kernels (`@njit(cache=True, nogil=True)`; `_fit_gaussian_1d_kernel` additionally `error_model="numpy"`): `_row_col_max_kernel(pattern, good, row_max, col_max)` (initialised to `pattern[0, 0]`), `_cholesky_solve_3x3(a, b, x) -> status` (helper called from the fit kernel; status 0 ok / 1 sign / 2 pivot; **comparisons in the C++ direction** `if signbit(a[i, i]) != neg` and `if pivot < eps` -- never `not (pivot >= eps)` -- so NaN never fails it, the `neg` negation of the solution of `backsolve` `:493`; no `error_model`), `_fit_gaussian_1d_kernel(y, params) -> status` (`estimate` + Gauss-Newton with the verbatim stopping rule; status 0 converged / 1 too few points / 2 cholesky failed / 3 not converged; `params = (a, b, c, r2)`; the accumulators `xy`, `y2`, `ybar` seeded from `y[0] * 0.0` so the `.py_func` divides NumPy scalars), `_ahe_cdf_kernel(image, good, has_mask, tiles, cdfs)` (256 bins, the flat-histogram fill for an all-masked tile), `_ahe_equalize_kernel(image, cdfs, nx, j_l, j_u, j_c, j_f, i_l, i_u, i_c, i_f, out)`, `_to_uint8_kernel(buffer, out)` (flat buffer -> zeros).
3. Functions: `_circular_mask(shape, radius=None) -> bool (h, w)`; `_fit_gaussian_1d(y, *, emsphinx_compatible=True) -> (a, b, c, r2, converged)`; `_gaussian_background(pattern, good_pixels=None, *, emsphinx_compatible=True) -> (background, info)` (`info`: `gx`, `gy`, `c`, `converged_x/y`); `_remove_gaussian_background(pattern, good_pixels=None, *, emsphinx_compatible=True) -> float64`; `_ahe_tiles(shape, n_regions) -> (tiles, j_pairs, i_pairs)` (the `setSize` bookkeeping, plain NumPy); `_mosaic_ahe(pattern_uint8, n_regions, good_pixels=None) -> float64`; `_to_uint8(buffer) -> uint8`; `_preprocess_pattern(pattern, *, good_pixels=None, gaussian_background=False, n_regions=10, emsphinx_compatible=True) -> float64` (D9 order; validates `n_regions`, `good_pixels` shape/dtype).

## 3. Tests -- exact assertions in `validation.md`
1. `test_spherical_back_projection.py` helpers: `ni_detector()` (`nickel_ebsd_small().detector.deepcopy()` with `pc = pc_average`), `ni_master_harmonics(bw)` (cached per module via `functools.lru_cache`), `direction_cosines(det)` (`_get_direction_cosines_from_detector`), `windowed_truth(projector, values)` (weighted zero-mean/unit-std on the LUT points), `misorientation_deg(rotation, zyz, sign)`, `KERNEL_NAMES = {"_unproject_kernel"}`. Classes: `TestConstruction` (D1 validation, the single-PC guard on `navigation_size` incl. a `(1, 1, 3)`-PC detector accepted, `azimuthal`/`twist`, the two empty-window guards (`sample_tilt=-40`; all-`True` mask; the `[0, 0]`-only mask that gives `rescaled_shape (1, 1)` and `n_points 0`), attributes, `window_harmonics` present in `vars(projector)` and `sht._quadrature_weights is not None` right after construction (eager, thread-shareable), `repr`, `deepcopy` isolation), `TestGeometry` (the D2 oracle at `1e-10` on the Ni detector and on three synthetic detectors incl. rectangular `(48, 60)`, `tilt` 10 and a PC below centre; binning-neutrality; EMsoft round trip; frames: `Z_d == (sin 20, 0, cos 20)`; south always zero for the tilt table of D4; `sample_tilt` 0: 78 % of the pixels below the equator, 60 % of the window lost), `TestSizes` (D3 table for `bw` 53/68/88, `floor(x + 0.5)` vs banker's, `omgS == 2 dim^2 - 4 (dim - 1)`, window fraction vs analytic `< 2 %`, `solid_angle_fraction` vs `window_fraction` `< 1 %` as plausibility only, the literal divisor `500002` pinned by the integer count), `TestLUT` (weights sum to 1, indices in range, **the rim structure in resampled coordinates** (facts (a)-(c) of D4, `rim_count <= 0.05 n_points`, counts recorded), **the structural pin of the resample map** (`pixel_index` bitwise and `weights` to `1e-12` from the reconstructed `(col, row)`; the stretch as negative control moves `i0` for `> 100` points and every weight row by `> 1e-6`), `signal_mask` nearest-pixel exclusion, circle exclusion incl. the rectangular `(48, 60)` pin `n_points == 953`), `TestUnproject` (**the `dctn` convention regression first**: `dctn(dctn(x, 2), 3) == 4 h w x` to `1e-12` and the `idctn` negative control; constant pattern -> mask bitwise incl. `uint8` with `iq == 1.0`, all-zero -> mask with `iq == 0.0`, `ptp` path never calls `dctn` (call recording), the kernel's literal `stdev == 0` branch on the **all-zero** resampled image and the tiny-amplitude (`1e-14`) image normalised (kernel called directly), weighted mean/std identities `1e-10`, `out=` reuse and stale-value contract, `return_image_quality`, `image_quality` equality incl. constants and zeros, dtype/shape errors, DCT-call recording `workers=1` and `type` 2 then 3), `TestImageQuality` (moved here from the preprocessing module: Ni values in `[0.7, 0.85]`, constant 1 / zeros 0 vs the literal transcription, scale invariance `1e-12`, offset dependence, `!=` kikuchipy's IQ recorded), `TestHarmonicRecovery` (`sph_harm_y` gated; `Y_l^m` for `(2,1), (3,2), (4,0), (5,3), (6,4), (8,2)`: windowed correlation `> 0.999`, spectral correlation `> 0.999`, weighted rms `< 0.01`; the mean-fill measurement of D5 with the 12x15 block: correlation `> 0.99` with fill), `TestWindowHarmonics` (`mlm[0,0] sqrt(4 pi)/(4 pi)` vs `window_fraction` rel `1e-3`; `signal_mask` changes `rDen` by `> 1 %`, finite/positive, **`min`/`max` of `rDen` pinned `approx(rel=0.05)`**; `squared_harmonics` validation), `TestForwardProjectionLock` (D8 at `bw` 68 default, 53 weekly; both correlators; both signs; **score min/max pinned `approx(rel=0.05)`**; the asymmetric-blob check with two detectors at the default `circular_mask=False`), `TestNickelSmall` (all nine patterns: south zero, window count == `n_points`, IQ in `[0.7, 0.85]` recorded, identities), `TestErrorFloor` (D12 recipe on `nickel_ebsd_small` in the default suite -- `refine_orientation` per-point vs `pc_average`, median `< 0.6` and max `< 1.0` deg asserted loosely, values recorded; **the 20-point `nickel_ebsd_large` subset (`[::15, ::15]`) in the default suite** with median `< 0.6`, max `< 1.5`, p95 recorded; the 165-point subset weekly with `p95 < 1.2`, max `< 1.5` and the correlation with `|dpc|` recorded; the method from `dependency_version["nlopt"]`), `TestKernels` (`.py_func` at `4 eps`, flags, `not hasattr(_preprocessing, "dctn")`), `TestBaselines` (timing/memory `record_property`, loose bounds).
2. `test_spherical_preprocessing.py`: `TestCircularMask` (`== Window("circular")` for square shapes, counts 2819/2821/29, rectangular difference recorded), `TestGaussianFit` (exact recovery of two non-integer-mean Gaussians, the ripple/noise cases with recorded accuracy, the integer-mean `ss = 0` non-convergence, the flat input `0/0` path (`error_model`; `converged False`, `status != 0`, no exception in the compiled kernel and in the `.py_func` under `np.errstate`, statuses recorded -- 3 compiled / 2 interpreted here -- not asserted), **`_cholesky_solve_3x3` NaN semantics and direction** (all-NaN -> 0; one flipped NaN sign bit -> 1; `a[0, 0] = 1e-17` -> 2; exact zero pivot -> 2; negative definite -> 0 with `A x = b`), the ramp, `n < 3`), `TestGaussianBackground` (the separable Gaussian: `a - 1` with `True`, error `< 0.01` with `False`, `> 5` with `True`; bands case recorded; flat -> mean; masked `[0, 0]` initialisation pinned by a pattern whose only maximum sits at `[0, 0]` under a mask that excludes it; subtract -> 0 on masked pixels; real pattern parameters recorded), `TestMosaicAHE` (tiles/midpoints for 10 and 7 regions, uniform -> 255, ramp range, all-masked tile identity `1e-10`, masked-out pixels equalised, `skimage` equivalence `< 8` gray levels for `n_regions` 10 and 4, divergence for 7 recorded, kikuchipy `adaptive_histogram_equalization` within 2 gray levels after min-max rescale, `n_regions` bounds), `TestToUint8` (`floor(x + 0.5)` rounding, flat -> zeros, `!= kp.pattern.rescale_intensity(., dtype_out=np.uint8)` on `[0, 1, 6]`), `TestProcessOrder` (the three branches: a spy on `_mosaic_ahe`/`_gaussian_background` call order, `uint8` input skips `_to_uint8`, float input does not, `n_regions=0` returns the float cast, defaults are `IndexEBSD`'s), `TestKernels` (`.py_func`, flags incl. `error_model` on the fit kernel only), `TestBaselines`.
3. Marks: `@pytest.mark.weekly` for the D8 lock at `bw` 53, the 165-point `nickel_ebsd_large` error-floor subset, the memory row at `bw` 113; everything else default, including the 20-point `nickel_ebsd_large` subset (download-backed, skips without the `tests` extra; the constitution's "subset in the default suite, full map weekly"). Estimated default-suite cost: the two `from_master_pattern` calls (~1 s), 27 `get_patterns` + 54 correlations at `bw` 68 (~2 s), `refine_orientation` on nine patterns x 2 (~0.5 s), `nickel_ebsd_large` load + backgrounds + 2 x 20 refinements (~3 s, measured), the rest milliseconds -- under 25 s single process.

## 4. Adversarial review and fixes
1. Ultracode workflow (Opus 5): reviewers refute `_back_projection.py` line by line against `detector.hpp`/`image.hpp` (`solidAngle` grid `(gridRes + 1)^2` and the divisor `gridRes^2 + (gridRes - 2)^2`, `scaleFactor` with `2 dim^2 - 4 (dim - 1)`, `round`, the LUT index order `i = y dim + x`, `omeg` per ring, `omgS` equator once, the DCT corner copy incl. the `wOut > wIn` reverse-order in-place trick being irrelevant here (fresh array), `wrk[0] = 0`, `imageQuality` on the input-size spectrum, `bilinearCoeff` order and clamps, the mean/stdev with `omeg` over `omgW`, the `stdev == 0` branch, `mask()`; and the geometry against `_master_pattern.py:133-205` (`x_scale` by `ncols`, `det_gn_y` descending, the half step, `~sample_to_detector`)), `_preprocessing.py` against `gaussian.hpp`/`ahe.hpp`/`imprc.hpp` (`estimate`'s `yc > 0` guard and `xy -= yi dx^2`, the Jacobian signs `dfda = -2 fxb`, `dfdb = fxb dxb`, the stopping rule, `n < 3`, `cholesky` diagonal-sign and `< eps` tests, `CircMask` integer centre, `cWrk[1..w-2]`, the fallback triple, `c = max`, `subtract` masked -> 0, `setSize` rounding and clamps, `upper_bound` semantics of `searchsorted(side="right")`, the `0.5/0.5` ends, the flat histogram fill, `nrm`, the four-term interpolation, `process` order and the `uint8` short-cut), a conventions reviewer (headers, numpydoc, three import blocks, numba flags, `workers=1`, no public names, no CHANGELOG, `signal_mask` polarity stated everywhere), and a test-quality reviewer with **bug injection** -- every mutation must be killed by a named test: **scatter instead of gather** (build the LUT from pixels -> nearest sphere point; dies by the `Y_l^m` weighted-rms and the window-count/`n_points` tests), **wrong hemisphere** (gather onto the south grid or drop the physical guard; dies by the south-zero real-data test and the D8 lock), **transposed row/col** or **vertical/horizontal flip** in the pixel map (dies by the direction oracle and the blob check), **`x_scale` divided by `ncols - 1`** (dies by the oracle at `1e-10`), **missing `- 0.5`** in `col`/`row` (oracle), **`(x_g + x_min)`** sign (oracle), **`sample_to_detector` not transposed** (oracle, `Z_d` test), **PC binning** (using `pc_emsoft`/`px_size` anywhere; dies by binning-neutrality), **EMSphInx stretch `X (w - 1)`** re-introduced (dies by the D2 oracle only if applied before the resample map; applied *at* the resample map it is invisible to the oracle, to the D8 lock and to a single-bright-pixel argmax test (measured: the argmax of a one-pixel pattern is 13.6 deg off at `(58, 58)` even without the stretch), so it dies by the **structural resample-map pin** of D4 -- `pixel_index`/`weights` from the reconstructed `(col, row)`, where the stretch moves `i0` for 295 of 1317 points), **circle in rescaled pixels** (the C++ LUT circle; dies by the `(48, 60)` pin `n_points == 953`, not 958), **empty-window guards dropped** (dies by the `sample_tilt=-40`, all-`True`-mask and `[0, 0]`-only-mask `ValueError` tests), **`navigation_shape != (1,)` guard** (dies by the accepted `(1, 1, 3)`-PC detector), **lazy `window_harmonics`** (dies by the eager-attribute test), **missing `sqrt(2)`** or **`solidAngle` without the circle/mask** (dies by the D3 size table and `w_out` under the `signal_mask`), **banker's rounding** of the sizes (dies by a detector whose `scale * ncols` ends in `.5` -- a test constructs one), **`omgS` counting the equator twice** (dies by `omgS == 2 dim^2 - 4 (dim - 1)`), **wrong normalisation weights** (unweighted mean/std or `omgS` instead of `omgW`; dies by the `1e-10` identities), **`stdev == 0` branch writing 0 or `>=`/`<= tiny`** (dies by the all-zero-resampled-image kernel test (ones expected) and by the tiny-amplitude image, which must be normalised to unit variance rather than masked), **`ptp` pre-check dropped** (dies by the `uint8`-constant -> mask bitwise test, since pocketfft's DCT is inexact), **short-cut `iq = 1.0` for the all-zero pattern** (dies by `image_quality(zeros) == 0.0 == unproject(zeros, return_image_quality=True)[2]`), **`_dct_image_quality` without the short-cut** (dies by the same identity on constants), **a second `dctn` binding in `_preprocessing`** (dies by `not hasattr(_preprocessing, "dctn")`), **`norm=` or `idctn` drift** (dies by the numeric `4 h w` round trip and the `idctn` negative control, besides the spy), **mean fill dropped** (dies by the D5 correlation `> 0.99` with the 255-block), **`mlm`/`flm2` swapped** in `squared_harmonics`/`window_harmonics` (dies by `mlm[0,0]` vs `window_fraction` and by the D8 normalised lock), **`squared_harmonics` not squared** (D8 normalised scores/lock), **sign of `rotation_from_zyz`** (D8: 35 deg), **`sample_tilt` sign** (`-sigma` -> `+sigma`: the oracle and the D8 lock), **DC not zeroed** (invisible to the normalisation identities, because the weighted mean is removed afterwards anyway; dies by the DCT-call recording test, whose spy on `dctn` asserts that the `type=3` input has `[0, 0] == 0` for a pattern with a large offset), **IQ on the truncated spectrum** (the equality `image_quality(pattern) == unproject(..., return_image_quality=True)[2]` cannot see it if both share the bug, so the IQ test also asserts the value against a test-local literal transcription of `imageQuality` on the *input-size* `dctn(pattern, type=2)`), **`type=3` -> `idctn`** (invisible to the identities, since `idctn`'s normalisation cancels in the unit-variance step; dies by the DCT-call recording test, which asserts the recorded names are `dctn` with `type` 2 then 3 and no `norm` kwarg, in the module namespace and in `scipy.fft`), **`workers` dropped**, **`nogil`/`cache` dropped, `parallel`/`fastmath` added**, `error_model` on the wrong kernel (flag tests); preprocessing: **abscissa `1..w-2` in faithful mode** (dies by the `a - 1` test), **no `+1` in exact mode**, **`c = mean` instead of `max`**, **fallback `b = 0`**, **stopping rule `<` only** (dies by the ripple case iteration count recorded? no -- by the integer-mean case, which then *converges* on the first iteration), **`cholesky` `< 0` instead of `< eps`** (dies by `a[0, 0] = 1e-17` -> status 2), **`cholesky` `not (pivot >= eps)`** (dies by the all-NaN matrix -> status 0), **`neg` negation of the solution dropped** (dies by the negative-definite case `A x = b`), **Python-float accumulators in `estimate`** (dies by the flat-input `.py_func` test, which must not raise), **`CircMask` centre `(w-1)/2`** (dies by the 2819 count), **AHE `hWdth = 1.0`** (tiles overlap; dies by the tile table), **`upper_bound` -> `lower_bound`** (`searchsorted(side="left")`; `upper_bound(mids, 3)` with `mids[0] = 3` returns 1, so pixel 3 interpolates between tiles 0 and 1 with `(l, u, c, f) = (0, 1, 1, 0)`, whereas `lower_bound` returns 0 and takes the `0.5/0.5` end branch; dies by the pixel-3 pair test), **flat histogram fill dropped** (all-masked tile: division by zero -> NaN; dies by the identity-ramp test), **masked pixels skipped in `equalize`** (dies by the "masked-out pixels equalised" test), **`_to_uint8` banker's rounding** (dies by a value ending in `.5`), **`process` order swapped** (AHE before background: dies by the spy order test), **`uint8` short-cut dropped** (a `uint8` image whose min/max are not `0`/`255` changes under `_to_uint8`, so `_preprocess_pattern(uint8_image, n_regions=10)` must equal `_mosaic_ahe(uint8_image, 10)` directly while the same values as `float32` go through `_to_uint8` first and differ; the spy-order test asserts both).
2. Fix, re-run the tests (`-n 0` then `-n 4`), `pre-commit run --files ...`, `--doctest-modules`, coverage `>= 95 %` (target 100 %; sanctioned exclusions: none expected -- the kernel's `stdev == 0` branch is reachable through the all-zero kernel test, both empty-window guards through their tests).

## 5. Commit and PR
1. Signed commits (spec + constitution amendments; failing tests; implementation; fixes); tick `specs/roadmap.md` Phase 5 boxes; push; PR into fork `develop` with the template, stating GPL-only licensing (EMSphInx-derived: BSD opt-out impossible), no CHANGELOG entry (private code).

## 6. Open questions -- Decided 2026-08-17 (autonomous mode, per the user's standing instruction)
Decided in this spec as written in `requirements.md`; flagged for the user's morning read.
1. (a) **`circular_mask=False` by default** (D1): matches `IndexEBSD`'s `circmask = -1` and kikuchipy's other indexing entry points; measured slightly *better* coarse accuracy without the circle on simulated patterns (0.34 vs 0.42 deg median at `bw` 68). Alternative `True` (the task's draft signature) rejected. Cheap to flip later (one default).
2. (b) **No `flip` parameter** (D1): kikuchipy has one array layout; the blob check pins it. Alternative: a `flip` keyword for EMSphInx parity -- rejected, since the C++ `flip` compensates *file* storage order, which kikuchipy's readers already normalise.
3. (c) **EMSphInx' `X (w-1)` pixel stretch not reproduced, not offered as a keyword** (D2): the direction oracle demands kikuchipy's pixel-centre convention; the measured orientation effect (median 0.42 -> 0.49 deg) is inside the coarse tolerance; Phase 10 will see it, if at all, as a sub-0.1-deg parity residual and can then decide.
4. (d) **`_dct_rescale` lives in `_back_projection.py`; Phase 2's `_resize_lambert` is left untouched** (D5): the two differ in shape (rectangular vs square), amplitude factor and mean removal; merging them would edit merged code for ten lines. Alternative rejected: refactor `_resize_lambert` onto the new helper (a later cleanup PR may).
5. (e) **`signal_mask` pixels are mean-filled before the DCT and excluded by nearest pixel** (D5/D4): measured 0.91-0.98 -> 0.997-0.999 correlation on the harmonic recovery. Alternatives (no fill; bilinear-neighbour exclusion) rejected as measured/argued there.
6. (f) **The projector keeps a private `SphericalHarmonicTransform` (`sht`) and offers `squared_harmonics(alm)`; `rDen` stays in the Phase 4 correlator** (D7). Alternative (a `MasterPatternHarmonics.squared()` method) rejected: `flm2` depends on the projector's grid, not on the master.
7. (g) **`SphericalBackProjector` stays private until Phase 6** (D12; roadmap amendment 0.1).
8. (h) **`error_model="numpy"` on `_fit_gaussian_1d_kernel`** (D9/D10), the second sanctioned use in the project.

Decided 2026-08-17 (autonomous mode) while applying the adversarial review of the spec; each with the measurement that settled it (`validation.md`, second recorded block):

9. (i) **The rim assertion is replaced by the three facts that hold** (D4): weight rows sum to 1 in `[0, 1]`; `x < 0` / `y < 0` -> the `i1`/`j1`-side weights exactly 0 with distinct indices; `x > w_out - 1` / `y > h_out - 1` -> the index pair collapses with *unequal* weights; the count is a fraction (`<= 0.05 n_points`) in resampled coordinates (measured 3 / 31 of 1117 / 1317 at `bw` 68). Both reviewers showed the draft's "two equal indices and a weight of exactly 1" is unsatisfiable by any clamp rule; the drafting count "2" was the circle case in detector coordinates (24 at the default). Alternative rejected: rejecting the rim ("centres mode", 1115 points) -- it would drop the physical half-pixel rim that the C++'s `X in [0, 1]` keeps.
10. (j) **The resample map is pinned structurally, not by a bright-pixel argmax** (D4): the reconstructed-`(col, row)` test kills the stretch for 295 of 1317 `i0`s and every weight row; a single-pixel argmax is measured unreliable (13.6 deg at `(58, 58)`, 10.3 at `(50, 5)` without any stretch) and identical with/without the stretch at half the probed pixels. D2 now states that the direction oracle does not cover the resample step.
11. (k) **`iq = 1.0 if pattern.flat[0] != 0 else 0.0` in the `ptp` short-cut, and the same short-cut in `_dct_image_quality`** (D5): the literal `imageQuality` gives exactly 1.0 for the constants 1 / 7 / 37 / 200 and 0.0 for zeros; the draft's unconditional 1.0 contradicted `_dct_image_quality(zeros) == 0.0`. Applying the short-cut in both places makes the identity `image_quality(p) == unproject(p, ...)[2]` hold by construction on constants; the literal value is asserted `abs 1e-12` there.
12. (l) **Two empty-window guards** (D1): `rescaled_shape < 1 px` (the one-argument `rescale` guard `:424`, which `Constants` bypasses) and `n_points == 0` (our addition: measured `rescaled_shape (1, 1)` with `n_points 0` for a one-pixel mask). Tested for a geometry cause (`sample_tilt=-40` -> `(0, 0)`) and two mask causes. Alternative rejected: only the mask case, as the draft's validation implied -- the tilt case is the likelier user error.
13. (m) **`cholesky` in the C++ comparison direction; the flat-input status is recorded, not asserted** (D9): compiled 3 (50 NaN iterations), `.py_func` 2 (NumPy negation flips the NaN sign bit of `fxb`) on this machine -- the C++'s own outcome ("failed to converged" vs "cholesky failed") is likewise whatever the platform's NaN sign propagation makes it, and both are caught by `BckgSub2D::fit`. Asserted instead: `converged False`, `status != 0`, no exception, the fallback, and the direction on constructed matrices (all-NaN -> 0, one flipped sign -> 1, `1e-17` -> 2, negative definite -> 0 with `A x = b`). Alternative rejected: an explicit `y2 == 0` guard in `estimate` (deterministic, same fallback) -- a deviation from the C++ for a degenerate input that the faithful path already handles without crashing, and `error_model="numpy"` is needed anyway for the integer-mean `ss = 0` quirk.
14. (n) **The DCT image quality has one owner, `_back_projection.py`; `_preprocessing.py` imports no `scipy.fft`** (D5/D10): the constitution's `workers=1` rule is enforced by one recording test on one binding, and the preprocessing module never depends on the projector module. `TestImageQuality` moves to the back-projection test module. Alternative rejected: parametrising the flag test over two namespaces -- a second call site with no consumer (Phase 6 takes IQ from `unproject`).
15. (o) **A numeric `dctn` convention regression is added** (D5): the `4 h w` round trip (`max |. / (4 h w) - x| = 1.6e-15` on a seeded `60 x 60`, asserted `1e-12`) and the `idctn(type=3)` negative control (`ptp 189.5` where the correct result is the constant `532800`; maximum ratio exactly `1/53^2`), mirroring Phase 2's `test_the_idctn_negative_control` and the roadmap's "`dctn` convention first".
16. (p) **The physical circle, not the C++ LUT's rescaled circle** (D1/D4): one predicate shared by `solid_angle_fraction` and the LUT; the C++ itself uses two (unrescaled in `solidAngle`, rescaled in the LUT loop) that differ by rounding only on rectangular detectors (5 of 958 points on `(48, 60)`, 0 on square ones); pinned by `n_points == 953` on `(48, 60)` and documented. Alternative rejected: the C++ rule in the LUT (one line, exact parity) -- it buys parity for `circmask = 0` on rectangular detectors only, which Phase 10 does not run, at the price of two circle definitions in the port.
17. (q) **`window_harmonics` eager; one immutable projector shared across threads; no `clone()`** (D1/D6/D11): the only mutable state of the draft was the lazily cached `mlm` (and the SHT's lazy weights); computing it in `__init__` (15.7 ms, already inside the D11 construction numbers) removes it, `unproject` keeps no scratch on the instance, and Phase 6 saves `nthreads x` the SHT tables (185 MB at `bw` 113 x 8 threads). Alternative rejected: a `clone()` sharing the LUT/SHT arrays -- nothing is left to clone.
18. (r) **`navigation_size != 1` guard with a `(1, 3)` reshape on the internal copy** (D1): accepts legitimate `(1, 1, 3)` PCs (measured `navigation_shape (1, 1)`), keeps the message honest (`EBSDDetector` is not subscriptable), and keeps kikuchipy's own `_get_direction_cosines_from_detector` on its fixed-PC branch.
19. (s) **Scores and `rDen` are measured-then-pinned (`approx(rel=0.05)` on min/max), the blob check is measured at the default `circular_mask=False`, and the `nickel_ebsd_large` 20-point subset joins the default suite** (D7/D8/D12): the constitution requires pinned scores (tech-stack.md Tests) and the large-map subset in the default suite (Tests bullet); the 20-point run costs ~3 s (load 1.8 s + backgrounds 0.7 s + 2 x 0.3 s) and is selected with `dependency_version["nlopt"]` rather than `importorskip`, which cannot fall back. The `sample_tilt` 0 record is corrected to 78 % of the pixels below the equator / 60 % of the window lost (draft: 55 %); the `solidAngle` divisor is labelled a quirk with the consistent value alongside, and the 0.3 % `solid_angle_fraction` vs `window_fraction` agreement is no longer cited as validation.
