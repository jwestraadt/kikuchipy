# Phase 4 -- `spherical-cross-correlation`: validation

## Automated (default suite; run from Git Bash)

```
uv run pytest tests/test_indexing/test_spherical_xcorr.py -n 0     # first run: warm the numba cache in one process
uv run pytest tests/test_indexing/test_spherical_xcorr.py -n 4
uv run pytest --doctest-modules src/kikuchipy/indexing/_spherical
uv run pytest --cov=kikuchipy.indexing._spherical --cov-report=term-missing tests/test_indexing/test_spherical_xcorr.py
uv run pre-commit run --files src/kikuchipy/indexing/_spherical/_xcorr.py src/kikuchipy/indexing/_spherical/__init__.py tests/test_indexing/test_spherical_xcorr.py
```

`eps` below is `numpy.finfo(float).eps = 2.220446049250313e-16`; `cell = 360/slP` deg and `half cell = 180/slP` deg for the correlator's `side_length = slP`; "misorientation" always means the symmetry-reduced metric of `requirements.md` D4 (`Orientation((~Rotation(zyz_to_quaternion(.))).data, symmetry).angle_with(..., degrees=True)`, `C1` for the symmetry-free cases). Every "measured" value comes from the drafting probe (a faithful transcription on top of Phase 1-3) and the compiled C++ driver, recorded at the end of this file; the assertion tolerances are measured-then-pinned with the stated margins. Random data uses `np.random.default_rng(seed)` with the seed given in the test name or parametrisation; nothing depends on test order. The C++ tests ported below (`testCorr` `:184-203`, `testNCorr` `:212-287`, `runTests` `:290-395`) pass their *refined* results (`correlate(..., ref = true)`, `:192`, `:275`) against `eps = cbrt(float epsilon) = 4.92e-3` deg (`:294`), `epsN = 4.92e-2` deg (`:316`) and `sqrt(eps) 5 = 0.351` deg for the point groups (`:345`); those three numbers are **Phase 7's** gate, re-run there on the same ports -- the coarse tiers below (`180/slP`-`360/slP` = 1.71-3.43 deg at `bw` 53) are this phase's. Bitwise assertions appear only where the two sides consume byte-identical inputs through the same code path (buffer reuse, pad garbage, flag exactness, clone); everything that crosses a library call shape or the compiled/interpreted boundary is pinned at a few `eps`.

Required assertions (each is a named test):

Sizes and layout (`SphericalCrossCorrelator.__init__`, D1)
- `side_length == fast_size(2 bw - 1)` and `half_side_length == side_length // 2 + 1` for the table `bw -> slP` `{53: 105, 54: 108, 55: 110, 56: 112, 57: 117, 58: 117, 59: 117, 60: 120, 61: 121, 62: 125, 63: 125, 64: 128, 68: 135, 88: 175, 113: 225, 123: 245, 158: 315}` (constructor cost only for `bw <= 68`; the larger three via the arithmetic on `fast_size` without constructing); `side_length_unpadded == 2 bw - 1`; guard against the scipy rule: `scipy.fft.next_fast_len(105, real=True) == 108 != 105` and `next_fast_len(117) == 120 != 117` are asserted so that a future "simplification" to `next_fast_len` fails here.
- `fxc.shape == (slP, slP, bwP)` complex128 C-contiguous, all zero before the first `compute`; `xc is None` before the first `compute`, `(bwP, slP, slP)` float64 C-contiguous after; `wigner_d_half_pi.shape == (bw, bw, bw)` and equal to `wigner_d_half_pi_table(bw, True)` (`np.array_equal` after masking the NaN slots, whose positions are asserted); `bandwidth < 1` -> `ValueError`; `wigner_d_half_pi=` of the wrong shape, dtype or non-C-contiguous, a `np.zeros`/`np.empty` buffer (tripwire slot `[0, bw - 1, 0]` must be NaN, Phase 3's `out=` idiom) and the **untransposed** `wigner_d_half_pi_table(bw, False)` (rejected by the sign of slot `[1, 0, 1]`, which is `d^1_{0,1}(pi/2) = -1/sqrt(2)` in the transposed layout and `d^1_{1,0}(pi/2) = +1/sqrt(2)` in the other, `bw >= 2`) -> `ValueError`; a `wigner_d_half_pi` from another instance is accepted and shared (`is`).
- No warning is emitted for `slP` 117 (`bw` 57; `warnings.catch_warnings(record=True)` assertion) -- a regression guard for constitution amendment 0.2, not a determination (Phase 1 never implemented the factor-13 warning; `grep warn _fft.py` finds only docstrings); the behavioural pin of the amendment is `side_length == fast_size(2 bw - 1) == 117` for `bw in {57, 58, 59}` together with the `next_fast_len` guard above.
- `interp_peak` before any `compute` -> `RuntimeError`; `correlate(..., refine=True)` -> `NotImplementedError` matching "Phase 7" on both classes; `n_fold` a `bool` or `< 1`, `mirror` not a `bool`, non-square/wrong-size `flm`/`gln` -> `ValueError`.

Spectrum kernel and inverse FFT (D2-D4)
- **Triple-sum oracle**: for `(bw, n_fold, mirror) in {(6, 1, F), (8, 1, F), (8, 2, T), (8, 3, F)}`, `flm` a random symmetric spectrum (`random_alm`, seed 1) and `gln` an *unrelated* random spectrum (so that transposes are not hidden by a peak), on 60 grid points `(k, n, m)` that include `k = 0`, `k = bwP - 1`, `n in {0, slP-1}`, `m in {0, slP-1}`: `|compute(flm, gln, n_fold, mirror)[k, n, m] - sum_l sum_{m',n'} f^l_{m'} conj(g^l_{n'}) wigner_D(l, n', m', index_to_euler((k, n, m), slP))| <= 1e-11` (measured worst 1.07e-14 / 1.51e-14 / 1.39e-14 at scale 12.4 / 14.6 / 7.6, i.e. > 500x margin), the oracle's imaginary part `<= 1e-13` (measured 1.9e-15), **and** each of the three other placements (`wigner_D(l, m', n')`, `conj(wigner_D(l, m', n'))`, `conj(wigner_D(l, n', m'))`) differs by more than 1 on at least one point (measured 9.4-17): the guard that kills a transposed table read, a missing conjugation of `gn`, or swapped alpha/gamma axes.
- **Inner-product oracle**: for `(bw, n_fold, mirror) in {(16, 1, F), (24, 4, T), (32, 3, F)}` (weekly: `(68, 4, T)`), 40 random grid points: `|<rotate_harmonics(flm, zyz), gln> - xc[k, n, m]| <= 1e-12 max|xc|` (measured 5.7e-14 / 4.3e-14 / 1.8e-13 / 8.6e-13 at scale 37.9 / 20.4 / 53.0 / 71.7, relative `<= 1.2e-14`).
- **Peak identity** (pins `norm="forward"` and the sign of the DFT): `correlate(flm, rotate_harmonics(flm, zyz0), n_fold, mirror)` for `(bw, n_fold, mirror) in {(6, 1, F), (8, 1, F), (8, 2, T)}`, `zyz0 = (0.7, 1.1, -2.3)` (`beta > 0`: recovered as the glide image with `beta <= 0`, D6): the *value* of the inner-product oracle at `zyz0` equals the total power `<flm, flm>` to `rel 1e-13` (measured equal to all printed digits: 20.430778 / 33.341807 / 9.196171); the interpolated `score` is `<= 1.01 power` and `>= 0.8 power` (measured 0.85-0.92 for white spectra, 1.005 in the ridge case of the Ni master, D5), and `max(xc) <= power (1 + 1e-12)`. **On-grid exactness** (the doctest example): for `zyz0 = index_to_euler((3, 5, 7), slP)` at `bw` 8, three seeds, `euler_to_index(zyz, slP) == (3, 5, 7)`, `|zyz - zyz0| <= 1e-12` and `|score/power - 1| <= 1e-12` (measured `|x| <= 2e-16`, `score/power = 1 + 2e-16` for three cells x five seeds; the axis neighbours of the peak cell are pairwise equal by `C(Q) = C(Q^-1)`, D4).
- **Separable inverse == full `irfftn`**: `_inverse_fft(fxc, n_fold) == scipy.fft.irfftn(fxc, s=(slP, slP, slP), axes=(0, 1, 2), norm="forward", workers=1)[:bwP]` to `abs 1e-12 max|xc|` for `(bw, n_fold) in {(16, 1), (17, 3), (24, 6), (53, 4)}` (measured `<= 3e-16` relative); plane skipping vs no skipping (`n_fold` passed as 1 to `_inverse_fft` on the same `fxc` whose skipped planes are zero) to `abs 4 eps max|xc|` (measured 0.0 here, but the two are different `pocketfft` call shapes -- `slP ceil(bwP/n_fold)` strided transforms vs `slP bwP` contiguous ones, SIMD-width dispatch at runtime -- so bitwise is not asserted; CI lesson).
- **Systemic zeros**: after `compute` with `n_fold` 4 and `mirror`, `fxc[:, :, m]` for `m % 4 != 0` is exactly zero, and `fxc[k]` for `k in [bw, slP - bw]` is exactly zero, `fxc[:, n]` for `n in [bw, slP - bw]` exactly zero; the C++ inclusive upper slice `slP - bw` is asserted separately (`bw` 16: slice 16 zero).
- **Buffer reuse**: `compute(f1, g1, 4, True)` then `compute(f2, g2, 1, False)` on one instance equals `compute(f2, g2, 1, False)` on a fresh instance bitwise (`np.array_equal` on `fxc` and `xc`), and the reverse order too (byte-identical inputs through the same kernel: bitwise is legitimate).
- **Pad-region garbage** (the only test that sees the zero-pad write of `:854`): on a warm instance set `cor.fxc[bw:slP - bw + 1] = 1e3 + 1e3j` (and, separately, `cor.fxc[:, bw:slP - bw + 1] = 1e3 + 1e3j` for the zero rows of `:833-836`), then `compute(f, g, n_fold, mirror)` equals a fresh instance bitwise on `fxc` and `xc` for `(n_fold, mirror) in {(1, F), (4, T)}` at `bw` 16 and 17 -- pins that the faithful writes cover every used slot; the untouched columns `m in [bw, bwP)` of rows `n < bw` are asserted still zero afterwards.
- **`compute` returns a fresh array**: `a = compute(f1, g1, ...)`, `b = compute(f2, g2, ...)` on one instance: `a is not b`, `a.base is None`, `a` bitwise equal to its copy taken before the second call, `cor.xc is b`.
- **Flags are exact and act on the right axis**: for a flag-conforming `flm` (rows `m % 4 != 0` and `(l + m)`-odd entries zero) and an unsymmetric `gln`, `compute(flm, gln, 4, True)` equals `compute(flm, gln, 1, False)` to `abs 1e-12 max|xc|` (the skipping is exact); for an `flm` that *violates* the flags (those entries filled), `compute(flm, gln, 4, True)` equals `compute(flm_masked, gln, 1, False)` to `abs 1e-12 max|xc|`, where `flm_masked` has exactly those entries zeroed, and differs from `compute(flm, gln, 1, False)` by more than `1e-3 max|xc|` -- which pins that `n_fold` skips columns of the alpha/`m` axis of `flm` (not `n` rows or `k` slices) and that `mirror` skips the odd degrees.
- **NaN table slots are never read**: replacing every NaN of the transposed table by `1e300` leaves `fxc` bitwise unchanged at `bw in {8, 9, 12}` for `(n_fold, mirror) in {(1, F), (3, T), (2, F)}` (measured `array_equal` True).
- **FFT-call recording (`workers=1` and the separable structure)**: `monkeypatch` wrappers are installed on **both** the `_xcorr` module namespace (every name in `{"ifft", "irfft", "fft", "rfft", "ifftn", "irfftn", "fftn", "rfftn"}` that `hasattr(_xcorr, name)`; the module binds `from scipy.fft import ifft, irfft`, D12) and `scipy.fft` (the same names, for a `scipy.fft.xxx(...)` call style); each records `(name, kwargs.get("workers"))`; one `compute` at `bw` 17 with `n_fold` 1 and one with `n_fold` 3 record **at least three calls each** (`assert len(recorded) >= 3`, so a wrong patch target cannot pass vacuously), every recorded `workers` is `1`, the recorded names are a subset of `{"ifft", "irfft"}` (no `n`-dimensional transform: this, not memory, pins the separable inverse -- D3/D11), and the number of `ifft` calls is 2 and of `irfft` calls 1 per `compute`.

Peak, neighbourhood, interpolation, Euler grid (D5-D6)
- `_find_peak`: equals `np.argmax` on random cubes (100 draws, seed 2); with `xc[0] = -inf` and a NaN elsewhere it returns the index of the largest finite value (the C++ `>` semantics; `np.argmax` would return the NaN's index); **tie-breaking**: on a cube with `xc.flat[[7, 41, 90]] = xc.max() + 1` it returns `7` (the smallest flat index, strict `>`; a `>=` mutation returns 90 -- the Ni ridge with 135 tied grid points cannot see this because every tie reduces to 0.000 deg), and the same for `_scale_and_find_peak` after scaling (`r_den` chosen so that three scaled values tie exactly, e.g. `r_den = 1` everywhere and the same three slots); `_scale_and_find_peak(xc, r_den)` returns `np.argmax(xc * r_den)` and leaves `xc` equal to `xc * r_den` bitwise (same multiplication, same order).
- **Glide identity on the full cube** (`bw` 16 and 60, even `slP`): `full = irfftn(fxc, s=(slP,)*3, norm="forward")`; `full[(-k) % slP, (n + slP/2) % slP, (m + slP/2) % slP] == full[k, n, m]` to `abs 1e-13 max|full|` (measured 1.2e-16 / 2.5e-16); for odd `slP` (`bw` 17, 63) the same identity with shift `(slP - 1)/2` and `(slP + 1)/2` is asserted **not** to hold (max relative difference `> 0.1`; measured 0.75 / 0.22) -- recorded so that nobody "fixes" the odd case to an on-grid glide.
- **Neighbourhood vs the full cube** at `k0 in {0, bwP - 1}` and 20 random `(n0, m0)` (seed 5): `emsphinx_compatible=False` block equals the periodic `3 x 3 x 3` block of `full` to `abs 1e-13 max|full|` for **even** `slP` (`bw` 16 / 60: measured 8.0e-17 / 3.2e-17 -- exact, the on-grid glide); for **odd** `slP` (`bw` 17 / 63) the assertion is *structural*, not against the full cube: every glided slot (`k + dk` outside `[0, bwP)`) equals `xc[slP - k, (n + s) % slP, (m + s) % slP]` with `s = (slP - 1)//2` bitwise and every other slot equals `xc[k, n, m]` bitwise, while the deviation from `full` is only **recorded** (`record_property`; measured 1.75e-2 / 0.153 at `bw` 17 and 1.8e-3 / 7.2e-2 at `bw` 63 for the seed-5 rotated-copy `gln`, but 0.52-0.97 at `k0 = bwP - 1` for an unrelated white `gln` -- the number is a property of the data, so it is not a tolerance); `emsphinx_compatible=True` differs from `full` by more than `0.05 max|full|` in at least one of the 20 blocks for every `bw in {16, 17, 60, 63}` (measured 0.32 / 0.35, 0.35 / 0.25, 0.091 / 0.082, 0.088 / 0.092; exhaustive over all `(n0, m0)`: 0.50 / 0.43 at `bw` 16, 0.124 / 0.110 at `bw` 60) **and** equals a test-local literal transcription of `:505-544` (per-slot shift of `inds[2]` *and* `inds[1]`, `+ bwP - 1` / `- bwP`, flat-offset reads, **every offset bounded by `size - 1`**) bitwise; interior `k0` (no glide): both settings equal the periodic block of `xc` bitwise (measured 0.0). **The even-`slP` overflows are pinned by construction, `slP` 32 (`bw` 16, `bwP` 17) and 120 (`bw` 60, `bwP` 61)**: (a) out-of-row `m` read: `k0 = 0`, `m0 = bwP` (slot 0 = `m0 - 1 = bwP - 1` -> `slP`), any `n0` -- because the shift is per slot index, **every** `[k, n, 0]` slot of the block (all three `k` planes, not only the glided one) contains `xc.flat[k' slP^2 + (n' + 1) slP]` with `k'`, `n'` the resolved indices of that slot (the next row's first element; in-buffer since `k' <= 1`; asserted against the transcription and against the flat buffer directly); (b) out-of-slice `n` read: `k0 = 0`, `n0 = bwP` (slot 0 = `bwP - 1` -> `slP`), any `m0` -- every `[k, 0, m]` slot contains `xc.flat[(k' + 1) slP^2 + m']` (the next beta slice's row 0; in-buffer since `k' <= 1`); (c) **past the end**: `k0 = bwP - 1`, `n0 = bwP - 2`, `m0 = 0` -- the three slots `[1, 2, m]` (centre `k` slot, shifted `n` slot) would be offsets `bwP slP^2 + m_slot >= size` and are asserted equal to `xc.flat[-1]` (the clamp), and `k0 = bwP - 1`, `n0 = 0`, `m0 = bwP - 2` -- the single slot `[1, 0, 2]` at offset exactly `size` likewise; the enumeration `35 / 123` past-the-end `(n0, m0)` pairs at `k0 = bwP - 1` for `slP` 32 / 120 is asserted by a test-local scan (`bwP - 1, bwP - 2, *` plus the three `(n0, bwP - 2)`), and **no clamped slot is ever compared with a C++ value** (undefined behaviour there).
- **Tri-quadratic interpolation**: on separable quadratics with cross term `0.2 (z - z0)(x - x0)` centred at `(0.3, -0.2, 0.45)`, `(0, 0, 0)`, `(0.49, 0.49, -0.49)`, `(-0.7, 0.6, 0.1)`: recovered `x` to `abs 1e-10` and value `5.0` to `abs 1e-10` (measured exact to 12 digits); a full 27-coefficient random tri-quadratic with a maximum inside the cube (built from random `a_{kji}` with a negative-definite quadratic part) is recovered to `abs 1e-8` (measured in the implementation, recorded); a saddle (`curv = (-1, +1, -1)`) leaves `x` finite; **the flat block** `np.full((3, 3, 3), 2.5)` (`det == 0`): the compiled kernel returns `x == (0, 0, 0)` and `2.5` **without raising** (the C++ NaN path to the `:1350` reset; requires `error_model="numpy"` -- measured: the default error model raises `ZeroDivisionError`, the `numpy` model returns `2.5, x = 0`), and `.py_func` under `np.errstate(divide="ignore", invalid="ignore")` returns the same (measured, with six `RuntimeWarning: invalid value encountered in scalar divide` when not silenced); `correlate` on an all-zero `gln` (constant `xc`) returns finite `zyz` and `score == 0` without raising, in both keyword settings, at `bw` 8.
- **`x[2]` bounds bug**: block with the maximum at `(0.2, 0.1, 1.5)`: `interp_peak(..., emsphinx_compatible=True)` returns `x == (0.2, 0.1, 1.5)` (alpha 1.5 cells beyond the centre) and the fitted value; `emsphinx_compatible=False` returns `x == 0` and the centre value; maximum at `(1.5, 0.1, 0.2)`: both settings reset (the `x[0]` term is checked twice in the C++ expression); maximum at `(0.1, 1.5, 0.2)`: both settings reset (the `x[1]` term -- so that a mutation of either of the two surviving terms is killed). The `emsphinx_compatible` keyword is the only knob (no module global): asserted by inspecting the signature (`inspect.signature(...).parameters["emsphinx_compatible"].default is True`).
- **Index <-> Euler**: `index_to_euler((k, n, m), slP)` matches the constitution formulas (`alpha = 2 pi m/slP - pi/2` etc.) to `abs 1e-15` for `slP in {32, 33, 105, 135}`; `euler_to_index(index_to_euler(knm)) == knm` exactly on every stored `k` and `n, m in {0, 1, slP//3, slP - 1}` for `slP in {32, 33, 105, 120, 135}` (measured exact); 200 random rotations per `slP` round-trip to `<= 1.5 sqrt(3) 180/slP` deg (measured worst 2.97 / 2.82 / 2.62 / 10.05 / 11.38 at `slP` 105 / 120 / 135 / 32 / 33) with `0 <= k < bwP` always; **unwrapped input** (D6, the `wrap_beta` addition): `euler_to_index((a, pi + 0.1, g), 135) == euler_to_index((a, -pi + 0.1, g), 135)` for three `(a, g)`, and 200 random triples with `beta` uniform in `[-4 pi, 4 pi]`, `alpha`, `gamma` in `[-3 pi, 3 pi]` give `0 <= k < bwP`, `0 <= n, m < slP` and the same index as their wrapped equivalent (`wrap_beta`, `alpha`/`gamma` reduced into `[-pi/2, 3 pi/2)`), for `slP in {33, 135}` (the unwrapped C++ formula would give `k = -2` at `beta = pi + 0.1`, `slP` 135 -- recorded); the glide: a `zyz` with `beta > 0` maps to a stored `k` and `index_to_euler` of it is the same rotation within the bound above; **rounding**: `euler_to_index` of a point exactly halfway between two cells uses `floor(x + 0.5)` (`std::round`), i.e. `nR = 66.5 -> 67` where Python's `round` gives 66 (asserted on a constructed `gamma`); **the `beta = 0` corner**: `euler_to_index((0, 0, 0), 135) == (67, 34, 34)` (the port's clamp; the C++ formula gives `k = 68 = bwP`, recorded), `euler_to_index((0, 0, 0), 128) == (64, 32, 32)`.

`correlate` -- ports of `sht_xcorr.cpp` (coarse path; D7, D10)
- **Symmetry-free pairs** (`testCorr`, `:184-203`, `runTests` sizes `:296-299`): for `bw in {53, 54, 55, 56, 57, 58, 59, 60, 62, 64, 68}` (default) and `{88, 113, 123, 158}` (weekly), three seeded rotations each (`rng = default_rng(bw)`), pairs from `random_pair_on_grid(bw, 1, False)`: the misorientation of `correlate(flm, gln, 1, False)` to the applied rotation is **`< 180/slP` when the applied `beta` is at least `2 pi/slP` from `0` and from `pi`, else `< 360/slP`**, and each value is recorded (`record_property(f"coarse_bw{bw}_rot{i}", ...)`); measured worst over six rotations per bandwidth 0.26-0.52 half cells and over 60 rotations at `bw` 53 / 60 / 63 / 68 0.51 / 0.35 / 0.51 / 0.51 half cells (medians 0.19-0.24). The raw argmax cell (`index_to_euler` of the argmax) is `< 2.5 half cells` (measured worst 1.55) -- recorded to show what the interpolation buys.
- **Point groups** (`:339-365`): for `name in {"112", "11m", "2/m", "3", "4", "4/m", "6", "6/m"}` (orix names of the C++ `112, 11m, 112/m, 3, 4, 4/m, 6, 6/m`; flags from `_symmetry.point_group_flags`: `(2, F), (1, T), (2, T), (3, F), (4, F), (4, T), (6, F), (6, T)`), `bw in {53, 60, 63}` in the default suite and `bw in 53..63` weekly, **three** seeded rotations each (`rng = default_rng(hash-free seed: 100 bw + group index)`; the count of the recorded measurement): same tier rule (measured worst 0.53 half cells, `112` at `bw` 63, over three rotations x `bw` 53-63); guard: the *unreduced* angle exceeds 30 deg for at least one case per group with `n_fold > 1` (measured 179.7-180 / 120 / 90 / 60), so the reduction is known to be doing work and to be on the correct side.
- **Mirror symmetrisation and zeroing are exact**: `random_pair_on_grid(bw, n_fold, True)` gives `flm[(l + m) odd] == 0` exactly (Phase 1 north == south property) and rows `m % n_fold != 0` exactly zero.
- **Near-degenerate `beta`** (D5): `bw in {53, 60, 63}`, `zyz = (0.7, beta, -1.9)` with `beta in {0.15 cell, pi - 0.15 cell, 1e-9, 0.45 cell}` (`cell = 2 pi/slP`): both keyword settings give misorientation `< 360/slP` (measured faithful worst 2.335 deg at `bw` 53 = 0.68 cells; per-plane worst 1.482 deg = 0.43 cells) and the values of both settings are recorded; at even `slP` (`bw` 60) `emsphinx_compatible=False` is `< 180/slP` (measured 0.19-0.28 / 0.23-0.31 / 0.008-0.009 / 1.05 deg over two draws) while the faithful setting is not (1.63 deg at `0.45` cells).
- `zyz` ranges (D7, per keyword; `cell = 2 pi/slP` rad here): on every result of the suites above with `emsphinx_compatible=False`: `-pi/2 - cell <= alpha, gamma <= 3 pi/2` and `-pi - cell <= beta <= cell` (closed; the `x = +-1` boundary is admitted by the `> 1` check); with the default `True`: the same `beta` and `gamma` intervals plus `np.isfinite(alpha)` only (the `x[2]` bug leaves `alpha` unchecked -- D5's pinned block yields `x[2] = 1.5`), and `max |x[2]|` over the suite is recorded (`record_property`; measured 0.56 over 460 correlations); `score` finite.
- **`clone()`**: shares `wigner_d_half_pi` (`is`), owns fresh `fxc`/`fm`/`gn` (`not is`), and gives bitwise the same `(zyz, score)`; running the original and the clone alternately with different inputs does not cross-contaminate (both equal fresh-instance results).

Normalised correlator (`testNCorr`, `:212-287`; D8)
- Wedge mask (`theta = atan2(n_y, n_x) in [-30, 60]` deg on the north hemisphere, additionally `n_z < cos 45 deg` on the south; band-limited by an `analyze`/`synthesize` round trip), `flm` band-limited by a round trip, `flm2 = analyze(f^2)`, `gln = analyze(mask * synthesize(rotate_harmonics(flm, zyz)))`: for `bw in {53, 54, 55, 56, 57, 58, 59, 60, 62, 64, 68}` (weekly 88, 113) three seeded rotations: `NormalizedSphericalCrossCorrelator(...).correlate(gln)` misorientation **`< 360/slP`** (one cell; measured worst 1.27 / 1.15 deg at `bw` 53 / 68 = 0.37 / 0.43 cells, 0.50 cells over the groups) and the un-normalised `correlate(flm, gln, ...)` on the same masked `gln` also `< 360/slP` (measured within 0.06 deg of the normalised result -- recorded, not asserted); the eight point groups at `bw in {53, 60, 63}` (default; `53..63` weekly), one seeded rotation each, same bound (measured worst 0.99 half cells = 0.50 cells over `bw` 53-63).
- `r_den` is finite and `> 0` everywhere for the wedge (all sizes above) and for the 55-deg cap (`np.isfinite(r_den).all() and (r_den > 0).all()`; the class `Notes` state that a zero radicand -- `f` constant over the window -- gives `+inf`, which *wins* the argmax, and a negative one a NaN that loses it, both unguarded as in the C++ and a caller error, D8); the constructor computes `r_den` from `mrf = compute(flm, mlm)` and `mrf2 = compute(flm2, mlm)` with the *master's* flags: a test at `bw` 17 (`n_fold` 1) and `bw` 24 (`n_fold` 3, mirror) asserts `r_den` equals a from-scratch NumPy evaluation of Huhle eq. 8-9 (`1 / sqrt(mrf2 - 2 (mrf/s2m) mrf + (mrf/s2m)^2 s2m)`, `s2m = mlm[0, 0].real sqrt(4 pi)`) on the two `compute` cubes to `rel 1e-13` (the drafting probe's `rDen` agreed with the compiled C++ to 4.4e-16-6.6e-16 relative at `bw` 17 / 24 / 53, recorded below); a wrong `s2m` (without the `sqrt(4 pi)`) changes `r_den` by more than 10 % somewhere (guard).
- Scores are recorded per case (`record_property`), asserted only `> 0` and finite (measured 0.50-0.68 for the wedge cases); the docstring states the "not divided by the pattern norm" semantics (asserted by `"pattern norm" in NormalizedSphericalCrossCorrelator.__doc__` or the equivalent phrase check).
- `clone()` shares `r_den`, `flm`, `flm2`, `mlm` and the table (`is`) and reproduces `(zyz, score)` bitwise.

Real data (Ni master, `kp.data.nickel_ebsd_master_pattern_small(projection="lambert", hemisphere="both")`, `MasterPatternHarmonics.from_master_pattern(mp, bandwidth=bw)`; `power = |a_0l|^2 + 2 sum_{m>0} |a_ml|^2` summed)
- **Autocorrelation at `bw` 68** (`n_fold` 4, mirror): `max(xc) == power` to `rel 1e-6` (measured 1.000000); the argmax's misorientation to the identity (symmetry `Oh`) `< 180/slP` (measured 0.000 -- the argmax lies on the `beta = -pi` ridge of the equatorial 2-folds); for **each of the 24 proper operators** `g` of orix `O`: `(k, n, m) = euler_to_index(quaternion_to_zyz((~g).data), slP)` has `xc[k, n, m] >= 0.9 power` (measured min 0.916), `interp_peak` from that cell has value `>= 0.9 power` (measured 0.949-1.005) and misorientation to the identity `< 360/slP` (one cell; measured worst 0.965 deg at the 180-deg z rotation, 23 of 24 `<= 0.67`); every grid point with `xc >= 0.95 power` is within `2 cells` of the identity after reduction (measured 1.49 deg vs 5.33); every operator has a grid point with `xc >= 0.9 power` within `1.5 cells` (measured 2.0 deg vs 4.0). Recorded: `min(xc)/power` (0.806), the count of grid points above 0.999 / 0.99 / 0.95 / 0.9 (135 / 405 / 1089 / 2226) -- the 135 is the ridge, which is why "24 maxima" is not asserted as a count.
- **Autocorrelation at `bw` 64** (`slP` 128, all 24 operators on the grid): the 8 operators with `beta in {0, pi}` (identity, `Rz(90/180/270)`, the four equatorial 2-folds about x, y, [110], [1-10]) have `xc == power` to `rel 1e-6` at their exact cell (measured 1.00000), the 16 with `beta = 90` deg have `xc >= 0.97 power` (measured 0.97804, the aliasing of the non-band-limited uint8 master, cf. Phase 3's 3-fold finding) -- both recorded; the interpolated peaks from the exact cells have misorientation `< 180/slP` for 22 of 24 and `< 360/slP` for all (measured 1.268 deg at `Rz(90)`/`Rz(180)`, the even-`slP` defect of D5 with `x[2] = 0.5`, 0.292 at two equatorial 2-folds, 0.0 elsewhere).
- **Rotated master** (`bw` 68, five seeded rotations, `gln = rotate_harmonics(flm, zyz)`): misorientation `< 180/slP` (measured worst 0.370 deg), `score/power >= 0.98` (measured 0.994).
- **D7 gate** (Phase 2 requirement): `flm_c`, `flm_p` from `emsphinx_compatible=True/False` (`a_00` -3.158 / -0.003 recorded), `flm2` from each setting's own `synthesize`, `mlm` from the wedge and from the 55-deg cap about `(0, -0.5, 0.85)` (mask fractions recorded: 0.214 / 0.213), four seeded rotations, `gln = analyze(mask * synthesize(rotate_harmonics(flm, zyz)))`: the normalised argmax misorientation is `< 360/slP` in both settings (measured 0.12-0.53 / 0.09-0.47 deg vs cell 2.667 -- pinned at one cell like every masked case), `|d_True - d_False| < 180/slP` (measured `<= 0.44`), and the scores are recorded per setting and mask (measured 3.71-3.93 vs 1.37-1.47) together with their ratio (`~2.6`, asserted `> 1.5` and `< 4` so that a future normalisation change is noticed); the un-normalised scores on the same masked `gln` (2.52-2.60 vs 1.98-2.21) recorded.

Kernels and conventions
- Every `@njit` kernel of `_xcorr.py` (`KERNEL_NAMES` asserted equal to the module's `CPUDispatcher` members) has a `py_func` (`hasattr` guard), driven with NumPy arrays only, whose result equals the compiled one: `_xcorr_spectrum` to `abs 4 eps max|fxc|` at `bw` 8, 9, 12 (measured bitwise), `_interpolate_maxima` with `|x_compiled - x_py| <= 1e-9` and the value to `rel 1e-12` on 50 random blocks plus the synthetic quadratics (measured bitwise here; the bound is a fraction of the `sqrt(eps)` stopping rule, since one FMA contraction can change the iteration count -- bitwise agreement is recorded, not asserted), `_extract_neighborhood`, `_find_peak`, `_scale_and_find_peak` bitwise (integer/copy semantics); every kernel is compiled with `cache=True, nogil=True` and without `parallel`/`fastmath`, asserted exactly as Phases 1-3 do (`targetoptions.get("nogil") is True`, `type(kernel._cache).__name__ == "FunctionCache"`, `not targetoptions.get("parallel", False)`, `not targetoptions.get("fastmath", False)`), **and** `targetoptions.get("error_model") == "numpy"` for `_interpolate_maxima` and `targetoptions.get("error_model") is None` for the other four (measured: `targetoptions` is `{'nogil': True, 'error_model': 'numpy', 'nopython': True, 'boundscheck': None}` resp. without the key on numba 0.65.1).
- Timing/memory baselines via `record_property` (informational): `correlate` at `bw in {53, 68, 88}` for `n_fold` 1 and `m-3m` (best of three after a warm-up), the three stages separately at `bw` 68 (kernel, `_inverse_fft`, peak), and one loose bound `< 5 s` per call; `tracemalloc` at `bw in {63, 68, 88}` (113 weekly) for both classes: construct + first `correlate` peak, resident after, and (after `tracemalloc.reset_peak()`) the recurring peak and transient of a second `correlate` -- all recorded; asserted (D11): recurring peak `< 3x` the analytic sum `fxc + xc [+ rDen] + table` (sanity; measured 1.92-1.94x / 1.71x), resident after `correlate` `< 1.2x` the analytic sum (measured 1.00-1.02x; a `[:bwP]` view of a full `irfftn` cube would give 1.30x), `cor.xc.base is None` and `cor.xc.nbytes == 8 bwP slP^2`. **These bounds do not discriminate a full-cube inverse that copies its slice** (measured: `irfftn` transforms in place along the first two axes, transient `+19.7` MB vs the separable `+29.7` at `bw` 68, identical recurring peaks 61.8 vs 62.1 MB); the separable structure is pinned by the FFT-call recording test above.

## Weekly
- `uv run pytest --weekly tests/test_indexing/test_spherical_xcorr.py -n 4`: the size list `{88, 113, 123, 158}` (`testCorr` port, three rotations each; measured `slP` 175 / 225 / 245 / 315, `correlate` 0.1 / 0.24 / ~0.35 / ~0.8 s), the point-group sweeps (plain, three rotations, and normalised wedge, one rotation) at the remaining `bw in 53..63` (54-59, 61, 62), the normalised wedge at 88 / 113, the inner-product oracle at `bw` 68, memory at 113, and 30 rotations at `bw` 68 for the near-degenerate-`beta` statistics (fraction of cases with the applied `beta` within one cell of `0`/`pi`, worst misorientation in each tier; recorded).

## Manual
- Headers: `_xcorr.py` carries the kikuchipy GPL header and the delimited EMSphInx notice (CMU/Lenthe, GPL-2.0-or-later conveyed under GPL-3.0-or-later, "changed by Johan Westraadt, 2026-08") listing the ported functions of `sht_xcorr.hpp` and `util/fft.hpp:671-678` with line ranges and the not-ported list; the test module has the kikuchipy header and cites `test/sht/sht_xcorr.cpp:108-395`.
- `src/kikuchipy/indexing/_spherical/__init__.py` lists ``_xcorr`` in its `Submodules` block and still imports nothing; no new public names in `indexing/__init__.pyi`; no CHANGELOG entry.
- Docstrings freeze: the closed form of `compute()` and the `rotate_harmonics` inner-product identity, the `[k, n, m]` layouts and `bwP = slP//2 + 1`, the two C++ `extractNeighborhood` defects with line numbers and both keyword settings, the `x[2]` bug, the score semantics of both correlators (un-normalised: total power at a perfect match; normalised: `xc rDen`, not divided by the pattern norm), the D7-gate numbers, the memory formula and measured peaks, the not-thread-safe/`clone()` rule, the pocketfft factor-13 remark, `refine=True` deferred to Phase 7, `rotation_from_zyz` provisional (Phase 5); numpydoc, types in signatures only, comment lines `<= 72` chars, three import blocks.
- Coverage of `src/kikuchipy/indexing/_spherical/_xcorr.py` `>= 95 %` (kernels via `.py_func`; the dead `vnc = -vnc` of the mirror branch is the only `# pragma: no cover`, D2).
- Adversarial review findings addressed or explicitly deferred with reason; the bug-injection list of `plan.md` 3.1 fully killed.
- Known limitations stated: the coarse path's accuracy is the tri-quadratic's (half a cell in general, one cell near `beta = 0`/`pi` and for masked patterns; Phase 7 refines); the faithful neighbourhood reproduces two C++ defects (per-slot shift of both the alpha and the gamma slot, even-`slP` off-by-one) and clamps the past-the-end reads of `k0 = bwP - 1` (`slP + 3` `(n0, m0)` pairs per even `slP`) to the last element; under `emsphinx_compatible=True` `alpha` is not bounds-checked; the odd-`slP` glide has no on-grid form; the separable inverse allocates one `fxc`-sized transient (recorded); `rDen` is unguarded against non-positive radicands (zero -> `+inf` wins the argmax); `euler_to_index` wraps `beta` and reduces `alpha`/`gamma` (a documented addition to the C++); `refine=True` not available; the sign of the boundary orientation is provisional.

## Definition of done
All Phase 4 boxes in `specs/roadmap.md` ticked, default suite green on Windows (this machine) with `-n 4` after a `-n 0` warm-up, weekly run once locally and green, PR opened into fork `develop`; determination results (worst oracle deltas, C++-vs-port deltas if the reviewer rebuilds the driver, coarse misorientation statistics per bandwidth and group, the near-degenerate values in both keyword settings, the Ni operator table at `bw` 68 and 64, the D7-gate scores, timings and `tracemalloc` peaks) recorded below. "PR merged" is tracked in the roadmap.

## Recorded results

### 2026-08-17 -- pre-implementation reference measurements (spec drafting)

Environment: this machine (Windows 11), Python 3.13.12, numpy 2.4.6, scipy 1.17.1, numba 0.65.1, orix 0.14.2, MSVC 19.44 (VS 2022 Community), FFTW 3 (the static `fftw3.lib` of `EMSphInx/build/fftwd/install`). Drafting probe: a faithful NumPy/Numba transcription of `sht_xcorr.hpp` (`compute` with the four slots, `SepRealFFT3D::inverse` via `scipy.fft` both separable and as `irfftn[:bwP]`, `findPeak`, `extractNeighborhood<1>` per-slot glide, `interpolateMaxima`, `interpPeak` with the `x[2]` bug, `eulerIndex`/`indexEuler`, `NormalizedCorrelator::Constants` and `correlate`) on top of the merged Phase 1-3 modules (scratchpad, not committed). C++ driver: `driver.cpp` subclassing `sphere::Correlator<double>` and `NormalizedCorrelator<double>` (to reach the protected `compute`/`findPeak`/`interpPeak`/`refinePeak` and `ncLut->rDen`), reading `bw, fMr, fNf, hasNorm` + `flm, gln [, flm2, mlm]` from a binary file and writing `slP, bwP, xc, iMax, eu, peak, eu_ref, peak_ref [, eu_n, peak_n, rDen, eu_nr, peak_nr]`, built with `cl /O2 /MD /std:c++14 /DEM_USE_D /DEMSPHINX_GIT_HASH=60f3517 /DEMSPHINX_GIT_BRANCH=master /I<EMSphInx>/include /I<EMSphInx>/build/fftwd/install/include driver.cpp /link <...>/fftw3.lib advapi32.lib shell32.lib`. EMSphInx's own `build/test/Release/TestXCorr.exe` (the compiled `sht_xcorr.cpp`) passes on this machine (exit 0, 13.5 s).

- **Port vs compiled C++**, `(bw, n_fold, mirror)` -> `max|xc - xc_cpp| / max|xc_cpp|`, argmax equal, `max|eu - eu_cpp|`, peak relative:
  `(16, 1, F)` [`slP` 32 even]: 1.75e-16, yes, 0.0, 0.0; `(17, 1, F)` [33]: 1.76e-16, yes, 0.0, 0.0; `(16, 3, T)`: 2.85e-16, yes, 0.0, 2.8e-16; `(17, 4, F)`: 1.66e-16, yes, 0.0, 1.6e-16; `(17, 2, T)`: 1.67e-16, yes, 0.0, 1.6e-16; `(24, 6, T)` [48]: 2.07e-16, yes, 4.4e-16, 0.0; `(53, 1, F)` [105]: 2.93e-16, yes, 0.0, 2.7e-16 -- identical for the separable and the full-`irfftn` inverse. Normalised (wedge mask, `testNCorr` recipe): `rDen` relative 6.6e-16 (`bw` 17), 4.4e-16 (24, `n_fold` 3, mirror), 5.3e-16 (53); `eu` identical, peak relative 0 / 1.7e-16 / 0. Non-zero entries of `fxc`: 15086 / 17408 (`bw` 16 `n_fold` 1), 4843 / 17408 (`n_fold` 3 mirror), 581387 / 584325 (`bw` 53). Coarse misorientation of those cases (unreduced): 0.76 deg (`bw` 16, half cell 5.6), 1.46 (17, 5.5), 0.23 (53, 1.71); the symmetric cases 120 / 180 / 180 / 119 deg unreduced (the C++ `refinePeak` gives the same 120 / 180, i.e. it converges to a symmetry-equivalent solution too). C++ refined misorientation for the symmetry-free cases: 0.0 (bw 16, 17, 53 -- `angle_with` clips), normalised 0.40 (17) / 0.014 deg (53).
- **Analytic oracle** (`bw` 6 / 8 / 8-`n_fold`2-mirror, 60 grid points each, `gln` unrelated to `flm`): worst `|oracle - xc|` per placement `A = D(l, m', n')`: 17.1 / 13.3 / 9.8; `B = conj(A)`: 13.5 / 16.4 / 11.4; **`C = D(l, n', m')`: 1.07e-14 / 1.51e-14 / 1.39e-14**; `D = conj(C)`: 11.3 / 12.6 / 9.4; scale 12.4 / 14.6 / 7.6; imaginary part `<= 1.9e-15`. Rotate-then-evaluate at the applied `zyz0`, form C: `20.430778 / 33.341807 / 9.196171` == total power to all digits (forms A/B/D: 10.87 / -5.02 / 1.56 etc.). Inner-product oracle at `bw` 16 / 24 (`n_fold` 4, mirror) / 32 (`n_fold` 3) / 68 (`n_fold` 4, mirror), 40 grid points: 5.68e-14 / 4.31e-14 / 1.83e-13 / 8.56e-13 (scale 37.9 / 20.4 / 53.0 / 71.7); interpolated peak / power for a rotated copy 0.869 / 0.919 / 0.855 / 0.845 and argmax-cell value / power 0.795 / 0.861 / 0.778 / 0.738.
- **Coarse accuracy, symmetry-free** (six rotations per `bw`, worst / median deg and worst in half cells): 53 (105): 0.471 / 0.346, 0.27; 54 (108): 0.547 / 0.253, 0.33; 55 (110): 0.708 / 0.394, 0.43; 56 (112): 0.589 / 0.250, 0.37; 57 (117): 0.398 / 0.225, 0.26; 58: 0.439 / 0.269, 0.29; 59: 0.745 / 0.315, 0.48; 60 (120): 0.778 / 0.323, 0.52; 62 (125): 0.530 / 0.274, 0.37; 64 (128): 0.379 / 0.218, 0.27; 68 (135): 0.394 / 0.327, 0.30; 88 (175): 0.337 / 0.217, 0.33; 113 (225): 0.227 / 0.111, 0.28; 123 (245): 0.220 / 0.116, 0.30. Sixty rotations: `bw` 53: argmax-only worst 2.654 (1.55 hc) median 1.683, tri-quadratic worst 0.882 (0.51 hc) median 0.318 p95 0.593; 60: 2.228 (1.49) / 1.451, 0.524 (0.35) / 0.246 / 0.495; 63: 2.105 (1.46) / 1.350, 0.739 (0.51) / 0.272 / 0.631; 68: 1.980 (1.49) / 1.290, 0.683 (0.51) / 0.246 / 0.567; 68 `m-3m`: 1.631 (1.22) / 1.056, 0.544 (0.41) / 0.239 / 0.475; 68 `6/mmm`: 1.863 (1.40) / 1.050, 0.554 (0.42) / 0.267 / 0.518; 16 (100 rotations): 7.873 (1.40) / 5.044, 2.712 (0.48) / 1.035 / 2.118; 24: 5.539 (1.48) / 3.580, 1.855 (0.49) / 0.701 / 1.685. `|x| > 1` occurred 0 times in 460 correlations (max `|x|` 0.563), the exact `x` check gives the same results as the faithful one everywhere.
- **Point groups** (three rotations x `bw` 53-63, worst reduced deg / half cells; worst unreduced): `112`: 0.758 / 0.53 (`bw` 63); 179.98. `11m`: 0.754 / 0.44; 0.75 (no reduction needed). `2/m`: 0.702 / 0.43; 179.99. `3`: 0.666 / 0.46; 120.36. `4`: 0.711 / 0.46; 179.95. `4/m`: 0.651 / 0.38; 179.98. `6`: 0.588 / 0.41; 179.97. `6/m`: 0.680 / 0.45; 179.98.
- **Masked (wedge) patterns**, `testNCorr` recipe, three rotations, normalised / un-normalised worst deg: 53: 1.269 / 1.274; 54: 0.476 / 0.454; 55: 0.407 / 0.413; 56: 0.369 / 0.381; 57: 0.487 / 0.486; 58: 0.548 / 0.532; 59: 0.605 / 0.600; 60: 0.376 / 0.380; 62: 1.052 / 1.049; 64: 0.415 / 0.416; 68: 1.151 / 1.152; 88: 0.560 / 0.559; scores normalised 0.50-0.68, un-normalised 0.31-0.47. Groups (`bw` 53-63 x 3): normalised / un-normalised worst in half cells `112` 0.95 / 0.99, `11m` 0.68 / 0.68, `2/m` 0.68 / 0.69, `3` 0.79 / 0.79, `4` 0.72 / 0.72, `4/m` 0.99 / 0.99, `6` 0.67 / 0.68, `6/m` 0.63 / 0.63.
- **Glide and neighbourhood**: full-cube glide identity, even `slP`: 1.16e-16 (`bw` 16), 2.52e-16 (60); odd `slP` with shift `(slP -+ 1)/2`: 0.75 (17), 0.22 (63) -- no on-grid identity. Neighbourhood at `k0 = 0` / `bwP - 1`, worst over 20 blocks, relative to `max|xc|`: faithful (C++) 0.324 / 0.354 (`bw` 16), 0.349 / 0.254 (17), 0.106 / 0.102 (60), 0.0879 / 0.0915 (63), 0.106 / 0.0986 (53); per-plane exact glide (`emsphinx_compatible=False`) 7.95e-17 / 6.51e-17 (16), 3.15e-17 / 3.94e-17 (60); per-plane `(slP-1)/2` shift (odd) 1.75e-2 / 0.153 (17), 1.81e-3 / 7.22e-2 (63), 1.95e-3 / 5.71e-2 (53); a bilinear odd-`slP` variant 9.6e-2 / 8.0e-2 (17), 2.5e-2 / 1.7e-2 (63), 2.5e-2 / 2.3e-2 (53) -- not adopted; interior `k0`: 0.0. Near-degenerate rotations `(0.7, beta, -1.9)` with `beta` in cells of `2 pi/slP` (`0.15`, `pi - 0.15`, `1e-9`, `0.45`; two independent draws of `flm` where a range is given), misorientation deg (argmax cell / faithful / per-plane [/ bilinear]): `bw` 16: 2.10 / 2.72 / 1.17; 3.20 / 2.05 / 0.83; 1.26 / 2.06 / 0.064; 5.22 / 5.50 / 1.73 (half cell 5.6). `bw` 17: 4.39-4.58 / 3.62-4.04 / 3.43-3.70 [2.57]; 4.10 / 2.24-2.26 / 0.75-0.93 [1.37]; 5.86 / 3.56-3.65 / 5.29-5.36 [3.18]; 2.24 / **6.28** / 1.11 [0.73] (half cell 5.5). `bw` 60: 0.51 / 0.96 / 0.19-0.28; 1.13 / 0.58-0.59 / 0.23-0.31; 0.25 / 0.84-0.92 / 0.008-0.009; 1.37 / **1.63** / 1.05 (half cell 1.5). `bw` 63: 1.47 / 0.64-0.66 / 0.73-0.76 [0.59]; 0.90 / 0.53-0.54 / 0.30-0.32 [0.42]; 1.80 / 0.43-0.44 / 0.89-0.92 [0.71]; 1.09 / **1.39** / 0.28 [0.21] (half cell 1.44). `bw` 53: 1.95 / 1.81 / 1.09 [0.69]; 1.62 / 0.62 / 0.25 [0.22]; 2.30 / 1.77 / 1.48 [0.82]; 1.54 / **2.34** / 0.41 [0.24] (half cell 1.71). Bold: faithful worse than half a cell (and worse than no interpolation).
- **Index <-> Euler**: round trip exact on the stored half for `slP` 32, 33, 105, 120, 135; random-rotation `euler -> index -> euler` worst 10.05 / 11.38 / 2.97 / 2.82 / 2.62 deg at `slP` 32 / 33 / 105 / 120 / 135 (bound `sqrt(3) 180/slP` = 9.74 / 9.45 / 2.97 / 2.60 / 2.31; ratios 1.03 / 1.20 / 1.00 / 1.08 / 1.14). Synthetic quadratics recovered to 12 digits; `x[2]` demo: fitted `(0.2, 0.1, 1.5)` accepted by the faithful bound (0.200) and rejected by the exact one (1.5); `(1.5, 0.1, 0.2)` rejected by both.
- **Timing** (best of 5-7, JIT warm, single thread), port `correlate` / C++ `compute + findPeak + interpPeak` / C++ `refinePeak` (ms): `bw` 53 `n_fold` 1: 12.0-12.2 / 8.9 / 2.5; `m-3m`: 6.6 / 3.3 / 0.6; `bw` 68: 25.6-27.1 / 16.3 / 5.2; `m-3m` 13.7-20.1 / 6.9 / 2.1; `bw` 88: 100-103 / 48.1 / 12.3; `m-3m` 43-44 / 18.9 / 2.7; `bw` 113: 242 / 90.4 / 29.4; `m-3m` 93 / 34.2 / 5.3. C++ `compute` alone 8.6 / 3.0, 15.6 / 6.3, 46.7 / 17.7, 87.8 / 31.4; C++ constructor (`Patient` planning) 7 / 17 / 33 / 67-75 ms. Port stages at `bw` 53 / 68 / 88 / 113, `n_fold` 1: kernel 3.0 / 7.9 / 40.3 / 105.7, separable inverse 9.0 / 17.3 / 60.6 / 136.5, full `irfftn` 11.9 / 23.4 / 85.3 / 184.1, peak 0.8 / 1.7 / 4.8 / 9.2; `m-3m`: kernel 0.7 / 1.7 / 8.0 / 20.7, inverse with plane skipping 5.3 / 11.1 / 33.5 / 71.8, full 12.6 / 24.0 / 79.1 / 186.9. Radix check (separable inverse, `n_fold` 1): `slP` 112: 10.2 ms, 117 (`9 x 13`): 11.1-12.4, 120: 10.5, 121 (`11^2`): 12.8, 125: 12.0. Ni autocorrelation `compute` at `bw` 68: 45.5 ms first call after construction (cold caches).
- **Memory** (`tracemalloc`, MB, drafting probe with the *non-in-place* normalised constructor and without `reset_peak` between construct and correlate -- superseded by the review re-measurement below): 63: 41.6 / 26.1 / +23.6 and 65.1 / 33.6 / 65.1; 68: 52.2 / 32.3 / +29.7 and 81.9 / 42.3 / 81.9; 88: 113.4 / 70.3 / +64.7 and 178.1 / 91.8 / 178.1; 113: 240.6 / 149.0 / +137.3 and 377.9 / 194.8 / 377.9; analytic `fxc` / `xc` / table 15.8 / 7.9 / 2.0, 19.8 / 9.9 / 2.5, 43.1 / 21.6 / 5.5, 91.5 / 45.8 / 11.5.
- **Ni master, `bw` 68**: `emsphinx_compatible=True`: `n_fold` 4, mirror, `a_00 = -3.1580`, `sum |alm|^2 = 11.2098`, doubled power 12.1287; `False`: `a_00 = -0.0029`, 5.9770. Autocorrelation: argmax `(0, 106, 106)`, `zyz = (193.12, -180.00, 193.12)` deg, `x = (0, 0.169, 0.169)`, interpolated peak 12.1861 (1.0047 power; the ridge and the faithful glide), reduced angle to identity 0.0000; `max xc / power = 1.000000`, `min = 0.8055`; grid points with `xc >= 0.999 / 0.99 / 0.98 / 0.95 / 0.9 power`: 135 / 405 / 405 / 1089 / 2226 with worst reduced angle to identity 0.000 / 0.667 / 0.667 / 1.491 / 2.667 deg and worst nearest-grid-point distance to a proper operator 180 / 180 / 180 / 89.3 / 1.995 deg (the operators with `beta = 90` deg sit between cells at odd `slP`, so only the 0.9 threshold covers them). Per operator (orix `O` order): nearest-cell value / power 0.943, 0.916, 0.968, 0.962 (z rotations at `k` 67), 0.966, 0.973, 0.966, 0.947, 0.971, 0.940, 1.000, 0.992 (`k` 0), 0.941, 0.925, 0.941, 0.947, 0.959, 0.966, 0.959, 0.941, 0.959, 0.941, 0.959, 0.966; interpolated peak / power 0.949-1.005; reduced angle of the interpolated peak 0.238, 0.192, 0.965, 0.667, 0.220, 0.157, 0.220, 0.159, 0.068, 0.258, 0.000, 0.150, 0.222, 0.160, 0.224, 0.159, 0.271, 0.220, 0.267, 0.222, 0.271, 0.224, 0.267, 0.220 deg. Rotated master, five rotations: worst 0.3702 deg, peak / power 0.99371.
- **Ni master, `bw` 64** (`slP` 128): power 12.0097; grid points `>= 0.999 / 0.99 / 0.98 / 0.95 / 0.9`: 1024 / 1024 / 1024 / 1040 / 3088, reduced angle worst 0.000 / 0.000 / 0.000 / 0.000 / 2.812, coverage worst 90 / 90 / 90 / 0.000 / 0.000; per operator: 8 at 1.00000 (identity and `Rz(90/180/270)` at `(64, 32, m)`, the four `beta = pi` 2-folds at `(0, 96, m)`), 16 at 0.97804 (`beta = 90` deg, `k` 32); interpolated peaks: `x = 0` and 0.97804 for the `beta = 90` sixteen, 1.00000 for identity and `Rz(270)`, **1.01239 with `x = (0, -0.049, 0.5)` and reduced angle 1.268 deg for `Rz(90)` and `Rz(180)`** (even-`slP` glide defect: `m0 + 1 = 65 = bwP` -> `65 - 65 = 0` instead of the exact `1`), 1.00293 / 0.292 deg for two equatorial 2-folds, 1.00738 / 0.000 for two more.
- **D7 gate** (`bw` 68, four rotations; normalised misorientation deg / score, `True` then `False`; then the un-normalised on the same masked `gln`): wedge (fraction 0.214): 0.479 / 3.757, 0.118 / 3.908, 0.487 / 3.713, 0.169 / 3.931 vs 0.457 / 1.465, 0.163 / 1.431, 0.190 / 1.406, 0.159 / 1.436; un-normalised 0.468 / 2.560, 0.164 / 2.601, 0.192 / 2.517, 0.142 / 2.595 vs 0.455 / 2.194, 0.170 / 2.095, 0.192 / 2.096, 0.156 / 2.084; per-rotation score differences +2.29 / +2.48 / +2.31 / +2.50, misorientation differences +0.02 / -0.04 / +0.30 / +0.01, un-normalised score ratios 1.17-1.25. Cap 55 deg (0.213): 0.330 / 3.767, 0.528 / 3.760, 0.305 / 3.850, 0.117 / 3.804 vs 0.473 / 1.419, 0.085 / 1.470, 0.308 / 1.371, 0.217 / 1.467; un-normalised 0.471 / 2.564, 0.102 / 2.558, 0.316 / 2.521, 0.210 / 2.541 vs 0.472 / 2.128, 0.088 / 2.210, 0.307 / 1.977, 0.218 / 2.177; misorientation differences -0.14 / +0.44 / -0.00 / -0.10 deg. **Outcome: the argmax stays within the coarse tolerance in both settings, the normalised score differs by a factor ~2.6 (the DC term); the default `emsphinx_compatible=True` stands.**
- **Table validation and flag exactness** (probe re-check): transposed table slot `[1, 0, 1] = -0.7071067811865476 = wigner_d_half_pi(1, 0, 1)`, untransposed `+0.7071067811865476`, at `bw` 2 / 3 / 16; slot `[0, bw-1, 0]` NaN in both layouts. `bw` 16: conforming `flm` -> `compute(., 4, True) == compute(., 1, False)` bitwise (0.0); violating `flm` -> `compute(flm, ., 4, True) == compute(flm_masked, ., 1, False)` bitwise (0.0) and differs from `compute(flm, ., 1, False)` by 0.894 relative.
- **`.py_func` and sentinels**: `_xcorr_spectrum.py_func` bitwise equal to the compiled kernel at `bw` 8 / 9 / 12 (`n_fold` 1 / 3 / 2, mirror F / T / F; 2 / 1 / 4 ms interpreted); `1e300` in every NaN table slot: `fxc` bitwise unchanged; `_interpolate_maxima.py_func`, `_extract_neighborhood.py_func`, `_find_peak.py_func`, `_scale_and_find_peak.py_func` bitwise equal to the compiled kernels (generic blocks; the drafting probe's `_interpolate_maxima` carried the default error model -- see the review block for the flat-block behaviour).

### 2026-08-17 -- spec-review re-measurements (same environment, same drafting probe unless stated)

- **`error_model`** (numba 0.65.1): `@njit(cache=True, nogil=True) def div(a, b): return a / b` -> `div(0.0, 0.0)` raises `ZeroDivisionError`; the same kernel with `error_model="numpy"` -> `nan` (`div(1.0, 0.0)` -> `inf`). The drafting probe's `_interpolate_maxima` (default model) **raises** on `np.full((3, 3, 3), 2.5)`; an `error_model="numpy"` build returns `2.5` with `x = (0, 0, 0)` (the C++ path: NaN steps, no convergence, `:1350` reset), and the two builds agree bitwise (value and `x`) on 200 random `N(0, 1)` blocks. `.py_func` on NumPy arrays returns `2.5, x = 0` with six `RuntimeWarning: invalid value encountered in scalar divide`; with Python `float`s it raises. `correlate` of an `a_00`-only spectrum with itself (`bw` 8): `xc` constant 1.0 (ptp 0.0), argmax flat index 0 = `(-90, -180, -90)` deg, 180 deg from the identity, and the drafting probe raised `ZeroDivisionError` in the interpolation -- the old doctest example was doubly wrong. `targetoptions` of the `numpy`-model kernel: `{'nogil': True, 'error_model': 'numpy', 'nopython': True, 'boundscheck': None}`; without the flag the key is absent.
- **`extractNeighborhood<1>` enumeration** (pure-Python literal of `:505-544`, `k0 in {0, bwP - 1}`, all `(n0, m0)`): `slP` 32 / 108 / 128 (`bwP` 17 / 55 / 65): triples with an `m` slot of `slP` 64 / 216 / 256, with an `n` slot of `slP` 64 / 216 / 256, reading past `size = bwP slP^2`: **35 / 111 / 131**, maximum overrun `slP + 1` = 33 / 109 / 129 elements; the past-the-end set is exactly `k0 = bwP - 1` with `n0 = bwP - 2` (any `m0`; three slots each) plus `(bwP - 1, n0, bwP - 2)` for `n0 in {0, bwP - 3, slP - 1}` (one slot each); `slP` 33: none. Probability that 20 random `(n0, m0)` at `k0 = bwP - 1` include a past-the-end block: 50 % at `slP` 32, 16 % at 120 -- the drafting probe (numba, no bounds check) read whatever followed the buffer there; the values re-measured below use the clamp.
- **Neighbourhood, clamped faithful vs full cube** (`bw` 16 / 60, seed 5 *per bandwidth*, rotated-copy `gln`; relative to `max|full|`): `k0 = 0`: 0.324 / 0.091 over 20 blocks (0 / 0 blocks with past-the-end reads), exhaustive over all `slP^2` blocks 0.502 / 0.124; `k0 = bwP - 1`: 0.354 / 0.082 over 20 blocks (0 / 0 hit), exhaustive 0.425 / 0.110 (35 / 123 blocks with clamped reads); per-plane exact glide 7.95e-17 / 2.10e-17 and 6.51e-17 / 2.80e-17. The pinned blocks: `(bwP - 1, bwP - 2, 0)` has 3 clamped slots, `(bwP - 1, 0, bwP - 2)` has 1, at both sizes.
- **Odd-`slP` per-plane block error is data-dependent** (`bw` 17 / 63, `k0 = 0` / `bwP - 1`, worst of 20 blocks): rotated-copy `gln`, seeds 5, 0, 1, 2, 3: 0.023 / 0.182, 0.021 / 0.167, 0.019 / 0.342, 0.015 / 0.191, 0.023 / 0.264 (`bw` 17) and 0.002 / 0.062, 0.002 / 0.052, 0.002 / 0.048, 0.002 / 0.045, 0.002 / 0.058 (`bw` 63); *unrelated* white `gln`: 0.102 / 0.609, 0.057 / 0.517, 0.074 / 0.772, 0.088 / 0.919, 0.065 / 0.706 (`bw` 17) and 0.019 / 0.555, 0.017 / 0.561, 0.014 / 0.966, 0.021 / 0.603, 0.021 / 0.516 (`bw` 63) -- hence the structural assertion.
- **Memory re-measured** (`tracemalloc`, MB, `reset_peak()` before the second `correlate`; un-normalised: construct + first `correlate` peak / resident / recurring second-`correlate` peak (transient); normalised with the in-place `rDen` of D8: construct peak / resident / recurring `correlate` peak): `bw` 63: 41.6 / 26.1 / 49.7 (+23.6), 49.3 / 33.6 / 57.2; **68: 52.2 / 32.3 / 62.1 (+29.7), 62.1 / 42.3 / 72.0**; 88: 113.4 / 70.3 / 134.9 (+64.7), 134.9 / 91.8 / 156.5; 113: 240.7 / 149.4 / 286.7 (+137.3), 286.3 / 194.8 / 332.1; analytic `fxc + xc + table` 25.6 / 32.3 / 70.1 / 148.8, `+ rDen` 33.5 / 42.2 / 91.7 / 194.6 -> recurring peak 1.92-1.94x (un-normalised), 1.71x (normalised), transient exactly `fxc + xc` (1.00x). **Full `irfftn` `[:bwP]` view instead of the separable inverse**: transient +15.6 / +19.7 / +42.9 / +91.1 (= the real cube, 0.66x `fxc + xc`: `irfftn` transforms in place along axes 0-1), recurring peak 49.1 / 61.8 / 134.5 / 285.5 -- indistinguishable from the separable 49.7 / 62.1 / 134.9 / 286.7 -- but resident 33.4 / 42.1 / 91.6 / 194.4 (1.30x analytic; `xc.base is not None`); `np.ascontiguousarray` of that view is the view itself (the leading-axis slice is contiguous), so a copying variant is byte-for-byte the separable footprint. Bare `scipy.fft.irfftn` on a random `(135, 135, 68)` complex array: transient 19.69 MB for a 19.68 MB result.
- **Doctest candidate** (`bw` 8, `slP` 15, `rng = default_rng(0)`, `flm = triu(normal + 1j normal)`, `flm[0]` real, `zyz0 = index_to_euler((3, 5, 7), 15) = (1.36135682, -1.88495559, 0.52359878)`): `correlate(flm, rotate_harmonics(flm, zyz0), 1, False)` -> `zyz - zyz0 = (0, 0, 0)`, `x = (1.2e-16, -4.2e-17, 9.0e-17)`, `score / power = 1.0000000000000002` (power 108.18156533840147), `euler_to_index(zyz, 15) == (3, 5, 7)`; the same for cells `(4, 2, 11)`, `(2, 9, 1)` and seeds 0-4 (15 cases, all exact, `score/power` 1.000).
- **Pipeline cost** (warm, wall time): `bw` 53: `random_pair_on_grid` 14 ms, constructor 1 ms, `rotate_harmonics` 1 ms, `correlate` 13 ms, `misorientation_deg` 11 ms (first orix call) ; `bw` 63: 14 / 2 / 2 / 41 / 1 ms; a wedge case (`ncorr_case`: constructor with two `compute`s + 3 x (rotate, synthesize, analyze, normalised + un-normalised `correlate`)) 213 ms at `bw` 53, 373 ms at `bw` 63.
- **Line references** re-derived against 60f3517 (`include/sht/sht_xcorr.hpp` unless stated): size members `:155-168`; `fxc`/`xc` allocation `:381-382`; `Constants` ctor `:361-370` (`dTable` `:368`); `compute` `:658-858`, flags `:699-705`, systemic-zero tables `:709-726`, `fm` loop `:736-744`, `gn` `:762`, column loop `:766`, mirror branch `:773-783` (dead negation `:783`), plain branch `:784-793`, `k % 2` `:794`, four slots `:796-817`, systemic-zero writes `:818-829`, zero rows `:831-837`, pad `:854`, FFT call `:857`; `findPeak` `:862-876`; `NormalizedCorrelator` `:237-270`, `correlate` `:1140-1159` (`:1146-1154`, `:1157`), `Constants` `:1182-1204` (`compute`s `:1191`/`:1194`, `s2m` `:1197`, `rDen` `:1200-1203`), "still needs to be divided" `:255`/`:1138`; `interpolateMaxima` `:1261-1366` (`/ det` `:1319-1325`, reset `:1350`); `extractNeighborhood` `:505-544` (glide `:527-533`, alpha `:530`, gamma `:531`); `util/fft.hpp` `howmany = vH` `:630`, `SepRealFFT3D<double>::inverse` `:671-678`; `test/sht/sht_xcorr.cpp` `makeNFold` `:124`, `dim` `:152`/`:218`, exact zeroing `:158-166`, `ref = true` `:192`/`:275`, `eps` `:294` (= 4.921e-3 deg), sizes `:295-298`, `epsN` `:316`, group `eps` `:345` (= 0.3508 deg); `include/modality/ebsd/idx.hpp` `flm2` `:275-281`; `include/idx/indexer.hpp` `sum2` `:73`, `:315`.

(implementation results follow)
