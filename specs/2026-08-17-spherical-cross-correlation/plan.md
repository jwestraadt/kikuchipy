# Phase 4 -- `spherical-cross-correlation`: plan

Branch `spherical-cross-correlation` off `develop` (01fef614, after the Phase 1-3
merges). Models: this plan and the spec on Fable 5 (xhigh, ultracode); tests,
implementation, adversarial review and fixes by Opus 5 agents (xhigh,
ultracode). Every task group is independently reviewable; the order below is
the implementation order. Tests are written (failing) before the code they
exercise. Numba caches: the new kernels are `cache=True`, so run the new tests
once with `-n 0` before `-n 4`. The drafting probe (a scratch NumPy/Numba port
plus a compiled C++ driver against the EMSphInx headers) that produced every
number in `requirements.md`/`validation.md` is **not** committed; its recipe is
in `validation.md` "Recorded results" so a reviewer can rebuild it.

## 0. Constitution amendments (applied 2026-08-17 in the spec commit)
1. `specs/roadmap.md`, Phase 4 boxes: rename the first box to "`_xcorr.py`: `SphericalCrossCorrelator` (spectrum kernel, separable `scipy.fft` inverse with `m % n_fold` plane skipping, `findPeak`, 27-neighbourhood + glide, tri-quadratic interpolation, `index_to_euler`/`euler_to_index`, `clone()`), `NormalizedSphericalCrossCorrelator` (Huhle `rDen` computed once, fused `xc *= rDen`/argmax); `refine=True` raises until Phase 7; `extractBunge` **not ported** (no consumer before Phase 9; reversed `zyz2eu` offsets -- use `_euler.bunge_to_zyz` there)"; extend the test box with "two analytic oracles (Phase 3 `wigner_D` triple sum, `rotate_harmonics` inner product), glide identity on the full `irfftn` cube, C++ `extractNeighborhood` defects pinned in both `emsphinx_compatible` settings, memory (`tracemalloc`) at bw 63/68/88/113".
2. `specs/tech-stack.md`, Numerics, `fast_size` bullet: **drop "Warn when `slP` has a factor 13"** and replace by "the factor-13 penalty of pocketfft is measured at `1.06-1.18x` on the inverse-FFT stage (`slP` 117 vs 120: 11.1-12.4 vs 10.5 ms, i.e. ~4-11 % of a ~17 ms `correlate` at `bw` 57-59) and is recorded in the `SphericalCrossCorrelator` docstring, not warned about (`bw` 59 is a recommended `fast_bandwidths()` value with `slP` 117 = `fast_size(117)`)".
3. `specs/tech-stack.md`, Numerics, quirks bullet: extend the list "(2x master mean over `totW`, quartered corner weights, `interpPeak` `x[2]` bounds bug, ...)" with "`extractNeighborhood` glide defects (per-slot alpha/gamma shift and the even-`slP` off-by-one, `sht_xcorr.hpp:527-533`; found in Phase 4)", and state that the correlator's single keyword `emsphinx_compatible=True` covers both the `x[2]` check and the neighbourhood glide, `False` giving the full bounds check and the per-plane glide (exact for even `slP`, half-cell approximation for odd).
4. `specs/tech-stack.md`, Numerics, Euler bullet: append "the correlator's `xc[k, n, m]` layout is beta (slowest), gamma, alpha (fastest) with `k < bwP = slP//2 + 1` (`beta in [-pi, 0]`); `xc(zyz) = <rotate_harmonics(flm, zyz), gln>` (real inner product, `m > 0` doubled), so `correlate(flm, rotate_harmonics(flm, zyz0))` returns `zyz0`; the master's z symmetry composes on the crystal side, i.e. on the *left* of `~Rotation(zyz_to_quaternion(zyz))`, which is where `orix.Orientation(data, symmetry)` puts it -- every symmetry-reduced test metric is `Orientation((~Rotation(zyz_to_quaternion(.))).data, symmetry).angle_with(...)`".
5. `specs/tech-stack.md`, Result-contract bullet (`scores`): append "measured in Phase 4: the normalised metric is `xc * rDen` at the peak, `~3.8` for the Ni master with the default `emsphinx_compatible=True` normalisation and `~1.4` with `False` (a factor 2.6 from the master's DC term) while the argmax stays within the coarse tolerance in both -- the D7 gate passed, the default `True` stands".
6. `specs/tech-stack.md`, Performance bullet: (a) **remove "`m % fNf` plane skipping" from the fallback list** ("triggers the fallback list (float32, `m % fNf` plane skipping, coarse-only default)" -> "triggers the fallback list (float32, coarse-only default)"), because D3 implements the plane skipping now as the default inverse (measured 17.3 -> 11.1 ms at `bw` 68 with `n_fold` 4), so only float32 and coarse-only remain for Phases 6/7 to pull; (b) append the measured Phase 4 baseline "coarse `correlate` 12 / 26 / 100 / 242 ms (`n_fold` 1) and 7 / 14-20 / 44 / 93 ms (`m-3m`) at `bw` 53 / 68 / 88 / 113 single thread (compiled C++ 9 / 16 / 48 / 90 resp. 3 / 7 / 19 / 34 ms); memory at `bw` 68 (`tracemalloc`): recurring `correlate` peak 62 / 72 MB (un-normalised / normalised) for the first instance, of which the Wigner table (2.5 MB), `r_den` (9.9 MB) and the spectra are shared by `clone()`s, so each additional thread costs `2 (fxc + xc)` = 59 MB (47 / 129 / 275 MB at `bw` 63 / 88 / 113)".
7. `specs/_research/explore-emsphinx-core-algorithm.md` section 3.4 and section 8: add an erratum/addendum line pointing to this spec's D5 for the two `extractNeighborhood` defects (section 8 item 12 describes only the even-`slP` off-by-one; the per-slot shift, the identical shift of the gamma slot `:531`, and the past-the-end reads at `k0 = bwP - 1` are new) and to D6 for the `eulerIndex` `beta = 0`/odd-`slP` corner and the missing `beta` wrap. Section 3.3's "NumPy equivalent" (`irfftn * slP^3`) is superseded by D3's separable form (`norm="forward"` on `[:bwP]` slices).
8. `specs/tech-stack.md`, Code layout and style, the `@njit(cache=True, nogil=True)` bullet: append "`error_model="numpy"` is added to a kernel **only** where IEEE non-finite semantics are needed to reproduce the C++ (Phase 4: `_interpolate_maxima`, whose unguarded `/ det` must yield NaN and hit the non-convergence reset instead of raising `ZeroDivisionError`); the kernel-flag tests assert its presence there and its absence elsewhere".
9. `specs/tech-stack.md`, Numerics, Euler bullet: extend the wrap list "every beta-taking entry point (`rotate_harmonics`, `wigner_D`, later the correlator's `derivatives`)" with "`euler_to_index` (which also reduces `alpha`/`gamma` into `[-pi/2, 3 pi/2)` before the C++ formulas; the C++ `eulerIndex` has no wrap and no caller)".

## 1. `_xcorr.py` -- tests `tests/test_indexing/test_spherical_xcorr.py`
1. Module header: kikuchipy GPL header + the delimited EMSphInx notice (source `include/sht/sht_xcorr.hpp` and `include/util/fft.hpp:671-678` @ 60f3517, "changed by Johan Westraadt, 2026-08"), the list of ported functions with line ranges (`requirements.md` Scope) and the not-ported list (`refinePeak`, `derivatives`, `denominator`, `extractBunge`); module docstring: the closed form of `compute()` (D4), the layouts (D1), the two `emsphinx_compatible` quirks (D5), the score semantics (D7/D8), memory (D11) and the "not thread-safe, use `clone()`" rule (D9). Three import blocks; `Examples` on `SphericalCrossCorrelator` (doctest gate): a `bw` 8 correlator, a seeded random spectrum (`rng = np.random.default_rng(0)`, `flm = np.triu(rng.normal(size=(8, 8)) + 1j rng.normal(size=(8, 8)))`, `flm[0] = flm[0].real`) correlated against `rotate_harmonics(flm, index_to_euler((3, 5, 7), cor.side_length))`, printing `repr(cor)`, `euler_to_index(zyz, cor.side_length)` -> `(3, 5, 7)` and `round(score / power, 6)` -> `1.0` (an on-grid rotation is recovered exactly, D4; measured for three cells x five seeds). **Not** a one-coefficient (`a_00`-only) function: that spectrum is rotation-invariant, `xc` is constant, the argmax is flat index 0 = `(-pi/2, -pi, -pi/2)`, a 180-degree rotation, and the flat block trips the `det == 0` path (measured).
2. Kernels, all `@njit(cache=True, nogil=True)` (`_interpolate_maxima` additionally `error_model="numpy"`, D5/D12), `.conjugate()` not `np.conj` (numba 0.57 floor), no transcendental functions:
   - `_xcorr_spectrum(flm, gln, table, n_fold, mirror, fxc, fm, gn) -> None` (D2; C++ `:699-854`).
   - `_find_peak(xc) -> int` (`:862-876`; strict `>`, the smallest index among ties), `_scale_and_find_peak(xc, r_den) -> int` (`:1146-1154`, in place, same tie rule).
   - `_extract_neighborhood(xc_flat, slp, bwp, k0, n0, m0, emsphinx_compatible, nh) -> None` (`:505-544`): takes the **flat** `xc` view so that the faithful even-`slP` reads one past a row (`m` slot `slP`) or one past a beta slice (`n` slot `slP`, the same shift is applied to `inds[1]`, `:531`) are legal in-buffer reads exactly as in the C++ (D5); **every** flat offset is bounded with `min(offset, size - 1)` because at `k0 = bwP - 1` the shifted `n` slot reaches past the array end (`(bwP - 1, bwP - 2, any m0)`: three slots, up to `slP + 1` elements past; `(bwP - 1, n0 in {0, bwP - 3, slP - 1}, bwP - 2)`: one slot) -- those values are undefined behaviour in the C++ and are never asserted against it; the clamp is documented in the docstring with the reachable set.
   - `_interpolate_maxima(p, x) -> float` (`:1261-1366`; `error_model="numpy"` so that a flat block follows the C++ NaN path to the `:1350` reset and returns the centre value instead of raising).
3. `_inverse_fft(fxc, n_fold) -> xc` (`bwP = fxc.shape[2]`) (D3, `scipy.fft`, `norm="forward"`, `workers=1`, plane skipping); `index_to_euler(knm, side_length) -> ndarray (3,)`, `euler_to_index(zyz, side_length) -> tuple[int, int, int]` (D6: `_euler.wrap_beta` on `beta` and `alpha`/`gamma` reduced into `[-pi/2, 3 pi/2)` first, then the C++ formulas, `floor(x + 0.5)`, modulo, the `k == bwP` clamp).
4. `class SphericalCrossCorrelator(bandwidth, *, wigner_d_half_pi=None)`: attributes `bandwidth`, `side_length_unpadded`, `side_length`, `half_side_length`, `wigner_d_half_pi` (shared table), buffers `fxc`, `_fm`, `_gn`, `xc` (`None` until the first `compute`); `compute(flm, gln, n_fold, mirror) -> xc` (a fresh caller-owned array per call, `self.xc` rebound to it, D7); `interp_peak(index, emsphinx_compatible=True) -> (zyz, peak, x)` (private-ish helper the tests and Phase 8 use; `RuntimeError` before `compute`); `correlate(flm, gln, n_fold, mirror, *, refine=False, emsphinx_compatible=True) -> (zyz, score)`; `clone()`; `__repr__` (`"SphericalCrossCorrelator: bw = 68, side_length = 135 (unpadded 135), half 68"`); validation of D7/D9. Docstring `Notes`: the C++ defects and the two keyword settings (incl. the clamped past-the-end reads and the unbounded `alpha` under `emsphinx_compatible=True`), the on-grid exactness and the un-normalised score semantics, the pocketfft factor-13 note, memory formula and measured peaks (recurring 62 MB at `bw` 68, `2 (fxc + xc)` per additional clone), thread rule, `compute` returning a fresh array.
5. `class NormalizedSphericalCrossCorrelator(bandwidth, flm, flm2, n_fold, mirror, mlm, *, wigner_d_half_pi=None)`: attributes `correlator` (a `SphericalCrossCorrelator`), `flm`, `flm2`, `mlm`, `n_fold`, `mirror`, `r_den` (`(bwP, slP, slP)` float64, computed in place per D8), `bandwidth`/`side_length`/`half_side_length` properties; `correlate(gln, *, refine=False, emsphinx_compatible=True) -> (zyz, score)`; `clone()`; docstring `Notes` on the C++ metric ("semi-normalised", not divided by the pattern norm, `Indexer::sum2` dead), the D7-gate numbers, the radicand remark of D8 (negative -> NaN slot that loses the argmax; exactly zero -> `+inf` that *wins* it -- a caller error, unguarded as in the C++), and that `mlm` is `SHT(binary window)` so `s2m = mlm[0, 0].real sqrt(4 pi)`.
6. `src/kikuchipy/indexing/_spherical/__init__.py`: add ``_xcorr`` ("SO(3) cross-correlation of two spherical functions from their harmonic coefficients, and its peak") to the `Submodules` block, alphabetically after `_wigner`; nothing imported.

## 2. Tests (`tests/test_indexing/test_spherical_xcorr.py`) -- exact assertions in `validation.md`
1. Helpers (module level, deterministic): `random_pair_on_grid(bw, n_fold, mirror, rng)` (the C++ `randomSphere`/`randomPair` recipe via `SphericalHarmonicTransform(bw, "legendre", dim)` with `dim = bw + (3 if bw % 2 == 0 else 2)`, `_grid.ring_number`/`n_rings` for `matchEquator`, exact zeroing of the rows `m % n_fold != 0`; `MasterPattern::makeNFold` deliberately not ported, D10), `random_zyz(rng)` (`(|u|, u, u, u)` normalised -> `quaternion_to_zyz`), `random_alm(bw, rng, n_fold, mirror)` (direct spectra for the cheap oracles), `wedge_mask(dim)` (`sht_xcorr.cpp:218-232`: `theta in [-30, 60]` deg on the north hemisphere, additionally `z < cos 45 deg` on the south), `cap_mask(dim)`, `misorientation_deg(zyz_true, zyz_found, name)` (D4 metric via `orix.quaternion.symmetry._groups` name lookup, `C1` for `"1"`), `full_cube(cor)` (`irfftn` oracle), `inner_product(a, b)`, `triple_sum_oracle(flm, gln, zyz)`, `KERNEL_NAMES` asserted equal to the module's `CPUDispatcher` members.
2. Classes: `TestSizes` (D1 table, `bwP`, layouts, `ValueError`s, table sharing/tripwire), `TestSpectrumKernel` (triple-sum and inner-product oracles; systemic zeros exactly zero; NaN slots never read via the `1e300` sentinel; buffer reuse across `(n_fold, mirror)` changes equals a fresh instance; the pad-region garbage test; `mirror` and `n_fold` flags each change `fxc` on non-conforming input, so ignoring one is caught; `compute` returns a fresh array each call), `TestInverseFFT` (separable == `irfftn[:bwP]`; plane skipping == no skipping when the planes are zero, `4 eps`; `norm="forward"` pinned by the peak == power identity; the FFT-call recording test: names and `workers`), `TestGlide` (full-cube identity even `slP`; the odd-`slP` non-identity recorded; neighbourhood faithful vs per-plane vs full cube at `k0 in {0, bwP - 1}` -- exact for even `slP`, structural for odd; interior exact; the even-`slP` out-of-row and out-of-slice reads reproduced from the flat buffer and the past-the-end slots clamped; `_find_peak`/`_scale_and_find_peak` tie-breaking and NaN semantics), `TestInterpolation` (synthetic quadratics; `x[2]` bug both modes; the `x[0]` and `x[1]` over-steps; non-convergence reset; the flat `det == 0` block on the compiled kernel and, under `np.errstate`, on `.py_func`), `TestIndexEuler` (round trips, glide, `beta = 0` corner, `floor(x + 0.5)` vs banker's rounding at `.5`, unwrapped `beta`/`alpha`/`gamma`), `TestCorrelate` (C++ size list default subset + weekly full; eight point groups at `bw in {53, 60, 63}` default, `53..63` weekly, three rotations each; near-degenerate `beta` in both keyword settings; `refine=True` raises; ranges of `zyz` per keyword), `TestNormalized` (wedge mask over the size subset and the groups at `bw in {53, 60, 63}` default, `53..63` weekly; `rDen` finite/positive for the wedge and the cap; `clone()` shares `r_den` and gives identical results; scores recorded), `TestNickel` (autocorrelation, 24 operators at `bw` 68 and 64, rotated recovery, D7 gate with both masks), `TestKernels` (`.py_func`, flags), `TestBaselines` (timing/memory `record_property`, loose bounds).
3. Marks: `@pytest.mark.weekly` for `bw in {88, 113, 123, 158}` of the size list, the point-group sweeps at `bw in 53..63` minus `{53, 60, 63}` (plain and normalised), the `bw` 68 inner-product oracle and the memory row at `bw` 113; everything else in the default suite. Recount of the default set (`compute`/`correlate` calls): symmetry-free 11 x 3 = 33, groups 8 x 3 x 3 = 72, near-degenerate 3 x 4 x 2 = 24, wedge 11 x (2 + 3 + 3) = 88, normalised groups 8 x 3 x (2 + 1 + 1) = 96, D7 gate 2 x 2 x (2 + 4 + 4) = 40, Ni 7, oracles/glide/flags/`.py_func` at `bw <= 32` ~60, memory ~15, timing 24 -- **~460 calls, ~380 of them at `bw` 53-68** (measured per case incl. setup: `random_pair_on_grid` 14 ms, `rotate_harmonics` 1-2 ms, `correlate` 13-41 ms, `misorientation_deg` 1-11 ms at `bw` 53 / 63; a wedge case with three rotations 213 / 373 ms), i.e. roughly 20-30 s of test time single-process plus imports and the two `from_master_pattern` calls -- under a minute at `-n 4`, re-measured and recorded with the implementation.

## 3. Adversarial review and fixes
1. Ultracode workflow (Opus 5): reviewers refute `_xcorr.py` line by line against `sht_xcorr.hpp` (`fm`/`gn` table indices `[m, k, j]` vs `[k, n, j]`, `start = max(m, k, n)` and the mirror `start` bump, `toggle` seed `(start + m) % 2`, the `k % 2` negation of `vnc`, the four slots and the `match` sign, systemic-zero and zero-pad writes incl. the inclusive `slP - bw` slice, `bwP = slP//2 + 1`, the separable inverse order (k, then n on `[:bwP]`, then c2r along m), `norm="forward"`, plane skipping on the `m` axis only, `findPeak` `>` semantics, `extractInds` order `(k, n, m)`, the periodic wrap, the per-slot glide and even-`slP` off-by-one in faithful mode and the per-plane glide in exact mode, all 27 `a_{kji}` coefficients and the Newton loop of `interpolateMaxima`, the `x[2]` bound, the Euler formulas and `floor(x + 0.5)`, `rDen` incl. `s2m = mlm[0,0].real sqrt(4 pi)` and the master flags on both `compute` calls, in-place scaling before `interp_peak`), a conventions reviewer (headers, numpydoc, three import blocks, numba flags, `workers=1`, no public names, no CHANGELOG), and a test-quality reviewer with **bug injection** -- every mutation must be killed by a named test: transposed slots (`fxc[slP-k, n]` <-> `fxc[k, slP-n]`), `vnc` sign for odd `k` dropped, `match` sign inverted, missing zero padding of the slices `k in [bw, slP-bw]` (dies **only** by the pad-region garbage test -- on a `np.zeros` buffer the pad write is a no-op that the buffer-reuse and layout tests cannot see), `slP = scipy.fft.next_fast_len(2 bw - 1)` (dies at `bw` 53: 105 vs `real=True` 108, and at `bw` 57: 117 vs 120), `bwP = (slP + 1) // 2` (dies for even `slP`, `bw` 16/60), missing `norm="forward"` (dies by peak == power), swapped alpha/gamma axes in `index_to_euler` (dies by the triple-sum oracle and the C++ recovery tests), transposed table read `table[k, m, j]` for `fm` (dies by the oracles), `gn` without conjugation, wrong glide (`+ bwP` instead of `+ bwP - 1`, or no glide) (dies by the neighbourhood-vs-full-cube tests), per-plane glide in faithful mode (dies by the faithful-vs-C++-values test at `k0 = 0`), interpolation coefficient off-by-one (`a_012`/`a_021` swapped) (dies by the synthetic quadratic with cross terms), Newton reset missing (dies by the non-convergence test), `error_model="numpy"` dropped from `_interpolate_maxima` (dies by the flat `det == 0` block test: `ZeroDivisionError` instead of the centre value), `x[2]` bug "fixed" in faithful mode (dies by the `x = (0.2, 0.1, 1.5)` test), the `x[1]` term dropped from the bound (dies by the `(0.1, 1.5, 0.2)` test), `>=` instead of `>` in `_find_peak`/`_scale_and_find_peak` (dies by the duplicate-maxima test: last instead of first index), the `beta` wrap or the `alpha`/`gamma` reduction dropped from `euler_to_index` (dies by the unwrapped-triple test: negative `k` or `n`), the past-the-end clamp dropped from `_extract_neighborhood` (dies by the `(bwP - 1, bwP - 2, m0)` block test, which asserts the clamped slots equal `xc.flat[-1]`; with `boundscheck` off the unclamped read is silent garbage), `compute` returning a view of a persistent buffer instead of a fresh array (dies by the two-results test), `rDen` not squared-rooted / not reciprocal / `s2m` without `sqrt(4 pi)` (dies by the C++-recorded `rDen` values and the wedge recovery), `xc *= rDen` after instead of before the argmax (dies by the wedge test where the two argmaxes differ, and by the score value), `n_fold` skipping applied to `n` rows or to the `k` axis (dies by the oracles with asymmetric `gln`), `mirror` flag ignored (dies by the mirror-flag test), `refine=True` silently coarse (dies by the `NotImplementedError` test), banker's rounding in `euler_to_index` (dies by the `.5` test), `k` clamp missing (dies by the `beta = 0`, `slP` 135 test), each of `cache=True`, `nogil=True` dropped and `parallel=True`, `fastmath=True` added on one kernel, `error_model="numpy"` added to a kernel other than `_interpolate_maxima` (flag test), `workers` dropped or `!= 1`, or a `*n` transform (`irfftn`/`ifftn`) used in `compute` (dies by the FFT-call recording test, which patches the `_xcorr` module namespace **and** `scipy.fft`, asserts at least three recorded calls per `compute`, `workers == 1` on each, and no `n`-dimensional transform), shared buffers between clones (dies by the clone-independence test), `np.argmax` NaN semantics (dies by a NaN-in-`xc` test on `_find_peak`).
2. Fix, re-run the tests (`-n 0` then `-n 4`), `pre-commit run --files ...`, `--doctest-modules`, coverage `>= 95 %` (target 100 % as Phases 1-3, with the dead mirror-branch negation of D2 excluded by `# pragma: no cover` -- the only sanctioned exclusion).

## 4. Commit and PR
1. Signed commits (spec + constitution amendments; failing tests; implementation; fixes); tick `specs/roadmap.md` Phase 4 boxes; push; PR into fork `develop` with the template, stating GPL-only licensing (EMSphInx-derived: BSD opt-out impossible), no CHANGELOG entry (private code).

## 5. Open questions -- Decided 2026-08-17 (autonomous mode, per the user's standing instruction)
The spec was approved with the decisions below; each is frozen in `requirements.md`/`validation.md` as written there and no draft alternative remains open.
1. (a) `error_model="numpy"` on `_interpolate_maxima` only, every other kernel without it (D5, D12; constitution amendment 0.8) -- accepted.
2. (b) `euler_to_index` wraps `beta` with `_euler.wrap_beta` and reduces `alpha`/`gamma` into `[-pi/2, 3 pi/2)` before the C++ formulas (D6; amendment 0.9) -- accepted.
3. (c) Trimmed default point-group sweeps: `bw in {53, 60, 63}` in the default suite, `53..63` weekly, three rotations per (group, bandwidth), plain and normalised (D10, section 2.3) -- accepted.
4. (d) The faithful even-`slP` reads past the end of `xc` at `k0 = bwP - 1` are clamped to `xc.flat[-1]` (`min(offset, size - 1)`), documented with the reachable set and never asserted against the C++ (D5) -- accepted.
5. (e) `emsphinx_compatible=False` uses the per-plane glide, exact for even `slP` and the half-cell approximation `s = (slP - 1)/2` for odd `slP` (structural identity asserted, deviation from the full cube recorded; the bilinear alternative not adopted) (D5) -- accepted.
6. (f) Tiered coarse tolerance rule: `< 180/slP` deg when the true `beta` is at least one cell from `0` and from `pi`, else `< 360/slP`; masked patterns `< 360/slP` for both correlators (D10) -- accepted.
7. (g) The factor-13 `slP` warning is dropped; the measured pocketfft penalty is recorded in the `SphericalCrossCorrelator` docstring instead (amendment 0.2) -- accepted.
8. (h) `clone()` and the shared `wigner_d_half_pi=` table are kept as specified (D9) -- accepted, no change.
