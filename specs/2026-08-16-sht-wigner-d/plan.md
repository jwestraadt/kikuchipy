# Phase 3 -- `sht-wigner-d`: plan

Branch `sht-wigner-d` off `develop` (7c019d52, after the Phase 1 merge; Phase 2
is independent and may land before or after). Models: this plan and the spec on
Fable 5 (xhigh, ultracode); tests, implementation, adversarial review and fixes
by Opus 5 agents (xhigh, ultracode). Every task group is independently
reviewable; the order below is the implementation order. Tests are written
(failing) before the code they exercise; the reference-table module is written
first because the tests import it.

## 0. Constitution amendments (applied 2026-08-16 in the specs PR)
1. `specs/tech-stack.md`, Euler bullet: the bullet already states the offsets ("Bunge equivalent `(alpha + pi/2, beta, gamma - pi/2)`") and the `~Rotation` orientation, so **do not restate them** (a duplicated statement of one convention inside one bullet is the drift that produced EMSphInx' own `zyz2eu` vs `rotations.cpp:296` contradiction); instead name the functions on the existing clause -- "Bunge equivalent `(alpha + pi/2, beta, gamma - pi/2)` (`_euler.zyz_to_bunge`/`bunge_to_zyz`)" -- and append only what is new: "EMSphInx' `zyz2eu`/`eu2zyz` (`rotations.hpp:1025-1039`) have the offsets reversed and are never ported; every beta-taking entry point (`rotate_harmonics`, `wigner_D`, later the correlator's `derivatives`) wraps `beta` into `[-pi, pi]` with `_euler.wrap_beta` (`sht_xcorr.hpp:895-899`) before taking `cos`/`signbit`". Add a Wigner bullet under Numerics: "Wigner d in Fukushima/Mathematica convention (`d^j_{k,m}` = `WignerD[{j,k,m},beta]`, the transpose of Wikipedia's); tables `[k, m, j, s]` (`s`: `beta`, `pi - beta`), `pi/2` table transposed `[m, k, j]` for the correlator; undefined slots NaN; `rotate_harmonics` satisfies `g(n) = f((~R) * n)` with `R = Rotation(zyz_to_quaternion(zyz))`; composition `rotate(rotate(a, z1), z2) == rotate(a, zyz(Q2 * Q1))`; the tables are bitwise equal to the scalar `wigner_d` (shared coefficient helpers, C++ association preserved); the naive recursion (no X-numbers) returns exactly `0.0` where its seed `c2^(k+m) s2^(k-m)` underflows -- `|d| <~ 1e-139` at `bw` 512 for `beta >= 2.5`, nothing lost at `bw <= 128` for `beta <= 3.0` -- pinned by `wigner_d(511, 400, 300, cos 2.5) == 0.0`; Mathematica reference tables live in `src/kikuchipy/data/emsphinx/wigner_reference_tables.py`".
2. `specs/tech-stack.md`, Tests/data bullet: "EMSphInx references generated with the binaries ship as `.npz`; the Mathematica tables of `test/sht/wigner.cpp` ship as the text module `src/kikuchipy/data/emsphinx/wigner_reference_tables.py` (Phase 3)".
3. `specs/roadmap.md`, Phase 3 boxes: name the reference-table module and `wigner_D`; add "table-based derivative formulas of `sht_xcorr.hpp:1009-1041` pinned in `test_spherical_wigner.py` against `wigner_d_prime`/`wigner_d_prime2` (Phase 7 copies them)". Phase 4/9 note: "`extractBunge` (`sht_xcorr.hpp:594-649`) uses the reversed ZYZ->Bunge offsets; if ported, use `_euler.bunge_to_zyz` and record the deviation".
4. `specs/_research/explore-emsphinx-xtal-util-vs-orix.md` 1.2-1.3 and `explore-emsphinx-core-algorithm.md` 3.4: add an erratum line pointing to this spec (the ZYZ->Bunge lines there quote EMSphInx' reversed `zyz2eu`).
5. `doc/user/bibliography.bib`: add `fukushima2016wigner` (see task 3.5); it renders on the Bibliography page immediately (`:all:`), like the Phase 0 entries -- and, like Phase 0, the phase is gated on `uv run sphinx-build -b html doc doc/_build/html` exiting 0 with the entry visible in `doc/_build/html/user/bibliography.html` (`validation.md`); a malformed entry would break the whole public Bibliography page.
6. `specs/tech-stack.md`, Grids bullet (Phase 1 property surfaced by this phase's oracle): append "`synthesize` writes order `m` on ring `y` only for `m < min(bw, 4y + 1)` (`N_phi = 8y` samples), so pointwise oracles against `sph_harm_y` are masked to `4y >= m_max` (Phase 1 synthesize oracle, Phase 3 through-transform oracle); `analyze(synthesize(.))` inverts the same truncation and needs no mask".

## 1. Reference tables (`src/kikuchipy/data/emsphinx/`)
1. `src/kikuchipy/data/emsphinx/__init__.py`: kikuchipy GPL header, docstring "EMSphInx reference data used by the spherical indexing tests" (nothing else; Phase 10 adds `.npz` files here).
2. `src/kikuchipy/data/emsphinx/wigner_reference_tables.py`: kikuchipy GPL header + the EMSphInx notice block (source `test/sht/wigner.cpp` @ 60f3517, "changed by Johan Westraadt, 2026-08: transcribed to Python"); module docstring with the Mathematica expressions (`N[Table[WignerD[{j,k,m}, beta], {j,0,4},{k,-4,4},{m,-4,4}], 32]` for `beta = Pi/2, Pi/3, 2Pi/3`; `D[WignerD[{j,k,m}, beta], {beta, 1|2}]` for `Pi/3, 2Pi/3`; `WignerD[{3,2,1}, Pi/6, Pi/2, Pi/3]`) and the C++ line ranges; constants `BETA_PI_2`, `BETA_PI_3`, `BETA_2PI_3` (as `pi/2` etc.), the seven dicts `D_PI_2`, `D_PI_3`, `D_2PI_3`, `D_PRIME_PI_3`, `D_PRIME_2PI_3`, `D_PRIME2_PI_3`, `D_PRIME2_2PI_3` (`{(j, k, m): value}` for the 165 defined entries each, sorted by `(j, k, m)`, values as the verbatim 32-digit literals), `D_3_2_1`, `NUM = 5`, and `as_array(table) -> np.ndarray (5, 9, 9)` (`[j, k + 4, m + 4]`, NaN where undefined). Generated once with a regex parser over the C++ file (`{ ... }` rows, `NAN`/number tokens; the parser is a throw-away script, its recipe is in this plan, not shipped) and spot-checked by hand against five entries listed in `requirements.md`.
3. Structural tests (in `test_spherical_wigner.py`): each dict has exactly 165 keys, `{(j, k, m): |k| <= j, |m| <= j, j < 5}`; all values finite; the five hand-checked entries; `as_array` puts NaN exactly on `j < max(|k|, |m|)` (240 per table).

## 2. Euler helpers (`_euler.py`) -- tests `tests/test_indexing/test_spherical_euler.py`
1. `wrap_beta(beta: float) -> float`: one `@njit(cache=True, nogil=True)` scalar kernel `_wrap_beta` (`fmod` into `[-pi, pi]`, `sht_xcorr.hpp:895-899`, `-0.0` preserved) and a Python `wrap_beta` calling it, so `_wigner.py` and later kernels share one implementation.
2. `_orient_axis(ax)` (`rotations.hpp:247-260`, in place on a `(3,)` view), `_zyz_to_quaternion_single(zyz, qu)`, `_zyz_to_quaternion_2d(zyz2d) -> (n, 4)`, `_quaternion_to_zyz_single(qu, eu)`, `_quaternion_to_zyz_2d(qu2d) -> (n, 3)`; module constants `_R_EPS = sqrt(eps)`, `_THR = 10 eps`, `_TWO_PI`.
3. Wrappers `zyz_to_quaternion(zyz) -> (..., 4)`, `quaternion_to_zyz(qu) -> (..., 3)` (reshape to 2-D, kernel, reshape back; `ValueError` on a wrong last dimension; `float64` cast), `zyz_to_bunge(zyz)`, `bunge_to_zyz(eu)` (affine, no wrap, any shape `(..., 3)`), `rotation_from_zyz(zyz) -> Rotation` (`~Rotation(zyz_to_quaternion(zyz))`; docstring: provisional sign, pinned by Phase 5), `rotation_to_zyz(rotation) -> (..., 3)` (`quaternion_to_zyz((~rotation).data)`).
4. Tests -- exact assertions in `validation.md`: 1000-random-triple Bunge equivalence (1e-14) and the wrong-offset guard; port of `test/xtal/rotations.cpp:288-318` on the `n = 25` and `n = 15` grids against a test-local transcription `_emsphinx_eu2qu` (`rotations.hpp:410-429`, incl. `orientAxis` and normalisation) at `10 eps`, and against orix `from_euler` as rotations (`min(|q - q'|, |q + q'|)`) with the count of sign-flipped degenerate cases recorded; `_emsphinx_eu2qu == Rotation.from_euler(...).data` bitwise on the random triples; `quaternion_to_zyz` round trips (random, `beta` in `{0, 1e-9, 1e-7, pi - 1e-7, pi - 1e-9, pi}`), branch values, ranges; `w >= 0`, `orientAxis` cases (`zyz = (0, pi, 0)` -> `(0, 0, 1, 0)`; `(pi/2, pi, pi/2)`; `(0, pi, pi)`); `zyz_to_bunge`/`bunge_to_zyz` inverses; `rotation_from_zyz` identities and shapes; `wrap_beta` cases (`4.0 -> 4 - 2 pi`, `-4.0 -> 2 pi - 4`, `pi -> pi`, `-pi -> -pi`, `-0.0 -> -0.0` (signbit), `7.0 -> 7 - 2 pi`); `.py_func` of every kernel; `cache`/`nogil` flags.

## 3. Wigner d (`_wigner.py`) -- tests `tests/test_indexing/test_spherical_wigner.py`
1. Coefficient helpers, all `@njit(cache=True, nogil=True)`, one-liners with the C++ association: `_u_jkm_0(j, k, m, tc)`, `_u_jkm_1(j, k, m)`, `_u_jkm_2(j, k, m, t)`, `_v_jkm`, `_w_jkm`, `_a_jkm_0/1/2`, `_a_jkm_0_pre/_2_pre(w, ...)`, `_b_jkm`, `_u_km_0/1/2`, `_a_km_0/1/2`, `_e_km` (`wigner.hpp:211-286`).
2. `_wigner_d_core(j, k, m, t) -> float` (`:317-370`, `0 <= m <= k <= j`, three-way branch on `t`), `wigner_d(j, k, m, cos_beta, negative_beta) -> float` (iterative reduction of `:300-315`, NaN guard), `wigner_d_half_pi(j, k, m)` (`:380-409`), `wigner_d_sign(j, k, m) -> int` (`:416-425`), `wigner_D(j, k, m, zyz) -> complex` (`:436-439`, with `wrap_beta`).
3. Table kernels writing into caller-allocated arrays: `_wigner_d_table_kernel(bandwidth, t, negative_beta, table)` (`:452-559`; the `isType0` branch selects `t0`/`tN` and the `_a_*_0`/`_a_*_2` pair; powers `pc2[i] = c2 ** float(i)`, `ps2` likewise, `2 bw` each), `_wigner_d_table_factors_kernel(bandwidth, e_km, w_jkm, b_jkm)` (`:678-691`), `_wigner_d_table_pre_kernel(bandwidth, t, negative_beta, table, e_km, w_jkm, b_jkm)` (`:575-671`), `_wigner_d_half_pi_table_kernel(bandwidth, table, transpose)` (`:699-761`), `_rotate_harmonics_kernel(alm, alpha, gamma, table, out)` (`:778-797`, `exp_m_gamma`/`exp_n_alpha` by content, `vp`/`vc` as `:785-790`, the `(-1)^(j+m+n)` slot-1 sign).
4. Wrappers: `wigner_d_table(bandwidth, cos_beta, negative_beta) -> (bw, bw, bw, 2)` (`np.full(nan)`, validates `bandwidth >= 1`, `|cos_beta| <= 1`), `wigner_d_table_factors(bandwidth) -> (e_km, w_jkm, b_jkm)` (NaN-filled), `wigner_d_table_pre(bandwidth, cos_beta, negative_beta, e_km, w_jkm, b_jkm, out=None)` (allocates NaN-filled if `out is None`, else checks shape/dtype/C-contiguity **and** that the representative undefined slots `out[bw-1, 0, 0, 0]`, `out[0, bw-1, 0, 1]` are NaN when `bw >= 2` (`ValueError` otherwise, so `np.empty` buffers are refused; docstring: all undefined slots must already be NaN), writes in place, returns the table), `wigner_d_half_pi_table(bandwidth, transpose)`, `rotate_harmonics(alm, zyz)` (`np.ascontiguousarray(alm, complex128)`, square check, `zyz` -> `(3,)` float64, `beta = wrap_beta(zyz[1])`, `t = cos(beta)`, `negative_beta = signbit(beta)`, table + kernel; docstring `Examples` and the memory sentence), `wigner_d_prime`, `wigner_d_prime2` (`@njit`, `:814-852`, NaN guard first; in `wigner_d_prime2` the `d2Coef` product `rjk * sqrt((j-k-1)(j+k+2))` is evaluated only inside the `k + 1 < j` branch, never unconditionally as `:845` does -- its radicand is negative on every defined `k == j` slot and `math.sqrt` raises in the `.py_func`; `requirements.md`).
5. Docstrings freeze: the convention block (Fukushima eq. 1, Mathematica equivalence, transpose of Wikipedia), the `D` definition, the `b^l_m` sum and direction identity `g(n) = f((~R) * n)`, composition order, table layouts and NaN rule, the beta wrap, memory, `:cite:` to `lenthe2019spherical` (and Fukushima 2016 -- **add a bibliography entry `fukushima2016wigner`** to `doc/user/bibliography.bib` (T. Fukushima, "Numerical computation of Wigner's d-function of arbitrary high degree and orders by extending exponent of floating point numbers", 2016, DOI 10.13140/RG.2.2.31922.20160, as cited by `wigner.hpp:45-46`); the Phase 0 bibliography list did not include it, so this is amendment 5 in section 0 -- a docs-only addition that renders on the public Bibliography page (`:all:`), no CHANGELOG entry as in Phase 0).
6. Tests -- exact assertions in `validation.md`: Mathematica tables (`2 eps`, `24 eps`), `dSign`, `D^3_{2,1}`; symmetry identities; NaN pattern; tables vs scalar bitwise (`bw` 1, 2, 3, 15, 32 default; 68/88/113 weekly), pre vs plain bitwise, `pi/2` table vs `wigner_d_half_pi` bitwise, transposed vs untransposed, `pi/2` table vs `wigner_d_table(bw, 0.0, False)` (1e-15), slot symmetry at `t = 0`, `negative_beta` swap identity, edge `t = +-1` finite; unitarity and the `eval_jacobi` closed form up to `j = 511`; `rotate_harmonics` identity, pure-z phase, glide, wrap, inverse, composition, power conservation, real `m = 0`, `l < m` ignored/zero, **the dependency-free brute-force `sum_n a^l_n wigner_D(l, m, n, zyz)` oracle** (numpy + the module's own `wigner_D`, runs on the oldest CI job) and the `sph_harm_y` direction oracle (random directions, and through `SphericalHarmonicTransform(32, "lambert")` **restricted to `ring_number(65) >= 4`**, the pixels on which Phase 1's Lambert `synthesize` is pointwise exact for `bw`-16 content); the pinned underflow limitation `wigner_d(511, 400, 300, cos(2.5), False) == 0.0` (weekly: the closed-form scan); derivatives vs 5-point finite differences, the Phase 7 formula pinning at `bw` 15, and the four `k == j` slots of `wigner_d_prime2` finite for dispatcher and `.py_func`; real data (Ni master invariances); `.py_func` of every kernel (with the `hasattr` guard); `nogil`/`cache` flags asserted as Phase 1 does (`targetoptions.get("nogil") is True`, `type(kernel._cache).__name__ == "FunctionCache"`) plus the negatives `not targetoptions.get("parallel", False)`, `not targetoptions.get("fastmath", False)`; `ValueError` paths (incl. `out=np.empty(...)`); timing/memory baselines via `record_property` (`wigner_d_table` including its NaN allocation, `wigner_d_table_pre(out=...)` without).
7. `src/kikuchipy/indexing/_spherical/__init__.py`: add ``_euler`` ("ZYZ Euler angle conversions and the beta wrap") and ``_wigner`` ("Wigner (lowercase) d functions, tables and harmonic rotation") to the `Submodules` block of the package docstring, alphabetical (`_euler`, `_fft`, `_grid`, `_sht`, `_wigner`), one line each like the Phase 1 entries (`__init__.py:31-38`); nothing is imported (checked in `validation.md`, Manual).

## 4. Adversarial review and fixes
1. Ultracode workflow (Opus 5): reviewers refute `_wigner.py` line by line against `wigner.hpp` (coefficient association, `isType0` grouping, `sign`/`signN` swap for `nB`, `pc2`/`ps2` indices `k+m`/`k-m` and their `-t` twins, slot-1 sign `(-1)^(j+m+n)`, `expAlpha`/`expGamma` content, `vc` conjugation, `k+1 < jMax`/`k+2 < jMax` guards, transposed `pi/2` indices, `pre` index `k bw^2 + m bw + i`, NaN guards, beta wrap) and `_euler.py` against `rotations.hpp` (`sigma`/`delta` operand order -- the B1 pitfall, `signbit` vs `< 0`, `rEps` vs `thr`, `orientAxis` branches, `qu2zyz` `y1, y2, x1, x2` signs and the `+2 pi` wrap, the `chi <= thr` branches), plus a kikuchipy-conventions reviewer (headers, numpydoc, three import blocks, numba flags, no public names) and a test-quality reviewer with bug injection (at least: transposed `d` (Wikipedia convention), dropped `(-1)^(k-m)` in the mirror slot, `signN` not swapped for `nB`, slot-1 sign `(-1)^(j+m)` instead of `(-1)^(j+m+n)`, `expAlpha` on `alpha`, missing conjugate in `vc`, `k+1 < jMax` off by one, `sigma`/`delta` swapped in `zyz2qu`, `+pi/2` on the wrong angle in `zyz_to_bunge`, ~~`< 0` instead of `signbit`~~ (**equivalent mutation, dropped from the kill list** -- recorded 2026-08-16: `w = cos(beta/2) cos(sigma)` can never be `+-0.0` in float64, `|cos|` of any double is `>= 6.12e-17` and the product cannot underflow, and `zyz2qu` sets `qu[0] = +0.0` in its `|w| <= rEps` branch, so no test can kill it; see `validation.md` Recorded results), missing beta wrap, `w * sqrt * j` re-association, `pi/2` table transpose ignored, `qu2zyz` `beta = pi` branch sign, **transposed `dBeta[n, m, j]` read in `_rotate_harmonics_kernel`** (survives every group-theoretic identity and the Ni fixture; must die by the brute-force `wigner_D` sum and by the `sph_harm_y` oracle when it runs), `d2Coef` evaluated unconditionally in `wigner_d_prime2` (must die by the `.py_func` test at `(0, 0, 0)`), `out=np.empty(...)` accepted by `wigner_d_table_pre` (must die by the `ValueError` test), each of `cache=True`, `nogil=True` dropped and `parallel=True`, `fastmath=True` added on one kernel (must die by the flag test)) -- every mutation must be killed by a named test.
2. Fix, re-run the tests (`-n 0` then `-n 4`), `pre-commit run --files ...`, `uv run sphinx-build -b html doc doc/_build/html` (exit 0; `fukushima2016wigner` visible on `user/bibliography.html`).

## 5. Commit and PR
1. Signed commits (spec + constitution amendments; reference tables + failing tests; implementation; fixes); tick `specs/roadmap.md` Phase 3 boxes; push; PR into fork `develop` with the template, stating GPL-only licensing (EMSphInx-derived: BSD opt-out impossible).
